# Testing

> Last verified against code: pending merge SHA (2026-05-19) — EXTR-1A.2b Real OpenAI Tier 2 default client + run_ledger 통합 landed (Cycle 41, **code change**: operator option A "OpenAI SDK 실제 wiring + run_ledger 한 PR" 2026-05-19; 3 modules — `src/extraction/llm/openai-client.ts` (NEW OpenAIClient + computeTotalCostUsd + OPENAI_TIER_DEFAULT_MODEL + OPENAI_PRICING_USD_PER_1M_TOKENS + OpenAIApiError, fetch-based no npm dep), `src/extraction/llm/client.ts` (interface extension — readonly vendor/tier/model + LlmInvokeResult.totalCostUsd?), `src/extraction/article/article-extractor.ts` (OPS-1A.1 run_ledger wrap + drops db/tier injection + adds runStage/domainOverrideReason deps for EXTR-1A.2c). 28 신규 tests (23 openai_client + 5 article_extractor OPS-1A.1 integration; ArticleExtractor fixture migrated db-injection → process.env.SQLITE_PATH=:memory: pattern). 1258 → 1337 tests (post-codex-round-4: +15 net — 3 missing_usage rejection + 3 incomplete-error usage carry + 3 researchSessionId validation + 2 cost-preserving failRun payload + 1 completeRun-throw failRun rescue + 3 misc; − 1 superseded silent-fallback test) on main. Previous code baseline = dd49e63 (2026-05-19, Cycle 40 PR #99 EXTR-1A.2a Article extractor skeleton). Earlier baselines: 5d037d3 (2026-05-19, Cycle 39 PR #98 EXTR-1A.1 Extractor router) → 49d4a79 (2026-05-19, Cycle 38 PR #97 EXTR-1A.0 Prompt Injection 방어 기반). **Earlier baseline (Cycle 40)**: EXTR-1A.2a Article extractor skeleton + LlmClient interface + INV-0029-* defensive pipeline (mock LLM) landed (Cycle 40, **code change**: operator delegated cut "그럼 몇 개의 슬라이스로 나눠서 작업해" 2026-05-19 — EXTR-1A.2 parent 가 3 sub-slices (2a/2b/2c) 로 분해; 2 modules in `src/extraction/llm/client.ts` + `src/extraction/article/article-extractor.ts` + 23 신규 tests covering full INV-0029-1/3/4/5 defensive pipeline (`checkLlmPolicy` fail-closed → `htmlToText` → `wrapUntrustedForTier` → `LlmClient.invoke`) + multi-vector adversarial defense-in-depth. Concrete OpenAI/Anthropic SDK wiring + run_ledger 통합은 EXTR-1A.2b/2c 으로 defer. 1234 → 1258 tests on main (post-codex-round-2). Previous code baseline = 5d037d3 (Cycle 39 PR #98 merged 2026-05-19, EXTR-1A.1 Extractor router). **Earlier baseline (Cycle 39)**: EXTR-1A.1 Extractor router + interface contract (AC-009 + AC-021 / NFR-007) landed (Cycle 39, **code change**: operator 표준 직정 "계속 진행 이슈 없으면 멈추지 마" — D7 sequence 의 EXTR-1A.0 unblock 직후 진입; 4 modules in `src/extraction/router/` (types + registry + router + index) + 25 신규 tests (tests/unit/extractor_router_test.ts) covering SOURCE_TYPE enum + isSourceType + ExtractorRegistry round-trip + 3 typed errors + TEST-009 article/dataset/report dispatch + envelope validation + envelope-consistency fail-closed + TEST-021 dry-run extensibility. 1209 → 1234 tests on main. Previous code baseline = 49d4a79 (Cycle 38 PR #97 merged 2026-05-19, EXTR-1A.0 Prompt Injection 방어 기반). **Pre-push rebase check**: main 동기 — origin/main = 49d4a79 (PR #97 직후 fresh, no intervening commits). **Earlier baseline (Cycle 38)**: EXTR-1A.0 Prompt Injection 방어 기반 (ADR-0029 INV-0029-1/3/4/5 enforcement) landed (**code change**: operator "extra 진입" 2026-05-19 = D7 production-caller-0 reactivation 의 P0-M3 EXTR-1A.* phase 진입 첫 slice + **6 codex hardening rounds**. 3 신규 modules + 123 신규 tests (round-0 base 63 + round-1 +11 + round-2 +12 + round-3 +9 + round-4 +3 + round-5 +15 + round-6 +10): `src/extraction/sanitize/html-to-text.ts` (htmlToText + DANGEROUS_TAGS + QUOTE_AWARE_TAG_STRIP + MALFORMED_TAG_FALLBACK) — INV-0029-5 6-step pipeline (comments/CDATA/DOCTYPE/PI strip with orphan fail-closed → DANGEROUS_TAGS pass × 2 [closed/self-closing/orphan EOF] → quote-aware tag strip × 2 [+ malformed quoted attr fail-closed fallback] → iterative entity decode for double-encoded payloads); `src/extraction/prompt/untrusted-wrapper.ts` (wrapUntrusted + wrapUntrustedForTier + escapeSentinelLiterals + TIER_TOKEN_CAPS + **CHARS_PER_TOKEN_HEURISTIC=1 universally-safe** [PR #97 codex round 2 P2 — English over-trim 의 tradeoff 로 CJK/multi-byte 도 cap 보장]) — INV-0029-1 sentinel + escape (isolation contract) + INV-0029-3 per-tier 토큰 상한 (Tier 3=4000 / Tier 2=8000 / Tier 1+=16000); `src/extraction/policy/llm-policy-gate.ts` (checkLlmPolicy + LlmProhibitedError + LlmManualReviewRequiredError + SourceNotRegisteredError) — INV-0029-4 fail-closed gate. ADR-0029 frontmatter INV-0029-1/3/4/5 cross_ref_code 4 entries 추가 — INV-0029-2 코드 패턴 금지 (no eval/Function/exec) 는 별도 static check anchor 후속). 1209 tests on main (Cycle 35 1086 baseline + 123 신규 over 6 codex rounds: round 0 initial 63 = 31 html_to_text + 20 untrusted_wrapper + 12 llm_policy_gate; round 1 P2 hardening +11 = CJK token cap + sentinel escape + entity-decode pipeline + orphan fail-closed; round 2 P2 hardening +12 = universally-safe token ratio + uppercase entity decode + decoded ordinary tag strip + quote-aware tag stripper + DOCTYPE strip; round 3 P2 hardening +9 = CDATA strip + orphan comment fail-closed + malformed quoted attr fallback + iterative double-encoded entity decode + IMPL_PLAN row sync + current-state active fields sync; round 4 P2 hardening +3 = CDATA unwrap preserves benign feed-content (RSS description/content:encoded) + TESTING.md cap description sync; round 5 P2 hardening +15 = decode-until-stable [6+/10-level nested ampersand bypass, MAX_DECODE_ITER=100 fail-closed when not stabilized] + semicolonless legacy entities `&lt`/`&gt`/`&amp`/`&quot` [HTML5 §13.5] + whitespace/case-tolerant tag-form sentinel escape `</untrusted >` / `< /untrusted>` / `</UNTRUSTED >` / `<untrusted >` opener variants; round 6 P2 hardening +10 = semicolonless numeric entities `&#60script` / `&#x3Cscript` [HTML5 §13.2.5.74] + greedy DANGEROUS_TAGS closed regex [nested `<script>a<script>b</script>payload</script>` body-leak 종결] + post-decode declaration strip [entity-encoded `<!--...-->` / `<!DOCTYPE>` / orphan encoded comment bypass 종결]). **Cycle 38 changes (code + doc, pending merge SHA, 본 PR engineering)**: (1) `src/extraction/` 디렉터리 신설 (sanitize/ + prompt/ + policy/ 3 sub-trees, EXTR-1A.* phase 의 시작점). (2) htmlToText pipeline: HTML comment strip → DANGEROUS_TAGS (script/style/noscript/iframe/object/embed) 완전 제거 (case-insensitive + self-closing + orphan opening 방어) → 일반 태그 strip → HTML entity decode (basic + numeric + hex 포함 Korean codepoint) → whitespace collapse + trim. (3) wrapUntrusted: `<untrusted>\\n{content_truncated}\\n</untrusted>` sentinel, maxTokens × CHARS_PER_TOKEN_HEURISTIC (=1, universally-safe per PR #97 codex round 2 P2 — English over-trim 의 tradeoff 로 CJK/multi-byte 도 cap 보장) truncation. wrapUntrustedForTier(content, tier) 편의 wrapper. INV-0029-1 system-prompt 경고는 caller 의무 명시. (4) checkLlmPolicy: in-memory SQLite test fixture 패턴 (production 은 getDb() 사용); switch 문 각 enum + unregistered + default fail-closed branch; sourceId TypeError 검증; typed error class 의 sourceId 필드 노출로 caller 가 instance match 후 detail 추출 가능. (5) future-implementation-inventory ADR-0029 entry 업데이트: design-only → partially-implemented (4/5 INVs cross_ref'd; INV-0029-2 deferred). **Scope boundary (operator "extra 진입")**: EXTR-1A.0 은 PRE-0029-2 의 defensive infrastructure 만 구현, extractor router (EXTR-1A.1) / article extractor (EXTR-1A.2) / Tier 1 escalate (EXTR-1A.3) 등 후속 slices 는 EXTR-1A.0 완료 후 별도 진입. INV-0029-2 (no-eval pattern enforcement) 는 single cross_ref 로 표현 어려운 패턴 absence 형 invariant — 별도 static-check anchor 가 EXTR-1A.* 후속 hardening slice 로 register. Previous code baseline = 2f2430f (Cycle 35 PR #94 ADR-0012 vault/JSONL validator). **Pre-push rebase check**: 본 PR push 직전 fetch + ahead check 재실행 — Cycle 36 (PR #95) + Cycle 37 (PR #96) 이 doc-only 이라 main 변화는 그 2 commits 만, ahead count 확인 후 필요 시 rebase. **Cycle 35 changes (code + doc, landed via 2f2430f)**: ADR-0012 INV-0012-5 + INV-0012-6 pure policy validator landed (Cycle 35, **code change**: operator D4 2026-05-18; `scripts/check-vault-jsonl-policy.ts` 신규 + 1 codex round hardening with MDX/EditorialIntent/scenario-citation), 1086 tests on main (Cycle 35 baseline 유지 + Cycle 36-37 doc-only no test delta) — **Thin-doc edits since 2f2430f (Cycle 35 merge SHA, PR #94 ADR-0012 vault/JSONL validator) → Cycle 36 merge (PR #95, INFRA-1A.10 glossary backfill) → this commit (2026-05-19, Cycle 37 doc-only)**: **INFRA-1A.11-future-implementation-inventory** (operator D6 2026-05-18 — `docs/context/future-implementation-inventory.md` 신설, 19 design-stage ADRs / 117 invariants 분류: 3 superseded + 2 partially-implemented + 14 design-only; validator policy 명시 — future-implementation invariant 의 `cross_ref_code` 부재는 violation 아님). Earlier same-baseline: Cycle 36 **INFRA-1A.10-glossary-backfill** (operator D5 2026-05-18 — 3 pre-existing validator WARN term_effects glossary missing 종결: `retention_protected_kinds` (DEC-007), `data_science_module` (DEC-010, ADR-0024), `cross_vendor_reviewer` (DEC-010, INV-0023-4)). validator WARN 5 → 2 (잔존 2건은 README.md frontmatter id 누락 — separate hygiene slice, scope 외). 1086 tests pass (no test delta — pure docs). **Cycle 35 changes (code + doc, landed via 2f2430f)**: (1) `scripts/check-vault-jsonl-policy.ts` 신규 — `VaultFileEntry` type + `VAULT_ROOTS = ["vault", "docs/vault"]` + `PERMITTED_VAULT_KINDS = {documenthub, dossier, scenario, thesis, contentdraft, publication, promotedclaim}` (separator + case insensitive normalize) + `ALLOWED_JSONL_PATH_PREFIXES = [.dev-cycle/, docs/audit-export/, docs/_generated/, tests/fixtures/, tests/lint/fixtures/, tests/policy/fixtures/]` + `findVaultFiles(repoRoot)` (recursive walk + frontmatter type parse via js-yaml) + `assertVaultContentKinds(files)` (모든 vault Markdown 의 type 검증 — missing or forbidden 시 fail) + `findJsonlFiles(repoRoot)` (repo-wide `*.jsonl` walk skipping node_modules/.git/dist/build/.next/.turbo) + `assertJsonlIsNotCanonical(files)` (ALLOWED_JSONL_PATH_PREFIXES 외 *.jsonl 거부) + aggregate `checkVaultJsonlPolicy(repoRoot)` + `VaultJsonlPolicyError` typed error + `import.meta.main` CLI guard. (2) Both assertions report ALL violations in one error message (not fail-fast). (3) `tests/policy/vault_jsonl_policy_test.ts` 신규 — 32 tests: 4 live (findVaultFiles/findJsonlFiles/assertions/checkVaultJsonlPolicy aggregate) + 8 vault content kinds (canonical 7 permit / case+separator normalize / missing type / forbidden type 'candidate_claim'/'note'/'misc'/'draft' / multi-violation reporting / VAULT_ROOTS+PERMITTED_VAULT_KINDS export coverage) + 12 JSONL allowlist (4 allowed paths pass / 6 forbidden paths (src/ data/ migrations/ docs/adr/ repo-root) fail / multi-violation reporting / ALLOWED_JSONL_PATH_PREFIXES export coverage) + 8 filesystem round-trip via tmpdir (mkdtempSync + mkdirSync + writeFileSync + rmSync — findVaultFiles enumerates with frontmatter, finds docs/vault/ too, findJsonlFiles repo-wide, skips node_modules/.git, checkVaultJsonlPolicy clean tmpdir / INV-0012-5 violation / INV-0012-6 violation). (4) ADR-0012 INV-0012-5 + INV-0012-6 frontmatter 에 cross_ref_code 추가 — 각각 `scripts/check-vault-jsonl-policy.ts:assertVaultContentKinds` + `:assertJsonlIsNotCanonical`. (5) `docs/current/CODE_MAP.md` 에 신규 script + 신규 test row 등록. **Scope boundary (운영자 D4 explicit)**: pure validator, NOT runtime wiring. INV-0012-5 의 "candidate claim 자동 markdown 생성 금지" execution-level enforcement 는 EXTR-1A.* phase deferred. 도메인이 ADR-0023 (D3, LLM routing) 와 다르므로 별도 PR 로 분리 (operator D4 명시). ADR-0012 INV backfill cumulative 7/7 완료 (INV-0012-1 + 2 + 3 + 4 + 5 + 6 + 7 모두 cross_ref_code 보유). Previous code baseline = 80182d5 (Cycle 34 PR #93 ADR-0023 config validator + 3 codex round hardening). **Pre-push rebase check**: 본 PR 은 D4 sequence — D3 (PR #93) merge 이후 즉시 진입, main 변화 없음 가정 (push 직전 fetch + ahead check 재실행). Previous code baseline = pending merge SHA (Cycle 33 PR #92 INV-0028-6 declared-vs-sniffed enforcement). Earlier baselines: 15e9e81 (Cycle 32 PR #90 raw fetch ban), d672ce1 (Cycle 27 PR #83 Opus followup), 589ae9d (Cycle 17 PR #75 INV-0017-4 backfill + stripCommentsAndStrings hidden fix). **Cycle 34 changes (code + doc, landed via 80182d5)**: ADR-0023 INV-0023-2 + INV-0023-3 + INV-0023-5 pure config validator landed (operator D3 2026-05-18; `scripts/check-llm-routing-config.ts` operator D3 2026-05-18; `scripts/check-llm-routing-config.ts` 신규 — `data/llm_routing.yaml` 의 Tier 0 vendor role lock (INV-0023-2) + capability-canonical-not-price (INV-0023-3) + Google scope = Tier 3 only (INV-0023-5) 3 invariants pure validator; LLM 실제 호출 없이 config schema enforcement 만), 1037 tests on main (Cycle 33 970 baseline + 67 신규 over 3 codex rounds: round 0 initial 37 = 5 live + 8 Tier0 + 11 capability + 12 Google + 1 loader; round 1 codex P1+P2 hardening +25 = 7 Tier 0 effort + 10 capability VALUE scan + 8 Google all-tier + tier_3 role allowlist; round 2 codex P2 +5 = composite key token-boundary scan cost_basis/price_axis/budget_label/cheap_indicator/negative-false-match). **Cycle 34 changes (code + doc, pending merge SHA, 본 PR engineering)**: (1) `scripts/check-llm-routing-config.ts` 신규 — `LlmRoutingConfig` type + `loadLlmRoutingConfig(path)` YAML parser + 3 assertion exports (`assertTier0VendorRoles` / `assertTiersCapabilityCanonical` / `assertGoogleScopeIsTier3Only`) + aggregate `checkLlmRoutingConfig(path)` + `FORBIDDEN_TIER_PRICE_AXIS_KEYS` Set + `NON_GOOGLE_TIERS` / `GOOGLE_REQUIRED_TIER` constants + `LlmRoutingConfigError` typed error + CLI entry `import.meta.main` (`bun run scripts/check-llm-routing-config.ts data/llm_routing.yaml`). (2) **INV-0023-2 enforcement**: tier_0.openai.role === 'default' + tier_0.anthropic.role === 'cross_vendor_review_only' + tier_0.google omitted or role==='disabled'. (3) **INV-0023-3 enforcement**: every tier has non-empty string `capability` field; NO tier-canonical key from {price, cost, cheap, budget, cost_tier, price_tier, price_class, cost_class} case-insensitive; vendor sub-blocks 는 exempt (operational cost metadata 가능). (4) **INV-0023-5 enforcement**: tier_0/1/2.google omitted or role==='disabled'; tier_3.google.role 존재하고 non-disabled. (5) `tests/policy/llm_routing_config_test.ts` 신규 — 37 tests (5 live YAML integration + 8 assertTier0VendorRoles synthetic positive/negative + 11 assertTiersCapabilityCanonical positive/negative + 12 assertGoogleScopeIsTier3Only positive/negative + 1 loader error path). (6) ADR-0023 INV-0023-2 / INV-0023-3 / INV-0023-5 frontmatter 에 cross_ref_code 추가 — 각각 `scripts/check-llm-routing-config.ts:assertTier0VendorRoles` / `:assertTiersCapabilityCanonical` / `:assertGoogleScopeIsTier3Only`. (7) `docs/current/CODE_MAP.md` 에 신규 script + 신규 test row 등록. **Scope boundary (운영자 D3 explicit)**: production wiring 아님 — LLM 실제 호출 / runtime routing 변경 없음. pure config/policy validator. INV-0023-4 (cross_vendor_review_ledger) + INV-0023-6 (Data Science Module) + INV-0023-8 (quote substring) 은 EXTR-1A.* phase defer 유지 (production caller 0). Previous code baseline = 0...0 (Cycle 33 PR #92 INV-0028-6 declared-vs-sniffed enforcement, merged 직전). **Pre-push rebase check**: `git fetch origin main` 0 commits ahead (PR #92 직후, parallel PR 없음). **Cycle 33 changes (code + doc, landed pending merge SHA — backfill 후속 PR)**: (1) `src/discovery/fetch/safe-fetch.ts` 에 `DeclaredContentGroup` type + `classifyDeclaredContentType(contentType): DeclaredContentGroup` (application/rss+xml + application/atom+xml + application/xml + text/xml → xml; application/feed+json + application/json → json; text/html → html; 나머지 / null / undefined → unknown — `;` parameter strip + lowercase + trim). (2) `isContentTypeMismatch(declared, sniffed): boolean` (unknown 어느 쪽이든 false / sniffed=executable 은 true defense-in-depth / 둘 다 known 인데 group 이 다르면 true). (3) safeFetch Step 8 (line 840-) executable reject 다음 declared-vs-sniffed 검증 분기 추가 — `response.headers.get("Content-Type")` 읽고 classifyDeclaredContentType 으로 분류, isContentTypeMismatch true 면 `ContentTypeMismatchError(declared, sniffed)` throw. (4) ContentTypeMismatchError 는 PR #82 (Cycle 24) 시점 정의돼 있었으나 production caller 0 였음 — 본 PR 이 wiring 종결, Cycle 24 partial enforcement note 폐기. (5) `tests/unit/safe_fetch_test.ts` 117 → 158 tests (+41 신규 = 17 classifyDeclaredContentType describe + 12 isContentTypeMismatch describe + 12 safeFetch integration describe — empty body + missing Content-Type + executable precedence + 정상 path 3종 + mismatch path 3종 + error message + text/plain pass-through). (6) `docs/current/CODE_MAP.md` safe-fetch.ts row + safe_fetch_test.ts row 갱신 (158 tests). **Scope boundary (운영자 D1 explicit do-not-do)**: EXTR-1A.* wiring / policy_decisions ledger writer / recordIntervention / LLM routing / Data Science Module / source allowlist schema / 모든 MIME type 일반화 — 모두 본 PR scope 외. 본 PR 은 safe-fetch boundary 의 RSS/JSON/HTML declared-vs-sniffed mismatch production wiring 만. Previous code baseline = 15e9e81 (Cycle 32 PR #90 INFRA-1A.9.h1 raw fetch ban static check). **Rebase note**: 본 PR 은 PR #90 (Cycle 32 raw fetch ban) + PR #91 (INFRA-1A.9.h2/h3 + DOC-1A.1 planned slice register) 가 parallel land 한 뒤 origin/main 으로 rebase 처리 — Cycle 32 → 33 renumber. **Cycle 32 changes (code + doc, landed via 15e9e81)**: (1) `scripts/validate_invariants.ts` 에 `checkRawFetchBan()` + helper `findRawFetchCalls()` + exports `RAW_FETCH_BAN_DIRS` / `RAW_FETCH_BAN_ALLOWLIST` / `RAW_FETCH_BAN_PATTERN` 추가. main check chain (`checkRequiredFields → checkIdUniqueness → checkTermEffects → checkScopeCreep → checkCrossRefCode → checkRawFetchBan`) 에 등록. (2) Detection: `(?<![.\p{ID_Continue}$])fetch\s*\(` Unicode-aware negative lookbehind — `.fetch(` / `$fetch(` / `prefetch(` / 한글 identifier suffix 모두 false-positive 차단. scope = `src/discovery/**` + `src/extraction/**`. allowlist = `src/discovery/fetch/safe-fetch.ts` (canonical wrapper). comment / string / regex-literal stripping 은 기존 `stripCommentsAndStrings` 재사용. (3) `tests/lint/raw_fetch_ban_test.ts` 신규 (+19 tests = pattern preceding-char exclusion 8 + comment/string defense 6 + repo-wide invariant 5). 마지막 repo-wide 테스트는 production-code compliance 를 hard test failure 로 pin — validator 자체는 INV-0002-1 warning-level 이지만 위반 introduction 은 CI fail. (4) `docs/adr/0028-safe-fetch-adversarial-web-defenses.md` INV-0028-1 statement body 에 static enforcement landed 명시. **`cross_ref_code` self-reference (scripts/validate_invariants.ts:checkRawFetchBan) 은 의도적으로 추가하지 않음** — validator 의 self-stripping pass 가 자신의 regex-literal pattern body 들과 상호작용하여 running backtick parity shift 가 발생, 해당 entry 가 false `export not found` 로 fail 함 (본 cycle 에서 직접 실험으로 확인). statement body 문장이 static check 의 authoritative reference. (5) `docs/context/current-state.md` 의 INFRA-1A.9.h1 anchor 를 planned → landed (strikethrough + 본 cycle 내용 명시). 911 → 930 tests (+19). **Opus PR #66~#78 review deferred findings 상태**: F1 full (production wiring) → EXTR-1A.* roadmap scope 유지. F3 (semantic level) → DEC-020 Q-045 wording correction 으로 covered. **F4 → 본 cycle 에서 landed.** F7 (junction table) → schema migration v10+. F10 (source_role) → INFRA-1B.1.h2-source-profile anchor 유지. F11 (TESTING hygiene) → P3 별도 PR. 남은 follow-up anchor = INFRA-1A.9.h2-cross-ref-semantic-level (P3, optional). Earlier baseline = cbf9bf6 (2026-05-18, PR #89) — INV-0002-1 + INV-0002-5 + INV-0006-5 cross_ref_code final misc batch (Cycle 31, INFRA-1A.9 follow-up wrap-up). Pre-this-PR baseline = d672ce1 (2026-05-18, PR #83) — Opus PR #66~#78 adversarial review followup engineering bundle (Cycle 27, **code change**: F2 unregistered source fail-closed generalization + F9 snapshot-id helper validation + F3/F5/F8 doc corrections + F1 partial wiring scope clarification + F4/h2 follow-up anchor register), 911 tests on main. **Cycle 27 changes (code + doc, landed via d672ce1)**: (1) `src/pipeline/policy-gate/risk-triggers.ts` `detectTermsViolation` 가 `sourceId=null` + collection action 조합에서 fire 하도록 일반화 (semantic = unregistered source 는 terms compliance 미증명 → fail-closed) — pre-fix `content_production` stage + `extract_full_text` + `sourceId=null` + `archive=full_snapshot_allowed` 조합이 stage-default `batch_report` → `allow` 로 fall-through 하던 silent bypass surface 닫음. (2) 같은 파일 `evaluatePolicyGate` 에 symmetric boundary throw 추가 — `sourceId=null` 이면 3 policy field 모두 `'unknown'` sentinel 강제 (registered-source unknown fail-closed 의 symmetric 짝). (3) `src/domain/snapshot-id.ts` `snapshotR2Key` + `formatSnapIdRationalePrefix` 에 내부 `assertValidSnapId` 호출 추가 — `recordR2UploadDecision` PR #62 writer-boundary fail-fast 와 symmetric. (4) `docs/adr/0017-source-policy-gate-mode-aware.md` INV-0017-5 statement + body §Decision policy_decisions SQL 을 actual v1+v7+v9 schema 와 정합 (pre-correction `gate_mode` / `reason` / `risk_level` / `intervention_id` field 표현 정정) + production wiring scope (EXTR-1A.*) 명시 (F1 partial). (5) `docs/adr/0017-source-policy-gate-mode-aware.md` drift history 에 INV-0017-5 schema correction (F8) + PR #76 metadata defect (F5) 기록. (6) `docs/decisions/DEC-020.md` Q-045 본문에 `cross_ref_code` semantic scope 명시 — reachability heuristic vs enforcement proof 구분 (F3 wording correction). (7) `docs/context/current-state.md` Opus PR #66~#78 review followup section + planned follow-up anchors (`INFRA-1A.9.h1-safe-fetch-raw-fetch-static-check` / `INFRA-1A.9.h2-cross-ref-semantic-level`) 등록. `tests/policy/gate_test.ts` +7 tests (F2 boundary throw 3 + F2 unregistered detector fire 4) + `tests/unit/snapshot_id_test.ts` 2 caller-obligation tests → throw expectation 으로 update = 904 → 911 tests. **INV-0017-5 production wiring scope clarification**: `recordPolicyGateDecision()` 의 production caller 는 EXTR-1A.* (P0-M3+) 까지 deferred — v0 closure 는 operator-gate namespace writer contract 만 해당. chunker 는 INFRA-1B.4.h1 archive_policy gate 별도, R2 upload 는 `recordR2UploadDecision()` (intended_action='r2_upload' namespace) 별도. **INFRA-1A.9 cross_ref backfill 누적 유지 (Cycle 26 종결 기준 16 entries)**: INV-0012-3 (4) + INV-0023-1 (1) + INV-0017-1 (1) + INV-0017-2 (1) + INV-0017-3 (1) + INV-0017-4 (2) + INV-0028-1~6 (6) = 16 entries. 본 PR Cycle 27 은 cross_ref 추가 아닌 evaluator + ADR schema correction PR — INV-0017-5 statement schema correction 만 land + cross_ref_code 추가는 production caller wiring 후 (EXTR-1A.*) 까지 defer. Thin-doc edits since 589ae9d → bbbc518 (Cycle 18 doc-fix, PR #76 merge) → Cycle 19 (PR #77 merge, INV-0028-1) → 178f274 (Cycle 20 merge, PR #78, INV-0028-2) → 11b2e03 (Cycle 21 merge, PR #79, INV-0028-3) → 16ec152 (Cycle 22 merge, PR #80, INV-0028-4) → b5a4b64 (Cycle 23 merge, PR #81, INV-0028-5) → 284588b (Cycle 24 merge, PR #82, INV-0028-6, INV-0028-* 시리즈 종결) → 4b44568 (Cycle 25 merge, PR #84, INV-0017-1) → dbc9915 (Cycle 26 merge, PR #85, INV-0017-2): **INV-0017-2 backfill** (policy_gate mode-aware enum invariant — `policy_gate는 mode-aware다 — {inline_block, inline_warn, batch_report}` — `src/utils/enums.ts:POLICY_GATE_MODE` source-of-truth enum constant; line 206 `export const POLICY_GATE_MODE = ["inline_block", "inline_warn", "batch_report"] as const` 정확히 statement 의 3 값과 일치. production caller chain: `isPolicyGateMode` runtime type guard (line 208) → `src/pipeline/policy-gate/decision-ledger.ts:134` writer-boundary 검증 + `src/pipeline/policy-gate/risk-triggers.ts:456 stageDefaultMode` return type. INV-0017-2 의 두 번째 절 "단계별 default mode" 는 이미 INV-0017-3 (Cycle 16 PR #74, target `stageDefaultMode`) 이 cover; 본 backfill 은 statement 의 enum 정의 부분에 집중 — INV-0017-3 와 target 분리. ADR-0017 INV backfill cumulative = INV-0017-1 (PR #84) + INV-0017-2 (this PR) + INV-0017-3 (PR #74) + INV-0017-4 (PR #75); INV-0017-6 / INV-0017-7 은 access_interventions / severity calc 영역으로 별도 후속 검토. INV-0017-5 는 EXTR-1A.* phase 까지 defer 유지. INFRA-1A.9 backfill 누적 = INV-0012-3 (4) + INV-0023-1 (1) + INV-0017-3 (1) + INV-0017-4 (2) + INV-0028-1~6 (6) + INV-0017-1 (1) + INV-0017-2 (1) = **16 entries** (as of Cycle 26 close). → d672ce1 (Cycle 27 merge, PR #83, Opus review followup — **code change**, new baseline) → this commit (2026-05-18, Cycle 28 doc-only, PR #86 rebased onto d672ce1): **INV-0012-7 backfill** (외부 LLM raw third-party text 송신 invariant 의 명시적 cross-ADR enforcement layer "policy_gate (ADR-0017) inline_block" 을 `src/pipeline/policy-gate/risk-triggers.ts:detectRisks` 로 cross_ref. INV-0017-4 (PR #75) 와 same target 이지만 다른 ADR 의 다른 invariant — INV-0017-4 는 8-trigger mode-invariance 의무, INV-0012-7 은 그 중 external_llm 검사 invariant. detectRisks aggregator 가 8 detector module-private 함수를 묶음, 그 중 `detectExternalLlmRawTextUnauthorized` (risk-triggers.ts:190) 가 INV-0012-7 enforce. **추가 변경**: 본 PR 안에서 INV-0012-7 의 pre-existing missing `status: active` field 도 동시 추가 — INV-0012-1~6 sibling consistency 회복. INFRA-1A.9 backfill 누적 16 → **17 entries**. **본 PR 은 PR #83 (Cycle 27 code change) merge 이후 rebase 처리** — Cycle 28 doc-only 로 renumbered, baseline d672ce1 위에서 작동) → c58cf30 (Cycle 28 merge, PR #86, INV-0012-7) → f29f075 (Cycle 29 merge, PR #87, INV-0023-7 — 17 → 18 entries) → this commit (2026-05-18, Cycle 30 doc-only, **batch PR per operator directive 2026-05-18 — 단일 INV 당 PR 패턴 → 다중 INV batch PR 패턴 전환**): **INV-0012-1 + INV-0012-2 + INV-0012-4 batch backfill** — 3 ADR-0012 schema/canonical-store invariants 한 PR 안 종결. (1) **INV-0012-1** (Neo4j canonical store for graph objects) → `src/storage/neo4j/connection.ts:withSession` (driver session boundary, 모든 graph query 의 production entry — r2-invariant-scanner / snapshot-fingerprint 등 10+ callers); (2) **INV-0012-2** (SQLite canonical store for relational metadata) → `src/storage/sqlite/connection.ts:getDb` (db handle entry, run-ledger / r2-invariant-scanner 등 10+ callers); (3) **INV-0012-4** (R2 permitted artifact store + raw_cloud_policy default = always_prohibited) → `src/storage/r2/policy.ts:checkPermittedPrefix` (R2 prefix 검증, INV-0012-3 와 same target 의 statement-distinct invariant) + `src/storage/source-registry/seed.ts:seedSources` (raw_cloud_policy default enum guard, INV-0017-1 과 same target 의 R2-perspective invariant). ADR-0012 INV backfill cumulative = INV-0012-1 (this PR) + INV-0012-2 (this PR) + INV-0012-3 (PR #72) + INV-0012-4 (this PR) + INV-0012-7 (PR #86) = 5 entries on 5 invariants (총 8 cross_ref_code targets). INV-0012-5 (Markdown vault rules) + INV-0012-6 (JSONL canonical 아님) 은 policy-level invariant 로 code enforcement 부재 — skip. INFRA-1A.9 backfill 누적 18 → **22 entries** (4 신규 targets: withSession + getDb + checkPermittedPrefix duplicate + seedSources duplicate). → 6308c2b (Cycle 30 merge, PR #88, INV-0012-1+2+4 batch) → this commit (2026-05-18, Cycle 31 doc-only, **final misc batch**): **INV-0002-1 + INV-0002-5 + INV-0006-5 batch backfill** — 본 INFRA-1A.9 stream 의 final clean candidates 일괄 종결. (1) **ADR-0002 INV-0002-1** (validator warning-level only, CI exit 0 unconditional) → `scripts/validate_invariants.ts:724` (file:line, `main()` 함수 정의 — process.exit 코드 path 의 anchor); (2) **ADR-0002 INV-0002-5** (term_effects 의무 — body-only drift 차단 Case 2) → `scripts/validate_invariants.ts:184` (file:line, `checkTermEffects()` 모듈-private function — body-only term attribute 변경 시 warn surface); (3) **ADR-0006 INV-0006-5** (LLM run_ledger 의무 — model + tokens + cost + cached_tokens + batch_id 기록) → `src/ops/run-ledger.ts:startRun` (INV-0023-7 same target, ADR-0023 INV-0023-7 statement 이 명시적으로 "ADR-0006 INV-0006-5 의 확장" — 동일 writer 가 두 invariant 모두 enforce). INFRA-1A.9 backfill 누적 22 → **25 entries** (3 신규 targets). **본 batch 이후 stream wrap-up**: clean cross_ref candidates 소진. 잔존 후보는 (a) production caller 0 / EXTR-1A.* phase defer (INV-0017-5/-6/-7, INV-0023-4/-6/-8), (b) config-level invariants without code enforcement (INV-0023-2/-3/-5, INV-0012-5/-6), (c) future-implementation design ADRs (0024 Data Science / 0029 Prompt Injection / 0019 Bidirectional Framing / 0031 Research App Ops 등 26 ADR/DEC 의 architectural design invariants — code 미구현). **Cycle 17 changes (landed via 589ae9d)**: (1) `docs/adr/0017-source-policy-gate-mode-aware.md` 의 INV-0017-4 frontmatter 에 `cross_ref_code: [src/pipeline/policy-gate/risk-triggers.ts:detectRisks, src/pipeline/policy-gate/risk-triggers.ts:evaluatePolicyGate]` 추가. (2) **hidden bug fix**: `stripCommentsAndStrings` 의 single-quote / double-quote regex 가 multi-line greedy 였음. comment 안 unpaired apostrophe (`Reuters'`, `let's`, possessive) 가 next `'` 까지 multi-line span eating → file 의 큰 부분 (risk-triggers.ts 의 경우 84%) 가 strip 되어 valid export 도 false `not found`. fix = single/double-quoted string body 에 `\\n` 제외 추가 (ECMAScript 단일-라인 string 표준 일치). backtick template literal 은 multi-line 유효이므로 그대로 유지. (3) regex literal heuristic 의 character-class body 도 single-line 제약. (4) `stripCommentsAndStrings` 순서 = strings → BLOCK comment → regex literal → line comment (Cycle 17 정정 — round 5/6 의 strings → regex → block → line 순서가 block comment 안 `/` 를 regex literal 로 false-match 하던 cascade). (5) +3 regression tests = comment 안 unpaired apostrophe 후 real export 보존 / comment 안 unpaired double-quote 후 real export 보존 / template literal multi-line 정상 매치. (6) repo-wide invariant test 가 자동 검증 — INV-0012-3 (4) + INV-0023-1 (1) + INV-0017-3 (1) + INV-0017-4 (2) = 총 8 entries 모두 resolve. 899 → 902 tests. **Cycle 16 changes (landed via 243c28a)**: (1) `docs/adr/0017-source-policy-gate-mode-aware.md` 의 INV-0017-3 frontmatter 에 `cross_ref_code: [src/pipeline/policy-gate/risk-triggers.ts:stageDefaultMode]` 추가. (2) repo-wide invariant test 가 자동 검증 — INV-0012-3 (4) + INV-0023-1 (1) + INV-0017-3 (1) = 6 entries 모두 resolve. 899 tests 유지 (cross_ref_code 시스템의 backfill 자동 검증 — Cycle 14 contract). **Cycle 15 changes (landed via 123571a)**: (1) `docs/adr/0023-llm-routing-v2-gpt-default-multi-vendor-minimal-cross-review.md` 의 INV-0023-1 frontmatter 에 `cross_ref_code: [src/ops/run-ledger.ts:startRun]` 추가 (domain_override_reason 의무는 INV-0023-1 의 3-조건 enforcement detail — Cycle 14 PR body 의 "INV-0023-3" 표현은 statement 와 enforcement site 정합성 차원에서 INV-0023-1 로 정정). (2) Cycle 14 의 validator 의 repo-wide invariant test 가 자동으로 본 entry 도 검증 — `scanCrossRefsAcross([...adrFiles, ...decisionFiles])` 가 INV-0012-3 의 4 entries + INV-0023-1 의 1 entry = 총 5 entries 모두 resolve 확인. **No new tests** — repo-wide invariant test 가 backfill 추가에 자동 적용 (validator 가 cross_ref_code 추가 시점에 검증, 새 test 추가 없이 cross_ref drift detection 누적). 899 tests 유지. **Cycle 14 changes (landed via 74bfcfc)**: (1) `scripts/validate_invariants.ts` `DocFrontmatter.invariants[]` type 에 optional `cross_ref_code?: string[]` field 추가 + 신규 `checkCrossRefCode()` 함수 — 각 entry 의 형식 (`<file>:<exportName>` 또는 `<file>:<line>`) 자동 detection, `file:exportName` 은 `export <kw> <name>` 선언 또는 `export { <name> }` re-export 블록 매칭, `file:line` 은 1..lineCount 범위 검사, 깨진 reference 는 warning. (2) `docs/adr/0012-non-archival-storage-neo4j-sqlite-r2.md` 의 INV-0012-3 frontmatter 에 cross_ref_code 4 entries 백필 — `src/storage/r2/policy.ts:checkPermittedPrefix` + `src/storage/audit/policy-decisions.ts:recordR2UploadDecision` + `src/ops/r2-invariant-scanner.ts:scanR2Invariants` + `src/discovery/worker/snapshot-fingerprint.ts:120` (allLinkedSourcesAllowR2SnapshotUpload 의 module-private signature line). (3) +13 tests `tests/lint/cross_ref_code_test.ts` — format detection (3) / file:exportName resolution 4 cases (valid named export + const declaration + re-export block + private symbol fail) / file:line 4 cases (valid in-range + boundary line 1 + out-of-range + line zero) / repo-wide invariant 2 (every cross_ref_code resolves + INV-0012-3 backfill landed) = 820 → 833 tests + 14 tests Codex PR #72 round 1 P2 fix (alias re-export only RHS importable + repo-root path escape reject + default-async/generator/abstract declaration forms — fixture `tests/lint/fixtures/cross_ref_code_fixtures.ts` 신설로 form 별 검증) = 820 → 847 tests + 13 tests Codex PR #72 round 2 fix (physical line count via `physicalLineCount()` — trailing newline 처리 / symlink-aware `isInsideRepo` via `realpathSync` 최심부 존재 ancestor walk-up / type-prefixed re-export specifier `type Foo` + `type Foo as Bar` 처리 / repo-wide scan 이 `docs/decisions` 도 포함) = 820 → 860 tests + Codex PR #72 round 3 fix (5 finding 종결, test refactor 로 parallel impl 제거: validator 의 `isInsideRepo` / `physicalLineCount` / `extractReExportedNames` / `hasNamedDeclaration` / `checkOneCrossRef` / `stripCommentsAndStrings` / `adrFiles` / `decisionFiles` 모두 named export + `import.meta.main` entry guard + test 가 직접 import, comment/string-literal stripping 으로 raw-text regex false-positive 차단, 신규 7 regression tests = ignore commented exports 1 / ignore block-comment 1 / ignore string-literal 3 forms 1 / real-vs-commented sibling 1 / strip preserves content 1 + parallel-impl probe 1 + file-lists parity 1 + DEC-coverage 1) = 820 → 867 tests + Codex PR #72 round 4 fix (3 P2 종결: `extractReExportedNames` 도 `stripCommentsAndStrings` 후 동작 → comment/string 안의 `// export { foo as fakeName }` 가 false-reachable 안 되게 차단; `hasNamedDeclaration` 의 trailing `\b` 를 `(?![A-Za-z0-9_$])` negative lookahead 로 교체 → `foo$` / `$$` / `한글` 같은 식별자 정상 match + prefix-of-longer-identifier false positive 차단; `stripCommentsAndStrings` 순서를 block-comment → strings → line-comment 로 → `"http://..."` 같은 URL string 의 `//` 가 line-comment 로 잘못 인식되어 그 줄 export 가 사라지던 문제 차단; 신규 13 regression tests = 4 (re-export block comment/string strip) + 5 (non-ASCII / `$` identifier) + 4 (string-before-comment ordering)) = 820 → 880 tests + Codex PR #72 round 5 fix (1 P1 + 2 P2 + 2 P3 종결, 1 P3 defer: `checkOneCrossRef` type guard for non-string YAML scalars → number/object/bool/null 도 throw 대신 warning 으로 surface, INV-0002-1 warning-only contract 유지; `stripCommentsAndStrings` 순서 재정렬 strings → block-comment → line-comment → regex-literal-heuristic → `"/*"` 같은 string 안 comment 표현이 진짜 block comment span 으로 오인식되어 사이 export 가 사라지는 문제 + regex literal 안의 fake `export ...` 패턴이 hasNamedDeclaration false positive 인 문제 차단; `hasNamedDeclaration` 의 negative lookahead `\\p{ID_Continue}` Unicode-aware 로 교체 + `/u` flag → `한글` 식별자가 `한글가` 같은 longer identifier prefix match 차단; `scanCrossRefsAcross` 가 `{ issues, inspectedFileCount }` 반환 → DEC test 가 `inspectedFileCount === decisionFiles.length` assertion 으로 강화 (tautological assertion 종결). **Defer** (Cycle 14 scope 외): `export { foo as "string-name" }` ES2020+ string alias syntax (코드 base 사용 0, ADR cross_ref target 으로 사용될 가능성 낮음 — 후속 cycle 의 anchor 로 register). 신규 13 regression tests = 3 (regex literal) + 4 (non-string scalar guard) + 4 (Unicode boundary) + 2 (strings-before-block-comments)) = 820 → 893 tests + Codex PR #72 round 6 fix (2 P2 + 1 P3 종결, 1 P3 defer-유지: `checkCrossRefCode` 가 `Array.isArray` guard 추가 → 비-array `cross_ref_code` (예: `cross_ref_code: src/foo.ts:bar` scalar) 가 string char-by-char iterate 되어 noise burst 내던 문제 차단; `stripCommentsAndStrings` 순서 = strings → regex-literal → block → line-comment → 이전 round 5 의 line-before-regex 순서 가 `/https?:\\/\\//` 같은 regex 안 `//` 를 comment 로 오인식, regex 뒷부분 + 같은 줄 export 삭제하던 문제 차단; `extractReExportedNames` 의 alias + bare regex 를 `\\p{ID_Start}` + `\\p{ID_Continue}` Unicode property escape + `/u` flag 로 교체 → `export { 한글 as 공개 }` 같은 non-ASCII identifier 인식. **Defer-유지** (round 5 P3 #5 와 동일 finding 의 round 6 P3 #4 반복): `export { foo as "string-name" }` ES2020+ string alias — 코드 base 사용 0, follow-up cycle anchor. 신규 6 regression tests = 3 (regex literal pre-line-comment) + 3 (Unicode re-export + defer 명시)) = 820 → 899 tests. **No behavior change to existing checks** — checkCrossRefCode 는 main check chain 에 추가되며 기존 4 check (Required fields / ID uniqueness / Term effects / Scope creep) 의 결과에 영향 없음. **Scope boundary**: 본 PR 은 validator 확장 + INV-0012-3 backfill 만. DEC-020 Q-045 priority 의 나머지 INV-0028-* (safe-fetch 8단 방어) + INV-0023-3 (domain_override_reason) + INV-0017 (audit) backfill 은 다음 cycles 로 분리 (각 ADR 의 enforcement code site 검증이 별도 review surface). **Cycle 13 changes (landed via 742af9b)**: (1) `src/domain/snapshot-id.ts` 에 `formatSnapIdRationalePrefix(snapId): string` helper 추가 — return `\`snap_id=${snapId}\`` (bare field, no trailing delimiter; caller joins with `"; "` separator). (2) `src/storage/audit/policy-decisions.ts` `formatRationale()` 의 inline `\`snap_id=${input.snapId}\`` literal → helper 호출로 전환. writer 와 reader (`RATIONALE_SNAP_ID_PREFIX_REGEX`) 의 `snap_id=` 필드명을 같은 module 안에 정렬하여 한쪽 update without the other 의 silent drift 차단. (3) +4 tests `tests/unit/snapshot_id_test.ts` `formatSnapIdRationalePrefix` describe — build / round-trip via `parseSnapIdFromRationale` with `;` separator / round-trip end-of-string / no caller-side validation 명시 = 816 → 820 tests. **No behavior change** — helper is pure function returning byte-identical string. **Cycle 12 changes (landed via c996775)**: (1) `src/domain/snapshot-id.ts` 신규 — single source of truth `SNAPSHOT_ID_REGEX` + `RATIONALE_SNAP_ID_PREFIX_REGEX` + `SNAPSHOT_R2_KEY_PREFIX` + `snapshotR2Key()` + `validSnapIdOrNull()` + `assertValidSnapId(value, context?)` + `parseSnapIdFromRationale()` (zero imports, domain layer purity). (2) 4 consumer site refactor — `src/storage/audit/policy-decisions.ts` local `SNAP_ID_SHAPE` 제거 + `assertValidSnapId` thin wrapper passing `"recordR2UploadDecision"` context (byte-compatible error message preserved); `src/ops/r2-invariant-scanner.ts` 3 local const (`SNAPSHOT_R2_KEY_PREFIX` / `SNAP_ID_RATIONALE_PREFIX` / `SNAP_ID_SHAPE`) + 2 internal validator function 제거 + reader-boundary `parseSnapIdFromRationale` / `validSnapIdOrNull` re-export (downstream importers unchanged); `src/discovery/worker/snapshot-fingerprint.ts` 2 inline `permitted_artifact/derived/snapshot/${snapId}` template → `snapshotR2Key()`; `src/storage/r2/policy.ts` `PERMITTED_PREFIXES[5]` literal → `SNAPSHOT_R2_KEY_PREFIX` import. (3) +18 tests `tests/unit/snapshot_id_test.ts` — shape regex (2) + R2 prefix literal + slash-termination (2) + `snapshotR2Key` (2) + `validSnapIdOrNull` (2) + `assertValidSnapId` 4 variants (well-formed / malformed regex / context-prefix / no-context) + `parseSnapIdFromRationale` (6 — canonical + end-of-string + null/empty + non-anchored + 5 invalid-trailing-char regression + space-no-semicolon) + `RATIONALE_SNAP_ID_PREFIX_REGEX` capture (1) = 798 → 816 tests. **No behavior change** — Opus adversarial review NO ACTIONABLE FINDINGS (7 obligation pass: behavior drift / layering / drift-surface scan / re-export pattern / stale comment scan / test coverage / import path arithmetic). Informational defer: writer-side `formatRationale()` 의 `\`snap_id=${input.snapId}\`` literal (5번째 잠재 consolidation 후보, 본 slice scope 외 — 별도 `formatSnapIdRationalePrefix()` helper 추출 후속 cycle 가능). **Cycle 11 changes (pending merge SHA)**: (1) `src/pipeline/policy-gate/risk-triggers.ts` 신규 — `RiskTriggerContext` + 8 detector (`detectExternal LlmRawTextUnauthorized` / `detectPaywalledSourceFetch` / `detectTermsViolation` / `detectWireServiceFullText` / `detectArticleRawQuoteOrCache` / `detectImageInclusionWithoutLicense` / `detectRawEmbedding` / `detectRawCloudUpload`) + `detectRisks()` + `stageDefaultMode()` + `evaluatePolicyGate()` (pure function, no SQLite I/O); (2) `src/pipeline/policy-gate/decision-ledger.ts` 신규 — `recordPolicyGateDecision()` generic writer (operator-gate namespace, intended_action=NULL, v8 r2_upload enum trigger bypass via WHEN clause) + `NON_RISK_TRIGGER_TYPE` sentinel + writer-boundary enum guards; (3) `src/utils/enums.ts` 확장 — `POLICY_GATE_MODE` (3) + `GATE_DECISION` (3: allow/warn/block) + `RISK_TRIGGER` (8 = ADR-0017 INV-0017-4 List A canonical) + `PIPELINE_ACTION` (11) + `PIPELINE_STAGE` (5 = ADR-0017 INV-0017-3 default mode mapping); (4) `tests/policy/gate_test.ts` 신규 — 53 tests (6 stageDefaultMode + 24 detectRisks 8 trigger fire/not-fire/mode-invariance/multi-trigger + 8 evaluatePolicyGate combined + 6 recordPolicyGateDecision namespace + writer-boundary + 9 TEST-023 E2E 8 trigger × ledger × INV-0017-4 mode-invariance). +53 tests initial + 3 tests Codex PR #68 round 1 P1 fix (wire-service bare AP alias + AFP / TASS word-boundary regex + 5 false-positive defense cases) + 3 tests Codex PR #68 round 2 fix (P2 detectExternalLlmRawTextUnauthorized sourceId=null fail-closed even when externalLlmPolicy='allowed' + P1 detectTermsViolation extended to external_llm_call_with_raw_text + external_llm_call_with_excerpt — terms 'no AI / no redistribution' clause coverage) + 6 tests Codex PR #68 round 3 fix (P1 detectExternalLlmRawTextUnauthorized intended_action 확장 = external_llm_call_with_raw_text + external_llm_call_with_excerpt 둘 다 enforce + P2 detectWireServiceFullText sourceName missing fail-closed when sourceId registered) + 5 tests GPT review post-PR-#68 옵션 D fix (Reuters Institute negative lookahead + `Reuters` / `Reuters News` / `Reuters Wire` positive regression + 3 runtime fail-closed boundary guards: stageDefaultMode + evaluatePolicyGate invalid stage / invalid intendedAction) = 717 → 787. Previous code baseline = fdb847a (Cycle 10 INFRA-1B.3.h7-gate-evidence-hardening, post-#65 review followup). Earlier baseline = 17a31ef (2026-05-17) — Cycle 9 / `OPS-1B.h4-r2-audit-column-rationale-drift-axis` (post-#58 GPT review 의 Issue 6 finding 종결, backward-compat contract hygiene). **Cycle 10 changes (landed via fdb847a)**: (1) `parseSnapIdFromRationale` regex 에 `(?=;|$)` delimiter lookahead 추가 — pre-Cycle-10 partial match 가 invalid trailing char (`@`, `.`, `/`, `=`) 통과시켜 Axis 5 silent + Axis 6 truncated comparison 의 root cause; (2) snapshot-fingerprint.ts `skipped_toctou` rationale 의 `mergeMatchedExisting` 분기 (first-create vs MERGE-matched 정확 attribution); (3) `assertValidSnapId` docstring 정정 — "snap_<ULID>" over-claim → "weak token shape, strong ULID guarantee at generation site"; (4) doc drift sweep 5 위치 (본 header / RUNTIME / DATA_MODEL / current-state / IMPL_PLAN). +7 tests (Finding 1 regression). Cycle 9 = AI-P1-15 의 v9 column 도입 + Cycle 8 의 cause-qualified naming 위에서 **column ↔ rationale dual-write contract violation surface** 추가 — `r2_audit_column_rationale_drift` (Axis 6) 가 `recordR2UploadDecision` dual-write 의 divergence 를 explicit violation 으로 노출. 발화 조건 = column + rationale 둘 다 well-formed 인데 서로 다른 snap_id (manual SQL repair / writer format regression / fixture mistake 등). column-only / rationale-only / both-null cases 는 silent (axis 5 or column-preferred resolution 영역). R2UploadOutcomeAuditRow shape 확장 = `columnSnapId` + `rationaleSnapId` raw fields 노출 (`snapId` resolved 값은 backward-compat). +6 tests = 704 → 710 tests total. Tests breakdown: 1 drift detection (column ≠ rationale 둘 다 well-formed) + 1 same-value silent + 1 column-null v8- legacy silent + 1 rationale-null column-preferred silent + 1 both-null Axis 5-only + 1 fetchR2UploadOutcomeAuditRows integration through SQLite read path. 16 existing tests 가 columnSnapId/rationaleSnapId field 로 갱신 (python script 로 일괄 — snapId === rationaleSnapId === columnSnapId canonical happy path 채택). Previous code baseline = 116c9ed (Cycle 8 PR #63 — 700 → 704 + Codex P1/P2 round 1 fix + 옵션 a/c 합산). Cycle 8 = AI-P1-13 의 narrow-scope axis naming + missing skipped_toctou coverage 종결: (1) `R2InvariantViolationType` rename `r2_object_without_graph_key` → `r2_object_without_graph_key_set_failed` (cause-qualified naming — scanner all-clear 의 의미를 정확히 좁힘); (2) 신규 Axis 4b `r2_object_without_graph_key_policy_recheck_skipped` 추가 (skipped_toctou audit row 도 R2 object orphan 상태 surface — 운영자 remediation 이 다름: 단순 rerun-SET 금지); (3) 두 orphan axis details 에 `expectedR2Key = permitted_artifact/derived/snapshot/${snapId}` 출력 (repair-CLI / 운영자 cleanup 직접 사용); (4) CLI formatter + counts 출력에 skipped_toctou 분리. +4 tests = 700 → 704 tests total. Tests breakdown: +1 in Axis 4 describe ("does NOT flag skipped_toctou rows" 정리) + 3 new in Axis 4b describe (skipped_toctou → orphan + does NOT mix with Axis 4 + malformed skipped_toctou → Axis 5). 4 기존 test 가 cause-qualified naming + expectedR2Key assertion 으로 갱신. Previous code baseline = d3ae654 (Cycle 7 PR #62 — 684 → 700). Earlier baselines: 8ac999b (Cycle 6 PR #61), cdd1faf (AI-P1-15 PR #57), 01f029f (AI-P1-14 PR #56), f081e50 (AI-P1-13 PR #54), 2f6ce43 (AI-P0-2 PR #53). Tests breakdown: 2 in audit_policy_decisions_test.ts (snap_id column dual-write + attempted/outcome pair sharing snap_id) + 4 in r2_invariant_scanner_test.ts (v9 column preferred over rationale, legacy v8- rationale fallback, malformed legacy row surfaces Axis 5, mixed v8/v9 resolves via own path). Previous code baseline = 01f029f (AI-P1-14 / INFRA-1B.1.h3-seed-sources-argv-allowlist PR #56 — 662 → 674). Earlier baselines: f081e50 (AI-P1-13 PR #54 — 652 → 662), 2f6ce43 (AI-P0-2 PR #53 — 647 → 652), 6500651 (AI-P1-6 PR #50 — 623 → 647), b660f46 (AI-P1-7 PR #49 — 609 → 623), 31abe60 (AI-P1-12 PR #48 — 569 → 609), AI-P1-1 PR #47 (561 → 569), 090ca5b (AI-P1-3 PR #45 — 544 → 561), 861796a (AI-P1-2 PR #44 — 521 → 544), 327f4b2 (AI-P0-1 PR #41 — 515 → 521), 75706c4 (INFRA-1B.3.x-audit PR #39 — 490 → 515). **Pre-merge SHA reachability**: `git fetch origin <branch>` resolves the branch SHA while the PR is open; post-squash-merge, the merge commit on main is the canonical reference and the next slice's baseline advances.

## Testing policy

- Behavior changes need verification evidence.
- Prefer test-first for bug fixes and clear behavior changes when a concise
  failing test can express the target behavior.
- Otherwise add or update tests in the same slice as the implementation.
- Bug fixes should leave regression coverage unless impractical.
- If automated coverage is not practical, record the manual check, eval, or
  reason.

## Install

```bash
bun install
```

bun + TypeScript + native bun:sqlite + neo4j-driver + Bun.S3Client.

Ruby lint (pre-INFRA-1A.2 doc governance — 별도):

```bash
ruby scripts/check-doc-governance.rb           # default mode (CI에서 실행)
ruby scripts/check-doc-governance.rb --strict  # placeholder remnant 검출 (옵션)
```

## Invariant validator (primary doc check)

```bash
# Read-only validation — exits 0 by design (warning level only)
bun run invariant:check

# Fixture regression tests — verify Case 1 / Case 2 detection
bun run invariant:fixture:scope-creep
bun run invariant:fixture:glossary-drift

# Regenerate docs/_generated/ artifacts (writes scope_tree.yaml / term_usage.yaml / effective_invariant_policy.yaml)
bun run invariant:regen
```

## Foreground recovery (write mode)

```bash
# Foreground LLM only — persists unresolved_warnings into doc frontmatter
# AND orphan_warnings.yaml. Never run from CI; ADR-0002 INV-0002-2.
bun run invariant:write
```

## Code tests (landed, M1~M2)

| Check | Local command | CI workflow / job | Required? | Notes |
|---|---|---|---|---|
| install | `bun install` | ci.yml install job | yes | bun is mandatory runtime |
| typecheck | `bun run typecheck` | ci.yml typecheck job | yes (after Q-048 resolution) | tsc --noEmit --pretty false |
| unit tests | `bun test` | ci.yml test job | yes (after Q-048 resolution) | 26 file / **798 cases** — Bun native runner (post-Cycle-10 INFRA-1B.3.h7-gate-evidence-hardening pending: +7 tests in r2_invariant_scanner_test.ts parseSnapIdFromRationale describe — invalid trailing char (`@` / `.` / `/` / `=`) + whitespace-no-semicolon + end-of-string lone snap_id + canonical happy path; previous baseline 710 = Cycle 9 OPS-1B.h4-r2-audit-column-rationale-drift-axis). |
| migration dry-run | `bun run migrate --dry-run` | (manual) | recommended | verifies all v1~v8 schema files parse (v7 = policy_decisions ADD COLUMN intended_action / INFRA-1B.3.x-audit; v8 = policy_decisions ADD COLUMN upload_attempt_id + 3 enum/required triggers / INFRA-1B.3.h3-audit-hardening AI-P1-7) |
| invariant validation | `bun run invariant:check` | invariant-check.yml | no (warning only) | INV-0002-1: never hard-fails |
| fixture regression | `bun run invariant:fixture:scope-creep` | (manual / pre-PR) | recommended | Case 1 detection |
| fixture regression | `bun run invariant:fixture:glossary-drift` | (manual / pre-PR) | recommended | Case 2 detection |
| regenerate artifacts | `bun run invariant:regen` | invariant-check.yml (post-validate) | yes | writes docs/_generated/ |
| Neo4j FTS bench | `bun run bench:neo4j` | (manual, SPIKE-001) | spike only | needs Neo4j running; AC-002 p95 < 1s |
| Discovery dry-run | `bun run discovery:dry-run` | (manual) | recommended | RSS parse + queue enqueue dry-run |
| seed sources dry-run | `bun run seed-sources:dry-run` | (manual) | recommended | validates data/sources_seed.yaml |
| reproducibility manual | n/a (manual) | n/a | yes (P0-M5 gate) | AC-017 NFR-002 |

## CI / required checks

Workflow files (활성):

| File | Trigger | Required? | Notes |
|---|---|---|---|
| `.github/workflows/doc-governance.yml` | PR + workflow_dispatch | yes (required, 기존) | Ruby doc lint — duplicate / dangling / must-REQ-AC-link checks |
| `.github/workflows/ci.yml` | PR + push | **policy: required (DEC-020 Q-048 accepted) — branch protection admin task 미실시** (3-state 분리, 본 표 아래 "CI required check 등록 3-state" 참조) | bun install + typecheck + bun test (798 cases) + migrate dry-run (pending migration listing only — actual parse/apply validation is `DEPLOY-1A.0-migration-validation` slice 영역, Cycle 10 Finding 4 claim correction) |
| `.github/workflows/invariant-check.yml` | PR + push (paths-scoped) | advisory (warning-level by ADR-0002 INV-0002-1, never required) | bun run invariant:regen + invariant:check + fixture regression (boilerplate fixture 부재 시 informational skip) |
| `.github/workflows/doc-freshness.yml` | PR | advisory (soft warning) | DEC-020 Q-048 활성. src/scripts/tests/migrations 변경 시 thin docs / IMPLEMENTATION_PLAN / current-state / 06_ACCEPTANCE_TESTS / ADR 동반 갱신 누락 PR 코멘트 |

비활성 (rename 대기):

| File | Reason |
|---|---|
| `.github/workflows/cd.yml.example` | publishing pipeline 도입 시점 활성 (P0-M6) |

## CI required check 등록 3-state

DEC-020 Q-048 resolution 안에서 CI required check 활성은 3 단계로 분리되어
있다. 단계가 섞이면 "이미 required 인지 / 사용자 결정만 끝났는지" 가 모호해
지므로 본 표로 명시.

| Stage | 의미 | 현재 상태 (2026-05-14) | 책임 |
|---|---|---|---|
| (1) workflow exists | `.github/workflows/ci.yml` 파일이 활성 trigger 로 등록되어 PR / push 마다 실행 | ✓ done (PR #32 0a76d31, `claude/comprehensive-code-review-FE0w3` 가 rename) | repo (코드) |
| (2) policy decision accepted | 운영자가 ci.yml 을 required 로 promote 한다는 정책 결정 | ✓ done (DEC-020 Q-048 resolution, 2026-05-13) | 운영자 (DEC) |
| (3) branch protection admin task applied | GitHub repo settings → branch protection → require status check 에 `ci.yml` 명시 등록 | ✗ pending — admin task 미실시 (현재 main 은 PR-only protection 만 적용, required check 등록은 별도 admin step) | 운영자 (admin) |

(3) 이 완료되기 전까지는 ci.yml fail 이 PR merge 를 차단하지 않는다.
(1)+(2) 가 완료되었다는 사실 만으로 "required" 로 보고하는 표현은 stage
(3) 까지 lock 된 후 가능. 현재 표 column "Required?" 값은 (2) 정책 기준
("policy: required") + (3) admin 미적용 함께 명시.

invariant-check.yml 은 ADR-0002 INV-0002-1 warning-level contract 이므로
(2) policy decision = "advisory only" 로 lock — (3) 진입 대상 아님 (영구
advisory).

## CI notes (history)

- PR `claude/comprehensive-code-review-FE0w3` 가 `ci.yml.example` →
  `ci.yml` + `invariant-check.yml.example` → `invariant-check.yml` rename
  으로 (1) workflow exists stage 진입.
- 코드 테스트 (`bun test`) 는 26 file / **798 케이스** — Bun native runner
  1 분 이내 expected. History: 490 → 515 (INFRA-1B.3.x-audit PR #39) →
  521 (INFRA-1B.3.h1-policy-fix PR #41, AI-P0-1) → 544 (INFRA-1B.1.h1-
  source-bootstrap-neo4j PR #44, AI-P1-2) → 561 (INFRA-1B.3.h2-queue-cli
  PR #45, AI-P1-3) → 569 (INFRA-1B.4.h1-chunker-policy-gate PR #47,
  AI-P1-1) → 609 (INFRA-1B.5.h1-runbook-setup-hygiene PR #48, AI-P1-12)
  → 623 (INFRA-1B.3.h3-audit-hardening PR #49: +12 tests v8 audit
  hardening + +2 Codex P2 round 1 whitespace TRIM trigger, AI-P1-7)
  → 647 (OPS-1B.h1-runtime-invariant-scanner landed 2026-05-16:
  +24 tests in r2_invariant_scanner_test.ts for 3-axis reconciliation
  Snapshot↔policy↔audit — parseSnapIdFromRationale 5 + reconcile axes
  9 + SQLite fetchers 5 + Neo4j fetcher 2 + scanR2Invariants
  orchestrator 2 + combined 1, AI-P1-6)
  → 652 (INFRA-1B.3.h4-dedup-r2-backed-link-policy-fix landed
  2026-05-16: +5 tests in snapshot_fingerprint_test.ts cross-source
  archive_policy guard dedup-link path describe block — 3 archive_policy
  reject (metadata_only / excerpt_only / do_not_collect with
  allowed_public_data_only) + 1 raw_cloud_policy=always_prohibited
  preserve regression + 1 positive case, AI-P0-2 P0 legal-safety
  hotfix closing the dedup-link call site missed by PR #41)
  → 662 (OPS-1B.h2-r2-invariant-scanner-orphan-axis landed
  2026-05-16: +10 tests in r2_invariant_scanner_test.ts — 3
  fetchR2UploadOutcomeAuditRows (broadened SQL + malformed surfacing +
  backward-compat wrapper) + 3 Axis 4 r2_object_without_graph_key
  (set_r2_key_failed_neo4j orphan emit + recovery defensive emit +
  uploaded-no-emit) + 3 Axis 5 malformed_r2_upload_audit_row (uploaded
  malformed + set_r2_key_failed_neo4j malformed + rationale prefix
  truncation) + 1 orchestrator integration with ScanCounts new fields,
  AI-P1-13 P1 gate-blocker hotfix for PR #50 scanner blind spot)
  → 674 (INFRA-1B.1.h3-seed-sources-argv-allowlist landed
  2026-05-16: +12 tests in new file seed_sources_argv_test.ts — 5
  accept (empty + --dry-run + --neo4j + --preflight + all-3 combined)
  + 5 reject (--dryrun typo, --dry_run typo, --Neo4j capitalization,
  --dryrun mixed with --neo4j critical silent-write risk, positional
  arg) + 1 multi-unknown reporting + 1 KNOWN_FLAGS export shape,
  AI-P1-14 hygiene PR applying PR #45 parseArgs allowlist pattern to
  seed-sources CLI to close the silent-write risk of typoed
  `--dryrun --neo4j`)
  → 684 (INFRA-1B.3.h5-policy-decisions-snap-id-column-v9 landed
  2026-05-16: +10 tests = +2 in audit_policy_decisions_test.ts
  (snap_id column dual-write + attempted/outcome pair sharing snap_id)
  + +4 in r2_invariant_scanner_test.ts (v9 column preferred over
  rationale, legacy v8- rationale fallback, malformed legacy row
  surfaces Axis 5, mixed v8/v9 resolves via own path) + Codex PR #57
  P2 round 1 +4 validSnapIdOrNull guard (shape validator + empty-string
  column → rationale fallback + garbage column → rationale fallback +
  both anchors bad → Axis 5 surface preserved); AI-P1-15 v9 migration
  adds policy_decisions.snap_id TEXT column + partial INDEX + Codex
  PR #57 P1 schema_migrations record fix; closes AI-P1-13 follow-up
  where scanner depended on free-form rationale regex — structural
  column makes future format changes safe)
  → **700** (INFRA-1B.3.h6-policy-decisions-snap-id-schema-hardening
  Cycle 7 landed 2026-05-17: +16 tests = +8 in audit_policy_decisions
  _test.ts (assertValidSnapId shape validator × 4 [canonical / empty /
  missing prefix / invalid chars] + error-message JSON-stringify × 1 +
  recordR2UploadDecision writer-boundary integration × 3 [malformed →
  no INSERT / empty → reject / canonical → INSERT]) + +9 in new file
  migrate_v9_integration_test.ts (v9 fresh-apply column / partial
  index / schema_migrations row / post-v9 INSERT × 4 + re-apply
  duplicate-column [full SQL throws / migrate.ts recovery branch
  clean] × 2 + version ordering [description anchor + prior-version
  preserve] × 2 + 1 description-pattern lock); Cycle 7 closes the
  AI-P1-15 "first-class structured handle" contract by adding
  writer-boundary shape fail-fast at recordR2UploadDecision entry
  + migration path integration test that v8→v9 ALTER works + DATA_MODEL
  sync. Reader-side validSnapIdOrNull stays as defense-in-depth for
  legacy / out-of-band SQL writes).
- External CI owner: same repo (.github/workflows/)

## Before opening a PR

- `bun run typecheck`
- `bun test`
- `bun run invariant:check`
- update relevant docs if behavior/schema/runtime changed (AGENTS.md L46-60)

---

Rules:

- List only real commands once project has code.
- If no command exists, write "No command currently defined."
- Do not invent commands.
- If CI runs a command that cannot be run locally, explain where it runs and why.
- If a check is required for merge, name the exact workflow / job.
