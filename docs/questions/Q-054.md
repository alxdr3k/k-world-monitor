---
id: q-054
type: question
title: Source Registry canonical store 방향 — Neo4j Source node projection 확장 vs SQLite source_profile table 신설
status: open
created_at: 2026-05-15
updated_at: 2026-05-15
owner: user
proposed_answer: |
  본 Q 는 2026-05-15 GPT cross-review P1-2 (Source Registry 가 PRD/AC 요구
  핵심 필드 persist 안 함) 의 resolution 결정.

  현재 data/sources_seed.yaml 의 72 source 가 name / publisher / url / rss_url
  / api_base / access_method / reliability_tier / source_perspective /
  meta_category / subtopic_tags / collectability metadata 보유. 그러나
  seedSources() 가 SQLite 에 저장하는 것 = source_registry_slug_map +
  source_material_policy (3 policy 필드) 만. 나머지 metadata 는 validate 일부
  후 canonical store 에 0건 저장.

  PRD REQ-017 / AC-022 / AC-027 / AC-031 가 이 metadata 들 (collectability_score
  / access_method / source_perspective / subtopic_tags) 을 source quality
  control 의 핵심 invariant 로 정의. 그러나 runtime 에서 query 가능한 곳 없음.

  옵션:
  - (a) Neo4j Source node projection 확장 — seed-sources:neo4j command 가 YAML
    metadata 를 Neo4j Source 노드의 property 로 SET. graph query convenience.
    SQLite 는 policy 만.
  - (b) SQLite source_profile table 신설 — source_id PK + name / publisher /
    access_method / reliability_tier / source_perspective / meta_category /
    subtopic_tags_json / collectability_json / notes. Neo4j Source 는 graph
    traversal projection.

  Claude review 추천 = (b) — SQLite transaction 안에서 policy + profile 같이
  다루기 쉽고 (UPSERT 가 단일 transaction), audit / dashboard query 가 SQLite
  위에서 단순. Neo4j 는 graph traversal 용. (a) 는 cross-store mutation seam
  더 복잡.
blocks:
  - AI-P1-4
  - INFRA-1B.1.z-source-profile  # 신규 slice 진입 직전 lock

resolution: null

touches:
  - id: ADR-0017
    relation: extends_scope
    extended_to: [storage.sqlite.source_profile]
    parent_invariant: INV-0017
    statement: source_material_policy 외 source profile metadata canonical persistence path 결정 — Neo4j Source node vs SQLite source_profile table
  - id: ADR-0019
    relation: complies
    rationale: AC-027 source_perspective 분포 invariant 가 canonical runtime data 로 살아있어야 검증 가능

term_effects: []

invariant_review:
  status: pending
  reviewed_at: null
  fingerprint: null
unresolved_warnings: []
reviewed_terms:
  - source
  - source_perspective
  - collectability_score
  - access_method
reviewed_scopes:
  - storage.sqlite.source_profile
  - storage.neo4j.source_node
  - pipeline.discovery_layer.source_registry

provenance: user_confirmed
sensitivity: private
retention: long_term
ai_include: true
---

# Q-054: Source Registry canonical store 방향 결정

## Context

GPT cross-review P1-2 가 발견. seedSources() 가 SQLite 에 slug_map +
source_material_policy 만 INSERT — YAML 의 name / publisher / url / rss_url /
api_base / access_method / reliability_tier / source_perspective / meta_category
/ subtopic_tags / collectability / notes 는 모두 drop. PRD REQ-017 / AC-022 /
AC-027 / AC-031 가 의무화한 source quality control 필드가 runtime store 부재.

## Discussion

- (a) Neo4j Source projection 확장
- (b) SQLite source_profile table 신설

## Resolution

(pending operator decision)
