---
id: q-027
type: question
title: 백업 schedule + R2 lifecycle
status: resolved
created_at: 2026-05-11
updated_at: 2026-05-11
owner: user
proposed_answer: "Neo4j dump 일간 retention 30d / SQLite snapshot 일간 retention 90d / JSONL audit export 월별 retention 1y / R2 derived artifact 무기한 / open-license dataset versioned (snapshot date per fetch)"
blocks:
  - INFRA-1A.8
resolution: DEC-007

touches:
  - id: ADR-0014.section.backup
    relation: depends_on
    depended_refs:
      - INV-0014-6
    rationale: backup schedule이 lock되어야 backup runbook commit 가능

term_effects: []

invariant_review:
  status: pending
  reviewed_at: null
  fingerprint: null
unresolved_warnings: []
reviewed_terms: []
reviewed_scopes:
  - operations.backup.neo4j_dump
  - operations.audit.jsonl_export
  - storage.r2.permitted_artifacts

provenance: user_confirmed
sensitivity: private
retention: long_term
ai_include: true
---

# Q-027: 백업 schedule + R2 lifecycle

## Context

ADR-0014 INV-0014-6에서 백업 schedule을 명시했지만 retention과 R2 lifecycle은
Q-027로 별도 finalize. R24 자체 audit에서 신설.

## Discussion

권고:

| 백업 종류 | 주기 | retention | 비고 |
|---|---|---|---|
| Neo4j dump (`neo4j-admin database dump`) | 일간 | 30d | 단일 인스턴스 SPOF — local + R2 양쪽 보관 |
| SQLite snapshot | 일간 | 90d | relational metadata + run_ledger 보호 |
| JSONL audit export | 월별 | 1y | human-readable trail (ADR-0014 INV-0014-7) |
| R2 derived artifact (자체 차트/export) | 무기한 | — | versioned by publish date |
| R2 open-license dataset | versioned | snapshot date per fetch | dataset_vintage 테이블과 일관 (ADR-0011 R9/Q4) |

운영 비용:
- Neo4j dump 일간 + 30d retention = R2에 30 객체 × 평균 dump size
- SQLite snapshot 일간 + 90d = 90 객체. SQLite는 단일 파일이라 작음
- JSONL export 월별 + 12d = 12 객체/년

## Resolution

**Resolved by DEC-007** (2026-05-11) — research `docs/research/retention-
policy-2026-05.md` 권고 그대로 lock. R2 native lifecycle expire 3개
(backups/neo4j 30d, backups/sqlite 90d, tmp/multipart 7d) + transition
6개 + 의미적 GC application batch job 3개 (일/주/월) + soft-delete →
tombstone(14d grace) → hard-delete 2단계 + RETENTION_PROTECTED_KINDS 상수.
Q-A~Q-G micro 결정 7개 (cited Snapshot 영구 lock / metrics monthly rollup
/ JSONL IA 1y / dataset_vintage 무기한 IA / raw_cache ttl_ceiling default /
Tombstone 무기한 / R2 versioning OFF) 도 함께 lock. 구체 cron schedule
및 publishing site deployment 는 `docs/05_RUNBOOK.md` 에 commit.
