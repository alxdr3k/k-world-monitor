---
id: q-057
type: question
title: Claim promotion / semantic dedup 정책 — EXTR-1A.* 진입 직전 lock (promotion score / semantic claim key / claim_direction enum)
status: open
created_at: 2026-05-15
updated_at: 2026-05-15
owner: user
proposed_answer: |
  본 Q 는 2026-05-15 GPT cross-review 3부 (콘텐츠 성공 가능성) + 데이터 품질
  평가의 "claim bloat 방지" 권고 결정.

  GPT 분석: 뉴스 많이 가져오고 claim 많이 만들지만 promote / merge / rank 안
  되면 graph 가 쓰레기장처럼 커짐. 같은 사실이 여러 source 에 반복 →
  raw_body_hash dedup 으로는 막을 수 없음 (semantic 다른 wording 의 같은 사실
  → 다른 hash).

  EXTR-1A.* 진입 전 lock 필요 항목:

  1. **Promotion score** =
     - source reliability
     - source diversity
     - novelty
     - market/economic relevance
     - assumption impact
     - contradiction strength
     - time sensitivity
     - evidence quality
     - operator interest profile

  2. **Semantic claim key** — Claim extraction 시:
     - raw_claim_statement
     - normalized_claim_key
     - claim_subject
     - claim_predicate
     - claim_object
     - time_scope
     - geo_scope
     - metric_scope

  3. **Claim_direction enum** (Thesis stance 와 별도):
     - risk_increasing
     - risk_decreasing
     - opportunity_increasing
     - opportunity_decreasing
     - neutral_fact
     - ambiguous

  4. **Importance scoring** (confidence 와 분리):
     - importance_score
     - novelty_score
     - scenario_impact_score
     - time_sensitivity
     - affected_assumption_ids
     - market_relevance
     - policy_relevance

  옵션:
  - (a) EXTR-1A.0 (planned) 안에 claim promotion / semantic dedup 의무 추가
  - (b) 별도 신규 ADR 발급 (가칭 "Claim promotion + semantic dedup")
  - (c) Q 별도 분할 — promotion / dedup / direction / importance 각각 별도 Q

  Claude review 추천 = (b) — 새 invariant 다수 도입 (semantic claim key /
  claim_direction / importance scoring 모두 새 schema 측면) → DEC 가 아닌 ADR.
  EXTR-1A.1 진입 직전 lock 의무.
blocks:
  - EXTR-1A.1  # extractor router slice 진입 직전 lock

resolution: DEC-024

touches:
  - id: ADR-0025
    relation: extends_scope
    extended_to: [pipeline.extraction_layer.promotion, pipeline.extraction_layer.semantic_dedup]
    parent_invariant: INV-0025-1
    statement: ADR-0025 (10-stage object model, supersedes ADR-0011) 의 Claim layer 에 promotion score / semantic claim key / claim_direction / importance scoring 추가 — extractor 단계의 claim quality control. ADR-0011 은 ADR-0025 로 supersede 됐으므로 active scope anchor 는 ADR-0025 (Codex review 2026-05-15 inline #72 feedback).
  - id: ADR-0019
    relation: extends_scope
    extended_to: [pipeline.extraction_layer.bidirectional_balance]
    parent_invariant: INV-0019-1
    statement: claim_direction enum 이 Thesis stance 와 같은 layer 의 bidirectional framing — Claim-level polarity

term_effects: []

invariant_review:
  status: pending
  reviewed_at: null
  fingerprint: null
unresolved_warnings: []
reviewed_terms:
  - claim
  - promotion_score
  - semantic_claim_key
  - claim_direction
  - importance_score
reviewed_scopes:
  - pipeline.extraction_layer.claim_extraction
  - pipeline.extraction_layer.promotion

provenance: user_confirmed
sensitivity: private
retention: long_term
ai_include: true
---

# Q-057: Claim promotion / semantic dedup 정책 결정

## Context

GPT cross-review 3부 권고: extractor 붙이기 전 promotion policy + semantic
dedup + claim_direction + importance scoring 정의 없으면 claim bloat 발생.

## Discussion

- (a) EXTR-1A.0 안에 흡수
- (b) 신규 ADR 발급
- (c) Q 4 개로 분할

## Resolution

DEC-024 D6 defers this Q.

**Decision**: Option (b) — 신규 ADR (가칭 ADR-NNNN (다음 가용 ID) "Claim promotion, semantic dedup, claim direction") 로 lock. **EXTR-1A.1 진입 직전 accepted 상태 의무**.

본 PR (#40) 에서는 ADR 작성 안 함. Q-057 은 deferred 상태 + blocker 명시.

**Re-entry gate**: ADR-NNNN (다음 가용 ID) (가칭) 작성 + accepted, EXTR-1A.1 진입 직전.

ADR-NNNN (다음 가용 ID) (가칭) 필수 invariant:
- Claim 은 `raw_claim_statement` 보존
- `normalized_claim_key` 별도 필드
- `claim_direction` enum (Thesis stance 와 separate axis: risk_increasing / risk_decreasing / opportunity_increasing / opportunity_decreasing / neutral_fact / ambiguous)
- `confidence_score` vs `importance_score` 분리
- Dossier/Scenario promotion 은 `promotion_score` threshold 통과 의무
- semantic duplicate claim 은 별도 Claim node 안 만들고 evidence anchor 다수 attach pattern
