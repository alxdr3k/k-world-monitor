---
id: q-053
type: question
title: chunker raw text persistence 정책 — archive_policy gate + 신규 local_storage_policy enum 도입 여부
status: open
created_at: 2026-05-15
updated_at: 2026-05-15
owner: user
proposed_answer: |
  본 Q 는 2026-05-15 GPT cross-review (Latent P1) 가 발견한 chunker policy
  boundary gap 의 resolution 결정.

  현재 src/discovery/worker/chunker.ts 의 chunkSnapshot({ snapId, text }) 는
  sourceId / archivePolicy 입력 안 받음. raw text 를 Chunk.text Neo4j property
  에 저장. ADR-0012 INV-0012-3 의 raw text 저장 금지 invariant 는 R2 한정으로만
  코드 enforce — Neo4j 측에 적용 안 됨. caller 가 미리 정책 검사를 했다고 가정.
  v0 시점에는 caller 가 chunker 안 부르지만 (EXTR-1A 미land), EXTR-1A.* 진입 시
  caller-trust 가 silent break point.

  옵션:
  - (a) chunkSnapshot() 에 archivePolicy 의무 추가, archive_policy 기반 gate
    - metadata_only / do_not_collect → reject
    - excerpt_only → reject (또는 limit 까지 허용 결정)
    - full_snapshot_allowed → allow
    단순. 새 enum 안 도입.
  - (b) 신규 local_storage_policy enum 도입 — 3 층 정책 분리
    - cloud_storage_policy (= 기존 raw_cloud_policy)
    - local_storage_policy (신규)
    - external_llm_policy (기존)
    Source 별 "local 저장 OK 이지만 cloud 금지" 같은 세밀 분리 가능. v0 에서
    over-engineering 의심.

  Claude review 추천 = (a) — v0 의 archive_policy 4 enum 가 이미 의도된 layer.
  local vs cloud 분리는 향후 운영 신호 시 (b) 로 reflow.
blocks:
  - AI-P1-1  # 본 review action-items 의 P1-1
  - INFRA-1B.4.x-chunker-policy-gate  # 신규 slice 진입 직전 lock

resolution: null

touches:
  - id: ADR-0012
    relation: extends_scope
    extended_to: [storage.neo4j.chunk.raw_text_persistence]
    parent_invariant: INV-0012-3
    statement: ADR-0012 INV-0012-3 의 raw text 저장 금지를 R2 외 Neo4j 측까지 확장 — chunker Chunk.text property persistence boundary 에서 archive_policy 검사 의무
  - id: ADR-0017
    relation: complies
    rationale: source_material_policy.archive_policy enum 4 값 (metadata_only / excerpt_only / full_snapshot_allowed / do_not_collect) 을 chunker 가 검사 의무로 채택

term_effects: []

invariant_review:
  status: pending
  reviewed_at: null
  fingerprint: null
unresolved_warnings: []
reviewed_terms:
  - archive_policy
  - raw_cloud_policy
  - chunk
reviewed_scopes:
  - storage.neo4j.chunk
  - pipeline.discovery_layer.chunker
  - pipeline.policy.raw_text_persistence

provenance: user_confirmed
sensitivity: private
retention: long_term
ai_include: true
---

# Q-053: chunker raw text persistence 정책 결정

## Context

2026-05-15 GPT cross-review 가 Latent P1 으로 발견. Claude 12-layer review
의 L3 / L5 / L6 chunker analysis 가 boundary 자체 인식 못함.

src/discovery/worker/chunker.ts:
- chunkSnapshot({ snapId, text }) input — sourceId / archivePolicy 없음
- 500-word window 로 잘라 Chunk.text Neo4j property 에 저장
- ADR-0012 INV-0012-3 = "raw third-party text 클라우드 업로드 금지" — R2 한정
- archive_policy=metadata_only source 의 full body 가 chunker 까지 도달하면
  raw text 가 Neo4j 안에 저장 (policy spirit 위반)

## Discussion

- (a) archive_policy 4 enum 직접 사용 — v0 의도된 layer 활용
- (b) cloud / local / external_llm 3 층 정책 분리 — 세밀하지만 v0 over-engineering

## Resolution

(pending operator decision)
