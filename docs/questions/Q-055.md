---
id: q-055
type: question
title: Dataset ingestion MVP timing — M3 이전 dataset MVP (GPT 권고) vs ADR-0024 EXTR-1A.5 lock 유지
status: open
created_at: 2026-05-15
updated_at: 2026-05-15
owner: user
proposed_answer: |
  본 Q 는 2026-05-15 GPT cross-review 3부 (데이터 품질 평가) 의 권고 결정.

  GPT 가 RSS source 만으로는 세계경제 관측력이 부족하다고 평가:
  - macro dataset (FRED / World Bank / IMF / OECD / BIS / central bank)
  - market data (rates / FX / credit spreads / commodity curves)
  - policy docs (regulation PDFs / speeches / bill texts)
  - epidemic data (WHO / CDC / ECDC / local health agencies)
  - company/industry data (filings / earnings / shipping / logistics)
  - non-RSS official updates

  이건 ADR-0024 (Data Science Module — Polars + DuckDB + statsmodels + scipy
  deterministic dataset processing) 가 의식한 영역. SQLite schema 도
  dataset_vintage / derived_metric_ledger 를 갖고 있음. 그러나 implementation
  은 EXTR-1A.5 (P0-M3) 으로 lock.

  GPT 권고: M3 이전에 dataset ingestion MVP 1건 도입 — 단일 official macro
  dataset + vintage date + observation date + checksum + derived metric spec
  hash + metric → claim linkage. 뉴스 기반 claim 만으로 scenario 만들면 "그럴
  듯한 서사" 되기 쉬움. 숫자/시계열 기반 claim 들어가야 품질 ↑.

  옵션:
  - (a) ADR-0024 EXTR-1A.5 lock 유지 — P0-M3 진입 후 본격. 단계적 확장.
  - (b) M3 이전 dataset MVP — 1 dataset 만 (예 한국 부동산 지표 FRED 같은)
    P0-M6 (PUB-1A.5) MVP 와 동시 발행. ADR-0024 의 핵심 invariant (Polars +
    DuckDB stack lock / reproducibility 3-tuple) 그대로 유지 + scope 축소.

  Claude review 추천 = (a) 유지 — DEC-005/009/011 (한국 부동산 blog_long 1건
  v0) lock 이 이미 narrow. (b) 추가는 attention budget 초과. 단 PUB-1A.5 첫
  발행 retro 에서 "RSS only output 품질" 실측 후 v1+ dataset ingestion priority
  결정.
blocks:
  - EXTR-1A.5  # Data Science Module slice timing
  - PUB-1A.5  # MVP gate scope 결정

resolution: null

touches:
  - id: ADR-0024
    relation: complies
    rationale: ADR-0024 의 Polars + DuckDB + statsmodels + scipy stack lock 유지. timing 만 결정 (M3 이전 vs M3 안).
  # depends_on 의 depended_refs schema 가 [INV-### | ADR-####.section] 만 허용
  # 하므로 DEC-005 anchor 는 relation: complies (schema-free) 로 등록 후
  # statement 안 prose 명시 (Codex review 2026-05-15 inline #52 feedback).
  - id: DEC-005
    relation: complies
    statement: v0 turn-key MVP scope (blog_long only / 한국 부동산) 와 dataset MVP 의 trade-off — 옵션 (a) 유지 시 DEC-005 그대로 honor, 옵션 (b) 시 dataset scope 추가

term_effects: []

invariant_review:
  status: pending
  reviewed_at: null
  fingerprint: null
unresolved_warnings: []
reviewed_terms:
  - dataset_vintage
  - derived_metric_ledger
reviewed_scopes:
  - pipeline.extraction_layer.dataset
  - pipeline.extraction_layer.derived_metric_computer

provenance: user_confirmed
sensitivity: private
retention: long_term
ai_include: true
---

# Q-055: Dataset ingestion MVP timing 결정

## Context

GPT cross-review 3부 (데이터 품질) 권고: M3 이전 dataset MVP 도입.
ADR-0024 EXTR-1A.5 P0-M3 lock 유지 vs scope 축소 + 조기 도입 결정.

## Discussion

- (a) lock 유지 — narrow MVP, P0-M3 진입 후 본격
- (b) M3 이전 — 1 dataset MVP + PUB-1A.5 와 동시 발행

## Resolution

(pending operator decision; deferred to PUB-1A.5 retrospective)
