# 04 Implementation Plan

제품 gate, 기술 흐름, 구현 slice 상태를 한 곳에서 시퀀싱한다.

세부 tracking은 issue tracker가 맡고, 이 문서는 roadmap / status ledger의
canonical view만 유지한다. 구현 단계의 얇은 문서 레이어
(`docs/context/current-state.md`, `docs/current/`)에는 전체 roadmap inventory를
복제하지 않는다.

## Taxonomy

| Term | Meaning | Example ID | Notes |
|---|---|---|---|
| Milestone | 제품 / 사용자 관점의 delivery gate | `P0-M5` | "사용자가 어떤 상태를 얻는가"를 기준으로 정의 |
| Track | 기술 영역 또는 큰 구현 흐름 | `INFRA` | infra / extraction / pub 같은 영역 |
| Phase | track 안의 구현 단계 | `INFRA-1A` | 같은 track 안에서 순서가 있는 단계 |
| Slice | 커밋 가능한 구현/검증 단위 | `INFRA-1A.2` | PR / commit / issue와 연결 가능한 크기 |
| Gate | 검증 / acceptance 기준 | `AC-###` / `TEST-###` | `06_ACCEPTANCE_TESTS.md` 또는 테스트 위치로 연결 |
| Evidence | 완료를 뒷받침하는 근거 | PR, code, tests, current docs | 본문 복제 대신 링크 / ID로 남김 |

## Thin-doc boundary

- `docs/04_IMPLEMENTATION_PLAN.md`가 roadmap / status ledger의 canonical 위치다.
- `docs/context/current-state.md`는 현재 milestone / track / phase / slice만 짧게 요약한다.
- `docs/current/`는 구현된 상태를 빠르게 찾는 navigation layer다. 미래 roadmap,
  phase inventory, 상세 backlog를 복제하지 않는다.
- Slice는 하나의 검증 가능한 목표로 작게 유지한다. 인접 cleanup은 별도 slice로 나눈다.
- 알려진 범위는 `planned`, `deferred`, `blocked`로 기록할 수 있다. 단 dev-cycle
  실행 후보는 명시 승인 없이는 `ready`이고 blocker가 없어야 한다.
- Evidence는 code / test / PR / current doc 링크로 남기고, 구현 상세를 이 문서에
  길게 복사하지 않는다.
- 완료된 slice라도 runtime, schema, operation, test command가 바뀌면 해당
  `docs/current/` 문서를 함께 갱신한다.

## Unplanned feedback

User feedback from real usage is triaged before it enters the roadmap.

- Clear defects, UX regressions, or acceptance failures may become small hotfix slices.
- Broader product or architecture changes go through Q / DEC / PRD / roadmap updates.
- Keep detailed feedback threads in the issue tracker. Record only the actionable
  slice, gate, evidence, and next step here.
- Bug fixes should leave regression evidence when practical.

## Status vocabulary

Implementation status:

| Status | Meaning |
|---|---|
| `planned` | 계획됨. 아직 시작 조건이 충족되지 않음 |
| `ready` | 시작 가능. dependency와 scope가 충분히 정리됨 |
| `in_progress` | 구현 또는 문서 작업 진행 중 |
| `landed` | 코드 / 문서 변경이 반영됨 |
| `accepted` | gate를 통과했고 milestone 기준으로 수용됨 |
| `blocked` | blocker 때문에 진행 불가 |
| `deferred` | 의도적으로 뒤로 미룸 |
| `dropped` | 하지 않기로 함 |

Gate status:

| Status | Meaning |
|---|---|
| `defined` | 기준은 정의됐지만 아직 실행하지 않음 |
| `not_run` | 실행 대상이지만 아직 실행하지 않음 |
| `passing` | 통과 |
| `failing` | 실패 |
| `waived` | 명시적 사유로 면제 |

## Milestones

MVP milestone rule: 대상 사용자가 현실적인 환경에서 core workflow를 end-to-end로
직접 써볼 수 있는 milestone을 최소 하나 포함한다. 필요한 환경 준비는 숨은
전제조건이 아니라 ops / implementation slice로 추적한다.

| Milestone | Product / user gate | Target date | Status | Gate | Evidence | Notes |
|---|---|---|---|---|---|---|
| `P0-M1` | Schema & Bulk Store Bootstrap — 10-stage ADR(0011 superseded by 0025; 0012-0021) lock + Neo4j Cypher schema + SQLite relational + R2 permitted-artifact 적용 | TBD | landed (gate 검증 대기) | AC-001, AC-002, AC-003, AC-005, AC-006, AC-008, AC-022, AC-026, AC-032 | **M1 own slices**: INFRA-1A.1 ~ INFRA-1A.8 일괄 landed (migrations/neo4j/v1, **migrations/sqlite/v1~v2** [v1 core schema + v2 enum constraints — INFRA-1A.2 / 1A.5], src/{domain,storage,utils}/, data/sources_seed.yaml, R2 policy, backup runbook). SPIKE-001 (Neo4j FTS p95 <1s) + AC-032 R2 upload audit ledger (Q-044) 미실시 — gate accept 차단 | M1 slice 전부 landed (INFRA-1A.* only). gate 검증 단계. SPIKE-001 결과 + Q-044 R2 audit code enforcement 후 accepted. (INFRA-1B.1 / 1B.1.x 는 P0-M2 phase slice — M2 evidence 로 분리, 본 row 에서 제외. migrations/sqlite/v3 [source_registry_slug_map] 도 INFRA-1B.1.x 의 P0-M2 evidence — 본 row 제외) |
| `P0-M2` | Source Registry & Collection Queue — Tier A seed (size cap 없음, v0 entry 72 source) + collectability_score + source policy gate + discovery → 큐 → fingerprint snapshot → chunk 1건 end-to-end | TBD | landed (gate 검증 대기) | AC-001, AC-020 (P0 fallback only — raw_body_hash diff), AC-022, AC-023, AC-024 | **M2 own slices**: INFRA-1B.1 ~ INFRA-1B.5 일괄 landed (src/storage/source-registry/seed.ts, src/discovery/{fetch,parse,scheduler,worker}/, src/pipeline/access-intervention/, **migrations/sqlite/v3 + v5 + v6** [v3 source_registry_slug_map — INFRA-1B.1.x, v5 crawl_state — INFRA-1B.2b, v6 discovery_queue — INFRA-1B.2]). M2 게이트 검증 (AC-022/023/024 evidence 확정) 미실시 | M1 의존. M2 own slice 전부 landed. **AC-009 (extractor 분기) 는 P0-M3 gate** (EXTR-1A.* slices 미진입) — 본 row gate 에서 제외. **AC-020 은 P0-M2 gate 안에서 raw_body_hash fallback behavior 까지만 검증** (canonical_text_hash primary diff 는 INFRA-1B.9 P1+ 까지 미구현 — AC-020 본문의 P0/P1 phasing 참조). **Cross-milestone early landings (참고만, 본 row gate 평가 제외)**: INFRA-1B.6 (P0-M3 slice, manual feedback CLI) + OPS-1A.1 (P0-M3 slice, run ledger — 본 slice 의 migrations/sqlite/v4_run_ledger_completed_at_idx.sql 도 M3 evidence 로 분리, 본 row 제외) 가 M2 phase 안에서 early-landed — gate accept 는 각 milestone 행에서 별도 평가. P1-M2-hardening (1B.2.x/1B.3.x/1B.6.x) 은 P0-M6 accept 후 별도 진입 |
| `P0-M2-hardening` | **P0-M2 post-MVP hardening sub-phase** — INFRA-1B.* slices 가 일괄 landed 된 후 P0-M2 게이트 검증 단계 안에서 진행되는 hardening slices 의 묶음 (NOT a separate milestone — accept 는 P0-M2 게이트 accept 와 동일). Landed (code hardening, 16 slices, all registered in slice 표): `INFRA-1B.3.x-audit` (PR #39 R2 upload audit ledger AC-032 / NFR-008) + `INFRA-1B.3.h1-policy-fix` (PR #41 R2 cross-source archive_policy guard, AI-P0-1 legal-safety P0) + `INFRA-1B.1.h1-source-bootstrap-neo4j` (PR #44 Neo4j Source node bootstrap + 3-way preflight + `BootstrapPreflightError` fail-fast, AI-P1-2 — discovery → snapshot E2E blocker 해소) + `INFRA-1B.3.h2-queue-cli` (PR #45 `bun run discovery:process-queue` CLI + new-path `source_not_found_in_graph` TypedQueueError 통일 + `parseArgs` allowlist fail-fast, AI-P1-3 — operator E2E processing baseline) + `INFRA-1B.4.h1-chunker-policy-gate` (PR #47 chunker archive_policy gate + empty-text preserve + `ChunkRejected` 5 reason enum + fail-closed inverted allowlist, AI-P1-1 / Q-053 D2 / DEC-024 D2) + `INFRA-1B.5.h1-runbook-setup-hygiene` (PR #48 RUNBOOK fresh-worktree setup + pre-commit secret scanner — `scripts/check-secrets.ts` 2-layer 9 vendor pattern + `execFileSync` argv form + fail-closed + 256MB maxBuffer + `--no-renames` `--diff-filter=ACMT`, AI-P1-12 — operator safety baseline) + `INFRA-1B.3.h3-audit-hardening` (PR #49 v8 audit hardening — `policy_decisions.upload_attempt_id` 컬럼 + 3 BEFORE INSERT 트리거 [intended_action enum + r2_upload_decision enum + upload_attempt_id required-when-r2-upload with explicit whitespace charset TRIM], AI-P1-7 — BEFORE/AFTER audit pair correlation key) + `OPS-1B.h1-runtime-invariant-scanner` (PR #50 R2 invariant scanner — Snapshot.r2_key ↔ policy_decisions uploaded audit ↔ source_material_policy 3-way reconciliation, 3 violation axis: r2_key_without_audit / audit_uploaded_without_r2_key / r2_key_with_restricted_source, AI-P1-6 — INV-0012-3 audit-by-absence cross-check) + `INFRA-1B.3.h4-dedup-r2-backed-link-policy-fix` (PR #53 dedup-link path archive_policy guard hole — snapshot-fingerprint line 490 allowlist 강화 (양 axis 검사), AI-P0-2 legal-safety P0 — PR #41 sibling call site (dedup-link) 누락 hole 종결) + `OPS-1B.h2-r2-invariant-scanner-orphan-axis` (PR #54 scanner SQL `decision IN ('uploaded', 'set_r2_key_failed_neo4j')` 확장 + 2 신규 violation axis [`r2_object_without_graph_key` + `malformed_r2_upload_audit_row`], AI-P1-13 P1 gate-blocker — PR #50 의 set_r2_key_failed_neo4j orphan + defensive coding regression 종결) + `INFRA-1B.1.h3-seed-sources-argv-allowlist` (PR #56 seed-sources CLI `parseArgs` allowlist + `UnknownArgumentError` + `import.meta.main` entry guard, AI-P1-14 — `--dryrun --neo4j` typo silent-write risk 종결) + `INFRA-1B.3.h5-policy-decisions-snap-id-column-v9` (PR #57 v9 migration: `policy_decisions.snap_id TEXT` 컬럼 + partial INDEX + recordR2UploadDecision dual-write + scanner column-preferred + `validSnapIdOrNull` shape guard, AI-P1-15 + Codex round 1 P1 schema_migrations 누락 + P2 shape 미검증 fix — scanner 의 free-form rationale regex 의존 구조적 해소) + **`INFRA-1B.3.h6-policy-decisions-snap-id-schema-hardening` (PR #62 writer-boundary `assertValidSnapId` fail-fast at `recordR2UploadDecision` entry + v8→v9 migration integration test + DATA_MODEL.md v9 sync, Cycle 7 — post-#58 GPT review P1 finding 3건 [writer boundary / migration path / DATA_MODEL] 종결)** + `OPS-1B.h3-r2-orphan-axis-repairability` (PR #63 scanner SQL `decision IN ('uploaded', 'set_r2_key_failed_neo4j', 'skipped_toctou')` 확장 + Axis 4 cause-qualified rename `r2_object_without_graph_key_set_failed` + 신규 Axis 4b `r2_object_without_graph_key_policy_recheck_skipped` + 양 orphan axis details 에 `expectedR2Key` 출력 + ScanCounts.skippedToctouAuditRows + RUNTIME.md scanner section 신설 (Codex P1) + SHA placeholder 교체 (Codex P2) + 사용자 옵션 a/c 합산 (IMPL_PLAN/current-state sync + AGENTS.md 정책 강화), Cycle 8 — post-#58 GPT review Issue 3 종결) + `OPS-1B.h4-r2-audit-column-rationale-drift-axis` (PR #64 Cycle 9 scanner Axis 6 `r2_audit_column_rationale_drift` 신설 — column ↔ rationale dual-write contract violation surface; R2UploadOutcomeAuditRow shape 에 raw `columnSnapId` + `rationaleSnapId` field 노출하여 reconcile() 가 read-time mismatch detection; post-#58 GPT review 의 마지막 (Issue 6 P2) finding 종결) + **`INFRA-1B.3.h7-gate-evidence-hardening` (PR #66 merged 2026-05-17 (fdb847a) — Cycle 10, post-#65 GPT review Findings 1~5 hotfix bundle: scanner `parseSnapIdFromRationale` `(?=;|$)` delimiter strictness lookahead + snapshot-fingerprint skipped_toctou rationale `mergeMatchedExisting` branch attribution + thin-doc drift sweep [TESTING/RUNTIME/DATA_MODEL header SHA backfill + RUNTIME 6→7 axis 운영자 CLI flow 정정 + current-state/IMPL_PLAN 의 12→16 slices 갱신] + docstring `snap_<ULID>` over-claim 정정 + 7 invalid trailing char regression tests + 2 rationale branch attribution assertions; Finding 6 `INFRA-1A.x-shared-snapshot-id-constants` 은 planned anchor only)**. Landed (doc baseline, 5 PRs — sub-phase support, NOT code hardening): #42 (AI-P1-8 frontmatter YAML parse fix), #43 (AI-P1-9 `DOC-SYNC-2026-05-15`), `PR-canonical-register-2026-05-15` (PR #46 — AI-P0-1 + AI-P1-2 + AI-P1-3 canonical slice 표 row 일괄 등록), `PR-canonical-register-batch-2-2026-05-16` (PR #51 — AI-P1-1 + AI-P1-12 + AI-P1-7 + AI-P1-6 canonical slice 표 row 일괄 등록 + Milestones row landed list 갱신, 코드 변경 0건), **PR #58 `PR-canonical-register-batch-3-2026-05-16` (AI-P0-2 + AI-P1-13 + AI-P1-14 + AI-P1-15 canonical slice 표 row 일괄 등록 + Milestones row landed list 8 → 12 갱신 + doc baseline list 4 → 5, 코드 변경 0건)**. Planned (follow-up anchors, gate accept evidence 제외 unless operator-promoted): `INFRA-1B.10` (Article-Level Conditional Fetch + document_fetch_state v7), `INFRA-1B.1.h2-source-profile` (Source profile canonical store, AI-P1-4 / Q-054 D3 + Q-058 D7), **`INFRA-1B.1.h4-category-enum-validation` (AC-031 source-side enum reject — blocked/sequence-dependent on h2/`data/categories.yaml`, NOT gate-blocking unless operator-promoted; PR #59 registered as planned follow-up anchor only, does NOT reopen the 16-slice engineering sequence)**, **`INFRA-1A.x-shared-snapshot-id-constants` (snapshot ID shape regex + canonical prefix 의 shared constants module — 현재 `policy-decisions.ts` `SNAP_ID_SHAPE` + `scanner.ts` `SNAP_ID_RATIONALE_PREFIX` + `snapshot-fingerprint.ts` `permitted_artifact/derived/snapshot/${snapId}` 3 site 가 같은 invariant 를 독립적으로 magic-string 으로 보유하는 duplication. shared module 로 reuse 시 regex drift / canonical prefix drift 의 silent failure 면 차단; Cycle 10 / post-#65 GPT review Finding 6 anchor only, NOT gate-blocking)**, `DEPLOY-1A.0-migration-validation` (migrate dry-run validation, AI-P1-5), `INFRA-1A.9-validator-extension` (TRACE-041, AI-P2-5), `INFRA-1A.10-glossary-backfill` (AI-P2-5, operator D5 2026-05-18 defer-eligible filler), `INFRA-1A.9.h4-llm-routing-config-validator` (operator D3 2026-05-18 — landed via PR #93, pure config validator for ADR-0023 INV-0023-2/3/5; row registered in slice 표 below), `INFRA-1A.9.h5-vault-jsonl-policy-validator` (operator D4 2026-05-18 — landed via 본 PR, pure policy validator for ADR-0012 INV-0012-5/6; row registered in slice 표 below), `INFRA-1B.2.h1-discovery-hardening` (AI-P2-2/8), `INFRA-1A.h1-supply-chain-audit` (AI-P2-10), `INFRA-1B.2.h2-end-to-end-test` (AI-P2-9), **`INFRA-1B.5.h3-robots-disallow-ledger-coupling` (planned, NOT gate-blocking — 운영자 결정 E-1 옵션 2026-05-17 GPT review post-PR-#68)**: ADR-0028 safe-fetch already blocks robots.txt disallow via `RobotsDisallowedError` (`safe-fetch.ts` `isRobotsPathDisallowed()`). Remaining gap is **event-level persistence/coupling** into `policy_decisions` or `access_interventions`, deferred to this slice. Scope = safe-fetch `RobotsDisallowedError` 발생 시점에 (a) `policy_decisions` row 또는 (b) `access_intervention` Neo4j node 를 어떻게 기록할지 + decision_id / source_id / url / rationale schema 설계. **NOT a `RISK_TRIGGER` 9번째 enum 추가** (그건 ADR-0017 INV-0017-4 가 body §Decision 8 항목 canonical 이라 scope creep). **Non-normative note**: robots disallow is fail-closed at fetch boundary, but event-level persistence is deferred — do NOT treat `policy_decisions` absence as a complete robots-event audit trail (ADR-0012 R2 audit-by-absence analogy 는 enforcement 관점에만 적용, audit-trail 관점에는 부적용). ADR-0017 INV-0017-5 statement 는 그대로 유지 (robots 는 본 invariant 의 domain 밖, ADR-0028 fetch boundary event domain).** **AC-023 / TEST-023 evidence source-of-truth (landed)**: `INFRA-1B.5.h2-policy-gate-risk-triggers` (initial slice setup PR #67 doc-only AI-P1-11 lock, engineering implementation 본 PR — 53 tests pass). 본 신규 slice 는 17번째 code hardening landed entry — 위 16 historical landed list 와 함께 P0-M2-hardening engineering sequence 의 AC-023 closure path 완성. planned follow-up anchors (gate evidence 제외) 와 구분. | P0-M2 게이트 accept 와 동시 | in_progress (17 code hardening landed: #39 / #41 / #44 / #45 / #47 / #48 / #49 / #50 / #53 / #54 / #56 / #57 / #62 / #63 / #64 / #66 (Cycle 10 INFRA-1B.3.h7, fdb847a) / 본 PR (INFRA-1B.5.h2 AC-023 engineering, pending merge SHA) ; +5 doc baseline #42 / #43 / `PR-canonical-register-2026-05-15` / `PR-canonical-register-batch-2-2026-05-16` / `PR-canonical-register-batch-3-2026-05-16` ; 나머지 planned) | (P0-M2 row 와 동일 — independent gate 아님) | post-AI-P1-8 (PR #42) baseline 위에서 진행 | DEC-024 D8 attention-budget rule (P0/P1 only) 적용. 본 sub-phase 의 의의 = INFRA-1B.* MVP slices 가 landed 된 후 발견된 hardening 항목들의 묶음 anchor — P0-M2 gate accept 의 obligatory evidence 는 INFRA-1B.3.x-audit + INFRA-1B.3.h1-policy-fix + INFRA-1B.1.h1-source-bootstrap-neo4j (PR #44) + **INFRA-1B.3.h2-queue-cli (PR #45 — operator E2E processing baseline)** + AC-022/023/024 evidence 확정 + SPIKE-001 결과. 그 외는 P0-M2 gate 와 별도 trace 만. **"landed" 분리 의미**: code hardening = 실제 src/ + migrations/ + tests/ 변경으로 P0-M2 hardening 가설 (audit trail / cross-source policy / E2E executable / typed error unification) 을 충족; doc baseline = invariant validator + thin-doc 정합 + canonical ledger sync 회복으로 code hardening 의 후속 review 가능 상태 유지. 두 종류 모두 sub-phase 의 일부이나 gate accept 의 obligatory evidence 는 code hardening 만 보유. |
| `P0-M3` | Extraction & Review — Haiku 1차 + Sonnet escalate + auto-confirm + reviewer queue + manual feedback CLI + access_intervention batch report | TBD | planned | AC-007, AC-010, AC-015, AC-019, AC-024, AC-025, SPIKE-002 | **M3 own slices** (cross-milestone early-landed during M2 phase, 본 row gate accept 시점에는 추가 검증 불요): INFRA-1B.6 (manual feedback CLI, PR #22 7f4e980, 2026-05-12) + OPS-1A.1 (run ledger, PR #23 23de14c, 2026-05-12 — 본 slice 의 **migrations/sqlite/v4_run_ledger_completed_at_idx.sql** 도 M3 own evidence). 나머지 M3 slices (EXTR-1A.0 ~ 1A.6, OPS-1A.2 ~ 1A.4) 는 planned — AC-007/010/015/019 evidence 는 EXTR-1A.* 진입 시 추가 | M2 의존. status: planned (M3 main work = EXTR-1A.* + OPS-1A.2~4 미시작; INFRA-1B.6 + OPS-1A.1 early-landed 는 cross-milestone leaf — own status enum 평가 시 main work 기준). M3 own slice 의 나머지는 P0-M2 gate accept 후 진입. (이전 표기 `partially_landed (cross-milestone early)` 는 Status vocabulary 의 enum 외 — `planned` 토큰 + Notes 분리로 정정, codex review 2026-05-14 P2) |
| `P0-M4` | Search & Dossier — Neo4j native FTS 검색 + Dossier 합성 1건 (counterclaim pool, source_perspective 분포, evidence_role schema + minimum coverage) | TBD | planned | AC-002, AC-004, AC-027, AC-043, AC-044 (a)+(b) only | TBD | M3 의존. AC-043 = `:EVIDENCE_FOR` evidence_role enum + operator_lock schema lock (AGG-1A.1 자동 검증, DEC-020 Q-043). **AC-044 phasing**: M4 gate 는 AC-044 (a) supporting ≥3 / opposing ≥2 / monitoring ≥3 minimum coverage 자동 검증 + (b) `:EVIDENCE_FOR` evidence_role enum + operator_lock enforcement 까지만 cover (AGG-1A.1). **AC-044 (c) EvidencePack v0 4-section 생성 (REQ-023 + ADR-0027 INV-0027-3) 은 P0-M6 PUB-1A.2 영역** — M4 gate accept 시점에는 (c) 미구현 정상 (P0-M6 row 에서 별도 cover). M4 gate 가 M6 deliverable 에 의존하지 않게 phasing 분리 |
| `P0-M5` | Scenario Validate — assumptions/branches/falsifier/counterclaim(polarity-symmetric)/monitoring/impact_targets/transmission_channels + revisions ledger | TBD | planned | AC-012, AC-014, AC-017, AC-026 | TBD | M4 의존 |
| `P0-M6` | Thesis & Content & Cite Check — Thesis(stance + market_stance) → ContentDraft (v0 blog_long only, DEC-005) → Publication 1건 + cite check 5+1 + cascade + EvidencePack v0 4-section + **자체 사이트 Astro skeleton + 첫 발행** (ADR-0022, DEC-005, DEC-006) | **2주 목표 (DEC-005 lock)** | planned | AC-013, AC-016, AC-018, AC-026, AC-028, **AC-034**, **AC-035**, **AC-036, AC-037, AC-038, AC-039, AC-040, AC-041, AC-042 (Editorial Quality Rubric, DEC-012)**, **AC-044 (evidence_role minimum coverage, ADR-0027)** | TBD | M5 의존 — **v0 turn-key MVP gate (2주 목표 lock, DEC-005)**. AC-034 = 외부 cross-post canonical cite anchor lint (NFR-010 / REQ-027). AC-035 = Astro Content Collection Zod schema build-time gate (REQ-027 build-fail 룰 + editorial_intent_id / editorial_quality_rubric_passed). **AC-036~042 = Editorial Quality Rubric (DEC-012, PUB-1A.5 accept 시 운영자 manual verify 의무). AC-044 = Dossier evidence_role minimum coverage (ADR-0027, supporting ≥3 / opposing ≥2 / monitoring ≥3 + operator_lock)** |
| `P1-M2-hardening` | **Discovery pipeline post-MVP hardening** — multi-worker safety (worker_id CAS) + scheduler streaming (chunked Phase 1/2). v0 MVP gate(P0-M6) accept 후 진입. P0-M2 slice들의 v0 단순화(single-worker, allSettled all-in-memory)를 production-grade로 hardening | post-P0-M6 | planned | AC-001, AC-003 (extended) | INFRA-1B.2.x / INFRA-1B.3.x | M6 의존. Q-038, Q-039 resolution. |
| `P1-M3-hardening` | **Pipeline feedback post-MVP hardening** — AccessIntervention resolveIntervention의 multi-reviewer race (Cypher MATCH+SET) 닫음. v0 MVP gate accept 후 multi-reviewer / web UI 도입 시점 진입 | post-P0-M6 | planned | AC-025 (extended) | INFRA-1B.6.x | M6 의존. Q-037 resolution. |
| `P1-MVP-prep` | **Production deploy readiness** — v0→production schema migration framework (ALTER-only contract, backfill scripts, drift CI check) + timestamp 형식 CHECK constraints. **P0-M6(MVP gate) accept 후 + P1-M2/M3 hardening 슬라이스 안정화 후, production deploy 직전에 진입** (cloud 배포 자체는 별도 ops slice — 본 milestone은 deploy 가능성 확보까지) | post-(P0-M6 + P1-M2/M3 hardening), pre-production-deploy | planned | AC-032 (extended) | DEPLOY-1A.1 / DEPLOY-1A.2 | M6 turn-key MVP accept + post-M6 hardening 슬라이스 모두 안정화된 이후 진입. Q-040, Q-041 resolution. PR #26 codex review 컨텍스트 + PR #25 retro F-17. |

## Tracks

| Track | Purpose | Active phase | Status | Notes |
|---|---|---|---|---|
| `INFRA` | Neo4j + SQLite + R2 스키마 / Source registry / policy gate / access_interventions / queue / store / ID / ledger | `INFRA-1B` (INFRA-1A 전부 landed) | in_progress | M1/M2 owner — INFRA-1B 게이트 검증 단계 |
| `EXTR` | extractor (article/dataset/report) + LLM routing + review queue | `EXTR-1A` | planned | M3 owner |
| `AGG` | dossier / scenario(impact_targets) / scenario_revisions / validate / thesis(stance + market_stance) | `AGG-1A` | planned | M4-M5-M6 owner |
| `PUB` | content_draft 4-format(v0 blog_long only) / cite_check 5+1 / publication / cascade / **자체 사이트 Astro + Cloudflare Pages** / vault publications/ sync (git push trigger) | `PUB-1A` | planned | M6 owner — v0 turn-key |
| `OPS` | run ledger / cost throttling / stale worker / 백업 / metrics framework / policy learning | `OPS-1A` | in_progress | M3+ 횡단 (OPS-1A.1 run ledger landed, OPS-1A.2+ planned) |

## Phases / Slices

| Slice | Milestone | Track | Phase | Goal | Depends | Gate | Gate status | Status | Evidence | Next |
|---|---|---|---|---|---|---|---|---|---|---|
| `INFRA-1A.1` | P0-M1 | INFRA | INFRA-1A | **재작성 (Round 25 canonical)** — 9-stage 글로서리 + ADR-0011~0021 신규 + ADR-0003/0004/0007/0008 supersede + PRD/HLD/current-state 갱신 | — | AC-001, AC-005, AC-006, AC-008, AC-022, AC-026, AC-032 | defined | landed | docs/glossary/, docs/adr/0011-0021, supersede markers on 0003/0004/0007/0008 | INFRA-1A.2 |
| `INFRA-1A.2` | P0-M1 | INFRA | INFRA-1A | Neo4j Cypher schema v1 + SQLite relational schema v1 + 마이그레이션 commit (Source/Document/Snapshot/Claim/Edge/Run/AccessIntervention/ManualClaimEntry/Thesis 노드 + source_policy/policy_decisions/dataset_vintage/metrics_*/evaluation_*/policy_learning_*/research_session/raw_cache_items 테이블) | INFRA-1A.1, SPIKE-001 | AC-002, AC-005 | defined | landed | migrations/neo4j/v1_schema.cypher + migrations/sqlite/v1_schema.sql + src/storage/ + scripts/migrate.ts + src/domain/ids.ts + tests/lint/id_prefix_test.ts; Q-004/Q-020/Q-024 resolved; PR #4 merged 2026-05-12 | INFRA-1A.3 |
| `INFRA-1A.3` | P0-M1 | INFRA | INFRA-1A | R2 버킷 + permitted_artifact prefix 정책 + raw_cloud_policy=always_prohibited 강제 + sha256 round-trip 테스트 (open license dataset only) | INFRA-1A.1 | AC-003, AC-020, AC-032 | defined | landed | src/storage/r2/policy.ts (PERMITTED_PREFIXES 9, checkPermittedPrefix, sha256HexBuf); src/storage/r2/client.ts (Bun.S3Client wrapper); tests/unit/r2_policy_test.ts (32 tests); PR #14 merged 2026-05-12 | INFRA-1A.4 |
| `INFRA-1A.4` | P0-M1 | INFRA | INFRA-1A | Neo4j edge UNIQUE constraint (5 relation type) + frontmatter 관계 배열 lint (배열 발견 시 CI fail) | INFRA-1A.2 | AC-008 | defined | landed | tests/lint/no_frontmatter_relation_array_test.ts (9 tests); Neo4j edge UNIQUE constraints landed in INFRA-1A.2; PR #5 merged 2026-05-12 | INFRA-1A.5 |
| `INFRA-1A.5` | P0-M1 | INFRA | INFRA-1A | text normalization util + sha256 helper + quote_reason enum / storage_level enum migration | INFRA-1A.2 | AC-007 | defined | landed | src/utils/text.ts + hash.ts + enums.ts; migrations/sqlite/v2_enum_constraints.sql (enum triggers); tests/unit/text_hash_test.ts (49 tests); PR #8 merged 2026-05-12 | INFRA-1A.6 |
| `INFRA-1A.6` | P0-M1 | INFRA | INFRA-1A | Source registry seed Q21 — Tier A seed (size cap 없음, v0 entry `docs/research/source-seed-list-2026-05.md` 72 source) + source_perspective 분포 균형 (전체 seed 기준 risk ≤50% / opportunity ≥25% / neutral ≥15%) + collectability_score 초기치. canonical 위치 = 이 repo `data/sources_seed.yaml` (외부 repo 의존 X) | INFRA-1A.2, Q-021 resolution | AC-022, AC-027 | defined | landed | data/sources_seed.yaml (72 sources); tests/unit/perspective_distribution_test.ts (TEST-027, 5 assertions); Q-021 resolved; AC-027 distribution risk 19% / opportunity 29% / neutral 42% / mixed 10%; PR #12 merged 2026-05-12 | INFRA-1A.7 |
| `INFRA-1A.7` | P0-M1 | INFRA | INFRA-1A | Scenario / Thesis / Source schema에 v0 즉시 bidirectional 필드 추가 (impact_targets, impact_direction_by_target, transmission_channels, stance, market_stance optional, source_perspective) | INFRA-1A.2 | AC-026, AC-027 | defined | landed | migrations/neo4j/v1_schema.cypher (Scenario property schema + indexes); src/utils/enums.ts (THESIS_STANCE/THESIS_MARKET_STANCE/SOURCE_PERSPECTIVE); src/domain/nodes.ts; tests/unit/bidirectional_schema_test.ts (27 tests); PR #9 merged 2026-05-12 | INFRA-1A.8 |
| `INFRA-1A.8` | P0-M1 | INFRA | INFRA-1A | Backup runbook — Neo4j dump 일간 + SQLite snapshot 일간 + JSONL audit export 월별. `docs/05_RUNBOOK.md` 갱신 | INFRA-1A.2, Q-027 resolution | AC-032 | defined | landed | docs/05_RUNBOOK.md Data Operations 섹션 전면 업데이트 — backup schedule, R2 lifecycle rules, retention batch, RETENTION_PROTECTED_KINDS, soft-delete 2단계, 복구 절차; PR #10 merged 2026-05-12 | INFRA-1B.1 |
| `INFRA-1B.1` | P0-M2 | INFRA | INFRA-1B | Source Registry Bootstrap — seed 72 sources from data/sources_seed.yaml into SQLite source_material_policy (enum validation + src_<ULID> IDs + idempotent upsert). Neo4j Source node creation is INFRA-1B.2+. (Collection Queue 는 INFRA-1B.2 / 1B.2a / 1B.2b 로 분리, manual_intake CLI 는 INFRA-1B.6 으로 분리; **8 위험 행동 트리거는 신규 `INFRA-1B.5.h2-policy-gate-risk-triggers` 슬라이스로 분리 — AI-P1-11 결정 lock 2026-05-17, ADR-0017 INV-0017-4 의 List A canonical**) | INFRA-1A.2, INFRA-1A.6 | AC-001, AC-022, AC-023 | defined | landed | src/storage/source-registry/seed.ts (parse+validate+upsert, ON CONFLICT DO UPDATE); scripts/seed-sources.ts (CLI --dry-run); tests/unit/source_registry_test.ts (22 tests); PR #15 merged 2026-05-12 (c51b2ce). **AC-023 evidence 는 본 slice 가 아닌 `INFRA-1B.5.h2-policy-gate-risk-triggers` 에서 close (AI-P1-11 D2a 결정).** | INFRA-1B.1.x |
| `INFRA-1B.1.x` | P0-M2 | INFRA | INFRA-1B | **Hotfix slice** (아키텍처 리뷰 follow-up, ADR-0030/DEC-014/DEC-015): (1) `PRAGMA busy_timeout=5000` — getDb()에 추가 (DEC-014); (2) source_registry_slug_map을 migrations/sqlite/v3_source_registry_slug_map.sql로 이전 + seed.ts 인라인 DDL 제거 (DEC-015); (3) seed.ts URL 파싱 유효성 검사 추가 (ADR-0028 PRE-0028-2); (4) Neo4j 풀 설정 환경변수화 (DEC-016) | INFRA-1B.1 | AC-022 | defined | landed | 모두 c51b2ce(PR #15)에 포함: PRAGMA busy_timeout(connection.ts), v3_source_registry_slug_map.sql migration, seed.ts validateWebUrl(), neo4j/connection.ts NEO4J_MAX_POOL_SIZE/NEO4J_ACQ_TIMEOUT_MS | INFRA-1B.2 |
| `INFRA-1B.2a` | P0-M2 | INFRA | INFRA-1B | **Safe-Fetch 기반 (ADR-0028 선행 구현)**: src/discovery/fetch/safe-fetch.ts — SSRF 방어(DNS pre-resolve + 사설IP 거부), 리다이렉트 체인 호스트 검증(≤3홉), 바이트 상한(DEC-017), zip bomb 방어, robots.txt 캐시, Content-Type sniff + 실행파일 거부; src/discovery/parse/xml-safe.ts — fast-xml-parser + XXE 비활성화(DEC-018). 방어별 단위 테스트 포함 | INFRA-1B.1.x | AC-001 | defined | landed | src/discovery/fetch/safe-fetch.ts + src/discovery/parse/xml-safe.ts + tests/unit/safe_fetch_test.ts (117 tests) + tests/unit/xml_safe_test.ts; PR #16 merged 2026-05-12 (ed09aa5) | INFRA-1B.2b |
| `INFRA-1B.2b` | P0-M2 | INFRA | INFRA-1B | **Discovery 스케줄러 + crawl_state**: migrations/sqlite/v5_crawl_state.sql — (source_id PK, last_polled_at, last_etag, last_modified_header, last_status, consecutive_failures, next_eligible_at); 바운드 세마포어 풀(전역 8 / 호스트당 1, ADR-0030 INV-0030-1); fetch/write 분리 패턴(INV-0030-2); etag/Last-Modified 조건부 fetch; 연속 5회 실패 24h backoff | INFRA-1B.2a | AC-001 | defined | landed | src/discovery/scheduler/{semaphore,pool,crawl-state,scheduler}.ts + migrations/sqlite/v5_crawl_state.sql + tests/unit/{semaphore,pool,crawl_state}_test.ts; PR #17 merged 2026-05-12 (0eec962) | INFRA-1B.2 |
| `INFRA-1B.2` | P0-M2 | INFRA | INFRA-1B | Discovery worker v0 (RSS/sitemap 1종 + API 1종, Tier A 한정) → 큐 적재. safe-fetch + 스케줄러 완성 후 실제 소스 연결 | INFRA-1B.2b | AC-001 | defined | landed | src/discovery/worker/{rss-worker,run-discovery}.ts + migrations/sqlite/v6_discovery_queue.sql + tests/unit/rss_worker_test.ts; PR #18 merged 2026-05-12 (896ddf2) | INFRA-1B.3 |
| `INFRA-1B.3` | P0-M2 | INFRA | INFRA-1B | Fetcher → Snapshot fingerprint row + content_hash dedupe (R2 binary는 permitted artifact만) | INFRA-1B.2, INFRA-1A.3 | AC-003, AC-020 | defined | landed | src/discovery/worker/snapshot-fingerprint.ts + tests/unit/snapshot_fingerprint_test.ts; PR #19 merged 2026-05-12 (4dfa94f) | INFRA-1B.4 |
| `INFRA-1B.4` | P0-M2 | INFRA | INFRA-1B | Chunker / Neo4j native FTS 인덱서 — snapshot 텍스트 → chunk + Neo4j FTS 인덱스 | INFRA-1B.3, INFRA-1A.2 | AC-002 | defined | landed | src/discovery/worker/chunker.ts + tests/unit/chunker_test.ts; PR #20 merged 2026-05-12 (06c49d7) | INFRA-1B.5 |
| `INFRA-1B.5` | P0-M2 | INFRA | INFRA-1B | access_interventions Neo4j 노드 + severity deterministic 산정 + batch_report mode 구현 | INFRA-1A.2, INFRA-1B.1 | AC-024 | defined | landed | src/pipeline/access-intervention/severity.ts (computeSeverity, GateMode × importance_score × relatedAssumptionIds); src/pipeline/access-intervention/recorder.ts (recordIntervention, aci_ ULID, Source→:HAS_INTERVENTION→AccessIntervention); src/pipeline/access-intervention/batch-report.ts (generateBatchReport, hasBlockers flag); tests/unit/access_intervention_test.ts (26 tests); PR #21 merged 2026-05-12 (c3b19c4) | INFRA-1B.6 |
| `INFRA-1B.6` | P0-M3 | INFRA | INFRA-1B | Manual feedback CLI — `pipeline feedback add|bulk|link|from-report` + `pipeline intervention review <id>` 3-option | INFRA-1B.5 | AC-025 | defined | landed | src/pipeline/feedback/{manual-claim-entry,intervention-review}.ts + tests/unit/feedback_test.ts; PR #22 merged 2026-05-12 (7f4e980) | EXTR-1A.1 |
| `INFRA-1B.6.x` | P1-M3-hardening | INFRA | INFRA-1B | **Intervention concurrency hardening** (PR #25 retro F-08, Q-037) — `resolveIntervention`의 MATCH+SET race를 `apoc.lock.nodes` 또는 CAS pattern으로 닫는다. v0는 single-operator라 deferred했지만 multi-reviewer / web UI 도입 전 hardening 필수 | INFRA-1B.6, Q-037 resolution | AC-025 | defined | planned | TBD | INFRA-1B.3.x |
| `INFRA-1B.3.x` | P1-M2-hardening | INFRA | INFRA-1B | **Discovery multi-worker hardening** (PR #25 retro F-15, Q-038) — `discovery_queue.worker_id` 컬럼 + 모든 UPDATE WHERE에 `worker_id = $self` CAS 조건. heartbeat / markQueueItemDone TOCTOU window 닫음. v0 single-worker에선 무해 | INFRA-1B.3, Q-038 resolution | AC-003 | defined | planned | TBD | INFRA-1B.2.x |
| `INFRA-1B.3.x-audit` | P0-M2 hardening | INFRA | INFRA-1B | **R2 upload audit ledger enforcement** (Q-044 → DEC-020, TRACE-040, AC-032 / NFR-008) — ADR-0012 INV-0012-3 audit-trail enforcement. `src/storage/audit/policy-decisions.ts` 신규 + `IntendedAction` enum (`r2_upload`) + `R2UploadDecision` enum (`attempted` / `uploaded` / `skipped_toctou` / `set_r2_key_failed_neo4j`). `migrations/sqlite/v7_policy_decisions_intended_action.sql` (ADD COLUMN intended_action + idx). `src/discovery/worker/snapshot-fingerprint.ts` 의 r2Put 2 call site (dedup back-fill + new-path) 전후에 명시적 INSERT — `attempted` row BEFORE r2Put (network 실패 시에도 audit trail 보존) + outcome row AFTER r2Put + post-recheck. `blocked_by_policy` 는 runtime row 가 아닌 audit-by-absence (정책 차단 시 r2Put 미호출, audit row 부재로 증명). tests/unit/audit_policy_decisions_test.ts (16 tests) | INFRA-1B.3, Q-044 → DEC-020 | AC-032 (NFR-008 evidence) | defined | landed | migrations/sqlite/v7_policy_decisions_intended_action.sql; src/storage/audit/policy-decisions.ts (`recordR2UploadDecision`); src/utils/enums.ts (`INTENDED_ACTION` + `R2_UPLOAD_DECISION`); src/discovery/worker/snapshot-fingerprint.ts (2 call site audit hooks); tests/unit/audit_policy_decisions_test.ts (16 tests) + snapshot_fingerprint_test.ts setup 갱신 (policy_decisions table) | INFRA-1B.2.x |
| `INFRA-1B.3.h1-policy-fix` | P0-M2 hardening | INFRA | INFRA-1B | **R2 cross-source archive_policy guard** (AI-P0-1 legal-safety P0, GPT P0-1 finding, ADR-0012 INV-0012-3) — `allLinkedSourcesAllowRawCloud()` → `allLinkedSourcesAllowR2SnapshotUpload()` rename + SQL 에 `archive_policy` 추가 SELECT + predicate `archive_policy='full_snapshot_allowed' AND raw_cloud_policy='allowed_public_data_only'` 두 축 모두 강제. Pre-fix bug: cross-source dedup 시 `archive_policy=metadata_only / excerpt_only / do_not_collect` 인 Source 가 `raw_cloud_policy='allowed_public_data_only'` 만 만족하면 R2-backed Snapshot 에 link 가능 (INV-0012-3 위반). 4 call site (dedup 전후 + new-path 전후) 갱신. ADR-0012 INV-0012-3 enforcement layer text 갱신 | INFRA-1B.3.x-audit | AC-032 (legal-safety axis), NFR-008 | defined | landed | src/discovery/worker/snapshot-fingerprint.ts (allLinkedSourcesAllowR2SnapshotUpload rename + 4 call site + ADR-0012 INV-0012-3 enforcement layer 갱신); tests/unit/snapshot_fingerprint_test.ts (+6 regression tests `cross-source archive_policy guard (AI-P0-1)` describe block); docs/adr/0012-non-archival-storage-neo4j-sqlite-r2.md (enforcement layer 함수명 + archive_policy 검사 명시); PR #41 merged 2026-05-15 (327f4b2) | INFRA-1B.1.h1-source-bootstrap-neo4j |
| `INFRA-1B.1.h1-source-bootstrap-neo4j` | P0-M2 hardening | INFRA | INFRA-1B | **Neo4j Source node bootstrap + 3-way preflight** (AI-P1-2, GPT P1-1 + Claude L3-M) — `seed-sources` 후 Neo4j Source node MERGE bootstrap + SQLite policy / SQLite slug_map / Neo4j Source 3-way 집합 alignment 검증 + `BootstrapPreflightError` fail-fast. discovery → snapshot E2E blocker 해소 (snapshot-fingerprint 의 `MATCH (src:Source {source_id})` runtime guard 가 매 transaction 에서 rollback 되던 상태). Source node properties 의도적 최소: `source_id` (UNIQUE, v1 schema), `slug`, `name`, `bootstrap_at`, `updated_at` 5 필드만. 전체 source profile metadata 는 AI-P1-4 영역. Codex P1 fix: `loadBootstrapRowsFromSqlite()` 가 SQLite slug_map 전체 (historical row 포함) bootstrap. Codex P2 fix: preflight 가 null source_id (`neo4jNodesMissingSourceId`) + duplicate source_id (`neo4jDuplicateSourceIds[]`) 도 검출. `was_created` 는 OPTIONAL MATCH query-local 변수 (Source node property 미저장) | INFRA-1B.1, INFRA-1A.2 | AC-001, AC-022 | defined | landed | src/storage/source-registry/neo4j-bootstrap.ts (bootstrapNeo4jSourceNodes + loadBootstrapRowsFromSqlite + preflightSourceRegistry + assertSourceRegistryAligned + BootstrapPreflightError); scripts/seed-sources.ts (`--neo4j` / `--preflight` flag + closeDriver finally); package.json (`seed-sources:neo4j` + `seed-sources:preflight`); tests/unit/neo4j_bootstrap_test.ts (23 tests = 15 base + 4 Codex P1 full slug_map coverage + 4 Codex P2 null/duplicate detection); 05_RUNBOOK.md Source Registry Bootstrap / Preflight section 신규; PR #44 merged 2026-05-15 (861796a) | INFRA-1B.3.h2-queue-cli |
| `INFRA-1B.3.h2-queue-cli` | P0-M2 hardening | INFRA | INFRA-1B | **`discovery:process-queue` CLI + `source_not_found_in_graph` error_code 통일** (AI-P1-3) — `bun run discovery:process-queue` operator-facing CLI (`src/discovery/worker/run-process-queue.ts`) — pending discovery_queue row 를 claim → safeFetch → snapshot fingerprint → R2 conditional upload → mark done/error 까지 처리. `--dry-run` flag 로 pending count + per-source 분포 + stale 'processing' (>1h) preview. SQLite-backed `makeArchivePolicyLookup` (FK-protected, missing-row actionable diagnostics). `parseArgs` allowlist + `UnknownArgumentError` fail-fast (Codex P2 — `--dryrun` typo / positional 거부). `closeDriver()` + `closeDb()` finally + `import.meta.main` entry guard. snapshot-fingerprint.ts new-path `createDocumentAndSnapshot()` Source-missing guard 가 `TypedQueueError('source_not_found_in_graph')` 발행 (이전 plain Error → `runtime_error` bucket) — dedup-path `ensureSourceLinkage()` 와 동일 code 통일, operator alert 단일 code cover | INFRA-1B.3, INFRA-1B.1.h1-source-bootstrap-neo4j | AC-001, AC-023 | defined | landed | src/discovery/worker/run-process-queue.ts (CLI entry + makeArchivePolicyLookup + pendingSnapshot + parseArgs + UnknownArgumentError); src/discovery/worker/snapshot-fingerprint.ts (new-path TypedQueueError 통일); package.json (`discovery:process-queue` + `:dry-run`); tests/unit/run_process_queue_test.ts (16 tests = 9 base + 7 Codex P2 parseArgs regression); tests/unit/snapshot_fingerprint_test.ts (+1 new-path Source-missing TypedQueueError regression); 05_RUNBOOK.md Queue Processing subsection (per-row 처리 단계 + 6 error_code remediation table); PR #45 merged 2026-05-15 (090ca5b) | INFRA-1B.4.h1-chunker-policy-gate |
| `INFRA-1B.4.h1-chunker-policy-gate` | P0-M2 hardening | INFRA | INFRA-1B | **Chunker archive_policy gate + empty-text preserve** (AI-P1-1 / Q-053 D2, DEC-024 D2) — `ChunkInput` 에 mandatory `sourceId` + `archivePolicy` 필드 추가. `ChunkRejected` typed error 에 5 reason enum (`metadata_only` / `excerpt_only` / `do_not_collect` / `unknown_archive_policy` / `empty_text`). 2-gate fail-fast BEFORE Neo4j transaction: (1) archive_policy gate — `if (archivePolicy !== 'full_snapshot_allowed') throw ChunkRejected(...)` — **Codex P1 fix: inverted from deny-list (`if (=== metadata_only / excerpt_only / do_not_collect)`) 로 unknown 값 fail-closed** (`unknown_archive_policy` reason 신규); (2) empty-text gate — throw 로 변경 (이전 contract: silent DETACH DELETE 로 기존 chunk 전체 wipe 가능 — 본 fix 로 snapshot revisit 시 ingestion 일시 fail 이어도 dossier evidence pack 보존 보장). 본 slice 가 INFRA-1B.4 chunker 의 P0-M2 hardening 형태로 access_intervention runtime baseline 강화 | INFRA-1B.4, INFRA-1B.3.h2-queue-cli, Q-053 D2 → DEC-024 D2 | AC-024 | defined | landed | src/discovery/worker/chunker.ts (ChunkInput mandatory sourceId + archivePolicy + ChunkRejected 5 reason enum + 2-gate fail-fast BEFORE Neo4j tx + empty-text throw contract); tests/unit/chunker_test.ts (refactored — 5 reason gate path + empty-text preserve regression); PR #47 merged 2026-05-15 (b09772a) | INFRA-1B.5.h1-runbook-setup-hygiene |
| `INFRA-1B.5.h1-runbook-setup-hygiene` | P0-M2 hardening | INFRA | INFRA-1B | **RUNBOOK fresh-worktree setup + pre-commit secret scanner** (AI-P1-12) — operator-facing safety baseline 2축: (1) **fresh worktree setup** — RUNBOOK 에 신규 worktree 부트스트랩 절차 (env load → schema migrate → seed sources → preflight → process-queue dry-run) 단일 entry point. (2) **pre-commit secret scanner** — `scripts/check-secrets.ts` (pure `scanForSecrets(files)` 2-layer defense: filename pattern + 9 vendor regex 본문 스캔) + `scripts/git-hooks/pre-commit` shim + `scripts/install-git-hooks.sh` (`core.hooksPath` 설정). Codex 6 round review fix 적용: **shell injection** (`execSync` → `execFileSync(file, [...args])` argv form, NO shell interpolation); **fail-closed read error** (try/catch 제거 → throw); **rename + type-change bypass** (`--diff-filter=ACM` → `ACMT` + `--no-renames`); **NUL-separated path parsing** (`-z` 출력 명시); **AWS STS ASIA missed** (`AKIA` 단독 → `(?:AKIA / ASIA)[0-9A-Z]{16}` union); **ENOBUFS bypass** (`maxBuffer` 1MB default → `MAX_BUFFER_BYTES = 256 * 1024 * 1024` const). GitHub token revoke 절차 = web-UI-only 명시 (`/authorizations/{id}` 2020-11 deprecated, `/user/personal-access-tokens/{id}` 미존재 — 서버측 revoke 분리, local credential cleanup = `printf protocol=https host=github.com newline newline / git credential reject`) | INFRA-1B.3.h2-queue-cli | (operator safety baseline — gate-blocking AC 없음, P0-M2 gate accept 의 supporting evidence) | defined | landed | scripts/check-secrets.ts (pure scanForSecrets + 9 vendor pattern + parseNulSeparatedPaths + STAGED_FILE_DIFF_ARGS + execFileSync argv form + 256MB MAX_BUFFER_BYTES + fail-closed); scripts/git-hooks/pre-commit (shim); scripts/install-git-hooks.sh (`core.hooksPath` 설정); package.json (`hooks:install` + `check-secrets`); docs/05_RUNBOOK.md (Fresh worktree setup + GitHub access token revoke + Pre-commit hook env scrub 3 section 신규); tests/unit/check_secrets_test.ts (40 tests — 9 vendor pattern + filename pattern + NUL parsing + redaction first4...last4 + fail-closed regression); PR #48 merged 2026-05-15 (88d542d) | INFRA-1B.3.h3-audit-hardening |
| `INFRA-1B.3.h3-audit-hardening` | P0-M2 hardening | INFRA | INFRA-1B | **v8 audit hardening — enum triggers + upload_attempt_id correlation** (AI-P1-7) — `migrations/sqlite/v8_audit_hardening.sql` 신규: (a) `policy_decisions.upload_attempt_id` TEXT 컬럼 + partial INDEX (`WHERE upload_attempt_id IS NOT NULL`); (b) `intended_action` enum BEFORE INSERT trigger (`r2_upload` 외 거부); (c) `r2_upload_decision` enum BEFORE INSERT trigger (`attempted` / `uploaded` / `skipped_toctou` / `set_r2_key_failed_neo4j` 외 거부); (d) `upload_attempt_id` required-when-r2-upload trigger — **Codex P2 round 1 fix: SQLite default `TRIM(x)` 는 ASCII space (0x20) 만 strip → tab/newline/CR 통과 bug. 명시 charset (space + char(9) + char(10) + char(13)) 사용한 TRIM(x, charset) = '' 로 교정**. `src/storage/audit/policy-decisions.ts` 에 `newUploadAttemptId()` ULID-based `uatt_<ULID>` + `R2UploadAuditInput.uploadAttemptId: string` mandatory + INSERT 9 컬럼 확장. `src/discovery/worker/snapshot-fingerprint.ts` 의 r2Put 2 call site (dedup back-fill + new path) 가 동일 `upload_attempt_id` 를 BEFORE/AFTER audit pair 에 thread (correlation key 로 attempted ↔ outcome 매칭, INFRA-1B.3.x-audit 의 audit pair 가 강제 correlation key 보유) | INFRA-1B.3.x-audit, INFRA-1B.5.h1-runbook-setup-hygiene | AC-032 (NFR-008 BEFORE/AFTER pair correlation 강화 evidence) | defined | landed | migrations/sqlite/v8_audit_hardening.sql (upload_attempt_id 컬럼 + partial INDEX + 3 BEFORE INSERT trigger: intended_action enum / r2_upload_decision enum / upload_attempt_id required-when-r2_upload with explicit whitespace charset TRIM); scripts/migrate.ts (v8 등록); src/storage/audit/policy-decisions.ts (newUploadAttemptId ULID + mandatory uploadAttemptId field + 9-col INSERT); src/discovery/worker/snapshot-fingerprint.ts (2 call site BEFORE/AFTER pair thread upload_attempt_id); tests/unit/audit_policy_decisions_test.ts (+12 v8 audit hardening tests + 2 Codex P2 round 1 whitespace TRIM trigger regression); tests/unit/snapshot_fingerprint_test.ts (audit pair correlation regression 갱신); PR #49 merged 2026-05-15 (cc69261) | OPS-1B.h1-runtime-invariant-scanner |
| `OPS-1B.h1-runtime-invariant-scanner` | P0-M2 hardening | OPS | OPS-1B | **R2 invariant scanner — Snapshot ↔ policy_decisions ↔ source_material_policy 3-way reconciliation** (AI-P1-6) — `src/ops/r2-invariant-scanner.ts` 신규 — read-only 3-way invariant 검증 (Neo4j `Snapshot.r2_key` ↔ SQLite `policy_decisions` uploaded audit row ↔ SQLite `source_material_policy`). 3 violation 축: (1) `r2_key_without_audit` — Snapshot 에 r2_key 가 있으나 대응하는 `policy_decisions(intended_action=r2_upload, r2_upload_decision=uploaded)` row 부재 (audit trail 누락); (2) `audit_uploaded_without_r2_key` — uploaded audit row 존재하나 Snapshot.r2_key 부재 (Neo4j set 실패 또는 cleanup race); (3) `r2_key_with_restricted_source` — Snapshot.r2_key 가 있으나 link 된 Source 중 `archive_policy != 'full_snapshot_allowed'` 또는 `raw_cloud_policy != 'allowed_public_data_only'` 1개 이상 존재 (INV-0012-3 위반 — INFRA-1B.3.h1-policy-fix 의 cross-source archive_policy guard 가 runtime 에서 enforced 됨을 audit-by-absence 로 cross-check). 분리 fetcher: `fetchR2BackedSnapshots()` (Neo4j) / `fetchUploadedAuditRows()` (SQLite policy_decisions) / `fetchSourcePolicies()` (SQLite source_material_policy). `parseSnapIdFromRationale()` position-0 anchor regex (rationale prefix `snap_<ULID>:` 로부터 snap_id 추출). Pure `reconcile()` 함수 (testable, side-effect free). `src/ops/run-r2-invariants.ts` CLI (`bun run audit:r2-invariants` + `--json`) — argv allowlist + `UnknownArgumentError` fail-fast | INFRA-1B.3.h3-audit-hardening, INFRA-1B.3.h1-policy-fix | AC-032 (NFR-008 audit-by-absence cross-check evidence) | defined | landed | src/ops/r2-invariant-scanner.ts (parseSnapIdFromRationale + reconcile pure function + fetchR2BackedSnapshots + fetchUploadedAuditRows + fetchSourcePolicies + scanR2Invariants orchestrator + 3 violation axis); src/ops/run-r2-invariants.ts (CLI entry + --json + parseArgs allowlist + UnknownArgumentError); package.json (`audit:r2-invariants` + `:json`); tests/unit/r2_invariant_scanner_test.ts (24 tests = 5 parseSnapIdFromRationale + 9 reconcile axes + 5 SQLite fetchers + 2 Neo4j fetcher + 2 scanR2Invariants orchestrator + 1 combined); docs/05_RUNBOOK.md R2 Invariant Scan section 신규; docs/current/CODE_MAP.md (`src/ops/` + new test 행); PR #50 merged 2026-05-15 (6500651) | INFRA-1B.3.h4-dedup-r2-backed-link-policy-fix |
| `INFRA-1B.3.h4-dedup-r2-backed-link-policy-fix` | P0-M2 hardening | INFRA | INFRA-1B | **R2-backed Snapshot dedup-link archive_policy guard** (AI-P0-2 legal-safety P0, post-PR #51 static review finding) — PR #41 (AI-P0-1) 가 `allLinkedSourcesAllowR2SnapshotUpload()` 의 back-fill + new-path call site 4개를 cover 했으나, **dedup-link path 의 inline guard (snapshot-fingerprint.ts line 490) 는 별도 narrower check** 로 PR #41 가 미수정. Pre-fix bug: `existing.r2Key !== null` 이고 `input.rawCloudPolicy === 'always_prohibited'` 만 검사 → `archive_policy ∈ {metadata_only, excerpt_only, do_not_collect}` + `raw_cloud_policy = allowed_public_data_only` 조합은 통과 → `ensureSourceLinkage()` 가 restricted Source 와 R2-backed Snapshot 사이에 edge 생성 → INV-0012-3 sibling-site 위반. Fix: allowlist guard 로 양 axis 모두 강제 (`archivePolicy !== 'full_snapshot_allowed' \|\| rawCloudPolicy !== 'allowed_public_data_only' → throw dedup_prohibited_source`). PR #47 lesson (allowlist > deny-list for enum gates) 적용 | INFRA-1B.3.h1-policy-fix, OPS-1B.h1-runtime-invariant-scanner | AC-032 (legal-safety axis), NFR-008 | defined | landed | src/discovery/worker/snapshot-fingerprint.ts (line 490 dedup-link guard allowlist 확장 + 보강 주석); tests/unit/snapshot_fingerprint_test.ts (+5 신규 "cross-source archive_policy guard, dedup-link path (AI-P0-2)" describe — 4 reject [metadata_only / excerpt_only / do_not_collect / always_prohibited preserve] + 1 positive ensureSourceLinkage MERGE via tx 확인); PR #53 merged 2026-05-16 (2f6ce43) | OPS-1B.h2-r2-invariant-scanner-orphan-axis |
| `OPS-1B.h2-r2-invariant-scanner-orphan-axis` | P0-M2 hardening | OPS | OPS-1B | **R2 invariant scanner orphan-axis 확장** (AI-P1-13 P1 gate-blocker, post-PR #51 static review finding) — PR #50 (AI-P1-6) scanner blind spot 종결. Pre-fix bug: `fetchUploadedAuditRows()` 가 `decision = 'uploaded'` 만 fetch, 그러나 snapshot-fingerprint.ts line 624/798 의 ternary `decision: setSucceeded ? 'uploaded' : 'set_r2_key_failed_neo4j'` 로 mutually-exclusive outcome → `set_r2_key_failed_neo4j` 경로 (r2Put 성공 + Neo4j SET 실패 → R2 object 존재 + Snapshot.r2_key NULL) 가 fetch 양쪽 모두 입력에서 제외되어 3 axis 어느 것도 발화 안 함. Fix path = option B (scanner 변경, audit lifecycle 보존 — PR #49 round 3 4-lifecycle-value lock 유지): SQL 을 `decision IN ('uploaded', 'set_r2_key_failed_neo4j')` 로 확장 + 2 신규 axis. **Axis 4 `r2_object_without_graph_key`**: well-formed set_r2_key_failed_neo4j row → violation (audit row 존재 자체가 orphan 증거; skipped_toctou 는 operator decision text 따라 narrow scope 로 제외, 별도 axis 후보 보류). **Axis 5 `malformed_r2_upload_audit_row`**: parseSnapIdFromRationale null → violation 으로 surface (defensive coding: read-only scanner 는 anomaly 를 throw 가 아닌 violation 으로 노출). `details.rationalePrefix` 120 char 트렁케이트 | OPS-1B.h1-runtime-invariant-scanner, INFRA-1B.3.h4-dedup-r2-backed-link-policy-fix | AC-032 (NFR-008 audit-by-absence cross-check 확장) | defined | landed | src/ops/r2-invariant-scanner.ts (fetchR2UploadOutcomeAuditRows 신규 fetcher + UploadedAuditRow backward-compat type alias + R2UploadOutcomeAuditRow + R2UploadOutcomeDecision + reconcile Axis 4/5 분기 + ScanCounts additive 확장 + scanner top-level 5-axis docstring rewrite); src/ops/run-r2-invariants.ts (CLI formatter 2 신규 violation type + Axis 2 docstring 정정); tests/unit/r2_invariant_scanner_test.ts (+10 신규 tests = 3 fetchR2UploadOutcomeAuditRows broadened SQL + malformed surfacing + backward-compat wrapper + 3 Axis 4 + 3 Axis 5 + 1 orchestrator integration); 기존 24 scanner tests 유지 (sed 로 audit row literal에 decision 필드 추가 + ReconcileInput 필드 rename `r2UploadOutcomeAuditRows`); PR #54 merged 2026-05-16 (f081e50) | INFRA-1B.1.h3-seed-sources-argv-allowlist |
| `INFRA-1B.1.h3-seed-sources-argv-allowlist` | P0-M2 hardening | INFRA | INFRA-1B | **seed-sources CLI argv allowlist + import.meta.main entry guard** (AI-P1-14, operator CLI safety) — PR #45 (AI-P1-3, run-process-queue) parseArgs 패턴을 scripts/seed-sources.ts 에 동일 적용. Pre-fix bug: `process.argv.includes(...)` 패턴은 `--dryrun` (no-hyphen typo) / `--dry_run` (underscore) / `--Neo4j` (capitalization) 같은 typo 를 silently 무시 → `--neo4j` 가 real graph write trigger 인 상황에서 typoed `--dryrun --neo4j` 가 silent SQLite + Neo4j mutation 으로 진행. Fix: exported `parseArgs(argv): ParsedArgs{dryRun, neo4j, preflight}` + `KNOWN_FLAGS` set + `UnknownArgumentError` (offending tokens + known-flag list + USAGE_LINE) + `if (import.meta.main) run()` entry guard (tests/ import 시 run() 부수효과 차단) | INFRA-1B.1.h1-source-bootstrap-neo4j, OPS-1B.h2-r2-invariant-scanner-orphan-axis | (operator CLI safety baseline — gate-blocking AC 없음, P0-M2 gate accept 의 supporting evidence) | defined | landed | scripts/seed-sources.ts (parseArgs / KNOWN_FLAGS / USAGE_LINE / UnknownArgumentError / ParsedArgs 추가 + 3 argv.includes() → 1 parseArgs destructure + import.meta.main entry guard); tests/unit/seed_sources_argv_test.ts (12 tests = 5 accept [empty / --dry-run / --neo4j / --preflight / all-3 combined] + 5 reject [--dryrun typo / --dry_run typo / --Neo4j capitalization / --dryrun mixed with --neo4j silent-write critical scenario / positional arg] + 1 multi-unknown reporting + 1 KNOWN_FLAGS export shape); PR #56 merged 2026-05-16 (01f029f) | INFRA-1B.3.h5-policy-decisions-snap-id-column-v9 |
| `INFRA-1B.3.h5-policy-decisions-snap-id-column-v9` | P0-M2 hardening | INFRA | INFRA-1B | **v9 migration: policy_decisions.snap_id column + scanner column-preferred read** (AI-P1-15, audit schema structural improvement) — AI-P1-13 가 노출한 구조적 약점 종결: scanner 가 free-form rationale regex 에 의존하던 부분 (parseSnapIdFromRationale, Axis 5 의 surface 자원) 을 first-class column 으로 교체. v9 migration: `ALTER TABLE policy_decisions ADD COLUMN snap_id TEXT` + partial INDEX (`WHERE snap_id IS NOT NULL`). recordR2UploadDecision dual-write (column + rationale prefix — v8- 레거시 consumer 호환). scanner `r.snap_id ?? parseSnapIdFromRationale(r.rationale)` 로 column-preferred + rationale fallback. **Codex PR #57 round 1 fix**: (P1) v9 SQL 끝에 `INSERT OR IGNORE INTO schema_migrations` 추가 — 누락 시 migrate.ts duplicate-column recovery 매번 fire + state tracking 부정확; (P2) `validSnapIdOrNull()` shape guard 도입 — `??` 가 empty/garbage column 통과시켜 Axis 2/4 mis-classification → `validSnapIdOrNull(column) ?? parseSnapIdFromRationale(rationale)` 로 강화 (malformed column → rationale fallback, both malformed → Axis 5 defensive surface 보존) | INFRA-1B.3.h3-audit-hardening, INFRA-1B.1.h3-seed-sources-argv-allowlist | AC-032 (NFR-008 audit schema structural — rationale regex 의존 종결) | defined | landed | migrations/sqlite/v9_policy_decisions_snap_id.sql (ALTER COLUMN snap_id + partial INDEX + schema_migrations record); scripts/migrate.ts (v9 등록); src/storage/audit/policy-decisions.ts (recordR2UploadDecision dual-write); src/ops/r2-invariant-scanner.ts (validSnapIdOrNull + SNAP_ID_SHAPE + column-preferred resolution); tests/unit/audit_policy_decisions_test.ts (+2 v9 dual-write tests); tests/unit/r2_invariant_scanner_test.ts (+8 v9 + P2 tests = 4 column/rationale resolution + 4 P2 validSnapIdOrNull shape + empty + garbage + both-bad surface); tests/unit/snapshot_fingerprint_test.ts (snap_id 컬럼 schema 추가); PR #57 merged 2026-05-16 (cdd1faf, Codex 2 round review) | INFRA-1B.3.h6-policy-decisions-snap-id-schema-hardening |
| `INFRA-1B.3.h6-policy-decisions-snap-id-schema-hardening` | P0-M2 hardening | INFRA | INFRA-1B | **v9 audit schema contract-level hardening** (Cycle 7, post-#58 GPT review P1 finding 3건 종결) — AI-P1-15 (PR #57) 의 v9 column 도입 후 GPT 정적 review 가 발견한 contract-level gap 3건 처리: (1) **writer-boundary shape validation 부재** — `recordR2UploadDecision` 가 TypeScript `string` type 만 신뢰, scanner-side `validSnapIdOrNull` 는 post-facto defense → exported `assertValidSnapId(value)` 도입 + INSERT 진입 직전 fail-fast (canonical `^snap_<chars>$` regex 검사). (2) **v8→v9 migration integration test 부재** — fixture table 이 snap_id column 을 이미 포함, 실제 ALTER 경로 미검증 → `tests/unit/migrate_v9_integration_test.ts` 신설 (fresh-apply column / partial INDEX / schema_migrations row + re-apply duplicate-column 처리 + migrate.ts recovery branch + version ordering invariants). (3) **DATA_MODEL.md v9 sync 부재** — v9 migration row + policy_decisions table description + audit ledger entry 갱신 의무 미이행 → 본 slice 안에서 sync. +16 tests (+8 audit_policy_decisions = assertValidSnapId × 4 + error msg × 1 + recordR2UploadDecision writer-boundary integration × 3; +9 new file migrate_v9_integration_test.ts = fresh-apply × 4 + re-apply × 2 + version ordering × 2 + description anchor × 1) | INFRA-1B.3.h5-policy-decisions-snap-id-column-v9 | AC-032 (NFR-008 audit ledger contract-level enforcement — writer-boundary + migration path + DATA_MODEL sync) | defined | landed | src/storage/audit/policy-decisions.ts (assertValidSnapId + SNAP_ID_SHAPE export + recordR2UploadDecision entry-side fail-fast); tests/unit/audit_policy_decisions_test.ts (+8 writer-boundary tests); tests/unit/migrate_v9_integration_test.ts (신규, +9 migration integration tests); docs/current/DATA_MODEL.md (v9 row + policy_decisions table description + audit ledger entry 갱신); PR #62 merged 2026-05-17 (d3ae654) | OPS-1B.h3-r2-orphan-axis-repairability |
| `OPS-1B.h3-r2-orphan-axis-repairability` | P0-M2 hardening | OPS | OPS-1B | **scanner repairability + skipped_toctou axis split** (Cycle 8, post-#58 GPT review Issue 3 종결) — AI-P1-13 (PR #54) scanner 의 narrow-scope axis naming + missing `skipped_toctou` coverage 처리: (1) **Axis 4 cause-qualified rename** `r2_object_without_graph_key` → `r2_object_without_graph_key_set_failed` (axis name 이 실제 cover scope = `set_r2_key_failed_neo4j` outcome 만 표현). (2) **Axis 4b 신규** `r2_object_without_graph_key_policy_recheck_skipped` (`skipped_toctou` audit row → 동일 physical state 의 R2 object orphan, 단 remediation 분리: **단순 rerun-SET 금지** — recheck 결정 재위반 risk). (3) **expectedR2Key in violation details** = `permitted_artifact/derived/snapshot/${snapId}` — 양 orphan axis 가 emit, repair-CLI / 운영자 cleanup 직접 사용. (4) **`R2UploadOutcomeDecision` extension** + SQL fetch broadening (`decision IN ('uploaded', 'set_r2_key_failed_neo4j', 'skipped_toctou')`). (5) **ScanCounts.skippedToctouAuditRows** + CLI counts 출력 분리. (6) **CLI formatter** new cases for both axes with cause-differentiated remediation text. (7) **scanner top-level docstring** 5 axes → 6 axes rewrite + AI-P1-6 original Axis 2 comment correction. (8) **RUNTIME.md scanner section 신설** (Codex PR #63 P1 finding — historical gap backfill) + TESTING.md SHA placeholder → 2e61825 (Codex P2). +4 tests in r2_invariant_scanner_test.ts (Axis 4 does NOT flag skipped_toctou × 1 + Axis 4b skipped_toctou orphan + expectedR2Key × 1 + Axis 4b does NOT flag set_failed × 1 + malformed skipped_toctou → Axis 5 only × 1). 4 기존 test 가 cause-qualified naming + expectedR2Key + skippedToctouAuditRows assertion 으로 갱신 | OPS-1B.h2-r2-invariant-scanner-orphan-axis, INFRA-1B.3.h6-policy-decisions-snap-id-schema-hardening | AC-032 (NFR-008 audit-by-absence cross-check 의 cause-qualified split + repair-CLI / operator usability) | defined | landed | src/ops/r2-invariant-scanner.ts (R2InvariantViolationType rename + Axis 4b 추가 + R2UploadOutcomeDecision skipped_toctou + ScanCounts.skippedToctouAuditRows + buildExpectedR2Key helper + scanner top-level 6-axis docstring); src/ops/run-r2-invariants.ts (CLI formatter 2 axes + skipped_toctou_audit_rows count + axis 2 docstring 정정); tests/unit/r2_invariant_scanner_test.ts (+4 new tests + 4 existing tests refactored); docs/current/RUNTIME.md (scanner section 신설, 6 axis runtime behavior + operator CLI flow + r2_upload audit lifecycle 명시); docs/current/TESTING.md (SHA placeholder → 2e61825); PR #63 merged 2026-05-17 (116c9ed) | OPS-1B.h4-r2-audit-column-rationale-drift-axis |
| `OPS-1B.h4-r2-audit-column-rationale-drift-axis` | P0-M2 hardening | OPS | OPS-1B | **scanner Axis 6 — column ↔ rationale dual-write contract violation surface** (Cycle 9, post-#58 GPT review 의 마지막 Issue 6 P2 — backward-compat contract hygiene) — AI-P1-15 (PR #57) 가 v9 `snap_id` column 도입 시 `recordR2UploadDecision` 이 column + rationale prefix 를 dual-write 하기로 contract. 그러나 scanner 가 column-preferred resolution 만 채택 + drift 미감지 → manual SQL UPDATE / writer format regression / fixture mistake 가 column 만 또는 rationale 만 수정한 경우 silent. v8- legacy consumer (rationale parsing) 는 다른 snap_id 를 볼 수 있는 risk. Fix: (1) `R2UploadOutcomeAuditRow` shape 확장 — `columnSnapId` + `rationaleSnapId` raw fields 노출 (snapId resolved 값은 backward-compat 유지). (2) `R2InvariantViolationType` 에 `r2_audit_column_rationale_drift` 추가 (Axis 6). (3) reconcile() 에 Axis 6 분기 — 양쪽 sources 가 well-formed AND 다를 때 fire. (4) CLI formatter case + scanner top-level 7-axis docstring rewrite. (5) RUNTIME.md scanner section 에 Axis 6 row 추가. 발화 안 함 cases (silent): column-null v8- legacy, rationale-null column-preferred, both-null (Axis 5 territory), 같은 값 v9 happy path. 발화 case: 둘 다 well-formed 인데 다른 값 | OPS-1B.h3-r2-orphan-axis-repairability, INFRA-1B.3.h5-policy-decisions-snap-id-column-v9 (v9 column owner) | AC-032 (NFR-008 dual-write contract enforceability — column ↔ rationale divergence auditable) | defined | landed | src/ops/r2-invariant-scanner.ts (R2UploadOutcomeAuditRow 확장 + R2InvariantViolationType + reconcile Axis 6 분기 + scanner top-level 7-axis docstring); src/ops/run-r2-invariants.ts (CLI formatter case + docstring); tests/unit/r2_invariant_scanner_test.ts (+6 Axis 6 tests + 16 existing 갱신 via python bulk script); docs/current/RUNTIME.md (Axis 6 row 추가, post-Cycle-9 7 violation axes); docs/current/TESTING.md (test count 704 → 710); PR #64 merged 2026-05-17 (17a31ef) | INFRA-1B.3.h7-gate-evidence-hardening |
| `INFRA-1B.3.h7-gate-evidence-hardening` | P0-M2 hardening | INFRA | INFRA-1B | **Gate evidence hardening hotfix bundle** (Cycle 10, post-PR #65 GPT review Findings 1~5 종결) — Cycle 9 (PR #64) landed 직후 GPT 정적 review 가 발견한 5 finding 처리 (Finding 6 은 planned anchor only `INFRA-1A.x-shared-snapshot-id-constants`). (1) **Finding 1 (P1) `parseSnapIdFromRationale` delimiter strictness** — regex `^snap_id=(snap_[A-Za-z0-9_-]+)` 는 `;` lookahead 없이 trailing garbage (`snap_xxx@evil` / `snap_xxx.bad` / `snap_xxx/extra` / `snap_xxx=value` / `snap_xxx whitespace-no-semicolon`) 도 well-formed 으로 parse → Axis 6 column ↔ rationale drift detection 우회 + canonical contract 약화. Fix: `(?=;|$)` lookahead 추가 (`/^snap_id=(snap_[A-Za-z0-9_-]+)(?=;|$)/`). (2) **Finding 2 (P1/P2) doc drift sweep** — TESTING/RUNTIME/DATA_MODEL header SHA 가 stale, RUNTIME 운영자 CLI flow 가 6 axis 로 표기 (실제 7 axes), current-state + IMPL_PLAN 의 "12 slices 모두 landed" 표기가 16 slices 실재와 drift. Fix: header SHA → 17a31ef + 7-axis 정정 + 16 slices 갱신 + AGENTS.md sync 정책 reference. (3) **Finding 3 (P2) skipped_toctou rationale branch attribution** — snapshot-fingerprint 의 mergeMatchedExisting (dedup-link 경로) vs first-create 경로가 같은 free-form rationale (`"post-r2Put cross-source policy recheck rejected"`) 으로 audit 됨 → operator triage 시 source 구분 불가. Fix: 조건 분기 `mergeMatchedExisting ? "new-path; MERGE-matched existing snapshot; ..." : "new-path; first-create post-r2Put window; ..."` + 2 regression assertions. (4) **Finding 4 (P2) migration test 표현 정정** — TESTING.md 의 "migration integration test" 표현이 실제 `migrate_v9_integration_test.ts` 가 `stripAlterTable()` helper 로 simulate 하는 unit-level duplicate-column recovery 검증을 over-state. Fix: ci.yml row 표현 + 본 thin-doc body 의 dry-run claim 표현 "pending migration listing only" 로 정정. (5) **Finding 5 (P3) docstring `snap_<ULID>` over-claim 정정** — `policy-decisions.ts` `SNAP_ID_SHAPE` 와 `scanner.ts` `SNAP_ID_RATIONALE_PREFIX` docstring 이 `snap_<ULID>` 라고 표현하나 실 regex 는 `^snap_[A-Za-z0-9_-]+$` 로 ULID 외 character 도 accept (test fixture `snap_001` / `snap_test` 사용 중). Fix: docstring 을 실 regex 표현으로 정정. **Finding 6 (P3) defer**: 3 site 가 같은 snapshot ID shape regex / canonical R2 prefix 를 magic-string 으로 보유 — shared constants module 의 anchor 만 `INFRA-1A.x-shared-snapshot-id-constants` 로 register (NOT gate-blocking, planned only). +7 regression tests (parseSnapIdFromRationale describe block, invalid trailing char @ . / = , whitespace-no-semicolon, end-of-string lone snap_id, canonical happy path) + 2 regression assertions (snapshot_fingerprint_test.ts new-path first-create TOCTOU rejected: rationale 가 first-create post-r2Put window 만 포함하고 MERGE-matched 표현 미포함 검증) | OPS-1B.h4-r2-audit-column-rationale-drift-axis | AC-032 (NFR-008 audit ledger writer contract + scanner read contract 의 dual-write delimiter strictness + cause-qualified rationale branch + thin-doc drift sweep) | defined | landed | src/ops/r2-invariant-scanner.ts (SNAP_ID_RATIONALE_PREFIX `(?=;|$)` lookahead); src/storage/audit/policy-decisions.ts (SNAP_ID_SHAPE docstring 정정); src/discovery/worker/snapshot-fingerprint.ts (skipped_toctou rationale mergeMatchedExisting 조건 분기); tests/unit/r2_invariant_scanner_test.ts (+7 parseSnapIdFromRationale invalid trailing char regression tests); tests/unit/snapshot_fingerprint_test.ts (+2 rationale branch attribution assertions); docs/current/RUNTIME.md (header SHA + 6→7 axis CLI flow 정정 + Thin-doc edits since marker); docs/current/TESTING.md (header SHA + test count 710 → 717 + dry-run claim 정정); docs/current/DATA_MODEL.md (header SHA + source-of-truth v{1..6} → v{1..9} + Sync timing 정책 reference); docs/context/current-state.md (12 → 16 slices 갱신 + Cycle 10 hardening list entry); docs/04_IMPLEMENTATION_PLAN.md (12-slice → 16-slice 정정 + Milestones row + 본 row + Finding 6 planned anchor 등록); PR #66 merged 2026-05-17 (fdb847a) | INFRA-1B.5.h2-policy-gate-risk-triggers |
| `INFRA-1B.5.h2-policy-gate-risk-triggers` | P0-M2 hardening | INFRA | INFRA-1B | **8 위험 행동 detection + mode-aware policy_gate trigger 구현 (AC-023 / TEST-023 evidence, AI-P1-11 결정 lock — D1a/D2a 2026-05-17)** — ADR-0017 INV-0017-4 의 List A 8 위험 행동 (1. raw source 외부 LLM 전송 — `external_llm_policy != allowed` / 2. paywalled · proprietary source / 3. terms `no scraping` · `no AI` · `no archive` · `no redistribution` / 4. wire-service full text / 5. article·report 원문 quote · cache / 6. 도표·스크린샷 콘텐츠 추가 / 7. raw source embedding · indexing / 8. raw source cloud upload) 을 mode-aware policy_gate 안에서 detection. 각 trigger 는 **mode 무관 inline_block 고정** (INV-0017-4) + `policy_decisions` ledger 기록 (INV-0017-5). 비위험 행동은 **stage-default mode** 적용 (INV-0017-3 — Discovery=inline_warn / Extract·Cache·Embed·Cloud upload=inline_block / 탐색·콘텐츠 제작=batch_report / Publication preflight=inline_block). retrospective AI-P1-11 의 List B (content-production safety) 는 ADR-0017 범위 밖 — P0-M6 publication 슬라이스 별도 axis 로 분리. **구현 본문 (본 PR landed)**: (a) `src/pipeline/policy-gate/risk-triggers.ts` — `RiskTriggerContext` + 8 detector + `detectRisks()` + `stageDefaultMode()` + `evaluatePolicyGate()` (INV-0017-3/4 합산 entry-point, pure function — SQLite I/O 없음, pre-flight 평가 가능). (b) `src/pipeline/policy-gate/decision-ledger.ts` — generic `recordPolicyGateDecision()` writer (operator-gate namespace, intended_action=NULL, v8 r2_upload enum trigger 우회) + `NON_RISK_TRIGGER_TYPE` sentinel + writer-boundary defense-in-depth assertions (triggerType / decision / gateMode enum 검증). (c) `src/utils/enums.ts` — `POLICY_GATE_MODE` / `GATE_DECISION` / `RISK_TRIGGER` (8) / `PIPELINE_ACTION` (11) / `PIPELINE_STAGE` (5) enum 신규. (d) `tests/policy/gate_test.ts` — 53 tests = stageDefaultMode (6) + detectRisks (24 — 8 trigger 별 fire / not-fire / mode-invariance / multi-trigger) + evaluatePolicyGate (8) + recordPolicyGateDecision (6 — intended_action=NULL 검증 + writer-boundary rejection + namespace 분리) + TEST-023 E2E (9 — 8 trigger 별 detection + ledger record + INV-0017-4 mode-invariance verified). **scope boundary**: 본 slice 는 detection module + ledger writer 만 land. chunker / R2 upload / embed / external LLM call site 의 실제 wiring 은 (i) chunker 이미 INFRA-1B.4.h1 에서 archive_policy gate enforce (PR #47, AI-P1-1) — 본 module 의 generic detector 와 cohesive, (ii) R2 upload 이미 INFRA-1B.3.h1/h4 에서 cross-source guard enforce (PR #41 + #53), (iii) embed / external LLM call site 는 EXTR-1A.* 영역 (P0-M3+) 에서 wiring — 본 slice 의 module 을 import 해 evaluatePolicyGate 호출 + recordPolicyGateDecision. **PR 1b follow-up notes** (별도 doc PR scope): ADR-0012 INV-0012-7 의 `external_llm_policy ≠ prohibited` 표현을 ADR-0017 의 `external_llm_policy = allowed` 로 정합 보정 (본 module 의 detector 1 은 strict 표현 채택). | INFRA-1B.1 (Source policy 3 필드 + ADR-0017 INV-0017-1), INFRA-1B.3.x-audit (policy_decisions ledger + INV-0017-5), INFRA-1B.4.h1-chunker-policy-gate (chunker integration baseline), INFRA-1B.5 (access_intervention + batch_report mode baseline — 본 slice 의 hardening 2), AI-P1-11 D1a/D2a 결정 lock (PR #67) | AC-023 (P0-M2 obligatory evidence), TEST-023 | defined | landed | src/pipeline/policy-gate/risk-triggers.ts (RiskTriggerContext + 8 detector + evaluatePolicyGate pure function); src/pipeline/policy-gate/decision-ledger.ts (recordPolicyGateDecision + NON_RISK_TRIGGER_TYPE sentinel + writer-boundary enum guard); src/utils/enums.ts (POLICY_GATE_MODE + GATE_DECISION + RISK_TRIGGER + PIPELINE_ACTION + PIPELINE_STAGE 신규); tests/policy/gate_test.ts (81 tests — 8 trigger detection + 5 stage default + evaluatePolicyGate combined + ledger persistence + TEST-023 E2E 8 trigger × ledger × mode-invariance + Codex PR #68 rounds 1~6 P1/P2 fixes (wire-service bare AP word-boundary + Reuters Institute negative lookahead + terms_violation external LLM coverage + unregistered source fail-closed + intended_action 확장 raw_text+excerpt + sourceName missing fail-closed + runtime fail-closed boundary guards for stage/action + policy field validation for archivePolicy/rawCloudPolicy/externalLlmPolicy + registered source 'unknown' sentinel fail-closed + stage-action compatibility matrix throw on non-risk path (storage-class containment, 운영자 결정 옵션 (a) GPT review 2026-05-18))); PR #(본 PR — pending merge SHA, INFRA-1B.5.h2 engineering) | (P0-M2 gate accept evidence — AC-023 / TEST-023 close) |
| `INFRA-1A.9-validator-extension` | P0-M2 hardening (**NOT gate-blocking — DEC-020 Q-045 / TRACE-041 resolution**) | INFRA | INFRA-1A | **Invariant validator scope 확장 — ADR INV ↔ 코드 cross-ref** (Cycle 14, DEC-020 Q-045 옵션 (b) implementation) — `scripts/validate_invariants.ts` 의 `DocFrontmatter.invariants[]` 에 optional `cross_ref_code?: string[]` field 추가 + 신규 `checkCrossRefCode()` 함수. 각 entry 의 format detection (`<file>:<exportName>` 또는 `<file>:<line>`), `file:exportName` 의 export 매칭 (선언 + re-export block), `file:line` 의 1..lineCount 범위 검사. 깨진 reference 는 warning-level (INV-0002-1 contract 준수). INV-0012-3 backfill 4 entries — `src/storage/r2/policy.ts:checkPermittedPrefix` (permitted artifact gate) + `src/storage/audit/policy-decisions.ts:recordR2UploadDecision` (audit ledger writer) + `src/ops/r2-invariant-scanner.ts:scanR2Invariants` (runtime scanner) + `src/discovery/worker/snapshot-fingerprint.ts:120` (allLinkedSourcesAllowR2SnapshotUpload module-private signature line — file:line 형식 사용 사유). **Scope boundary**: 본 PR 은 validator 확장 + INV-0012-3 backfill 만 land. DEC-020 Q-045 priority 의 나머지 backfill (INV-0028-1~6 safe-fetch 8단 방어 / INV-0023-3 domain_override_reason / INV-0017 audit) 은 다음 cycles 로 분리 — 각 ADR 의 enforcement code site 검증이 별도 review surface. 본 slice 의 boilerplate sync 정책 (local extension 으로 먼저 lock, 작동 검증 후 boilerplate PR 로 promote) 도 DEC-020 Q-045 그대로 따른다. **Hardening sub-slices**: h1 (PR #90 Cycle 32 landed) raw `fetch(` ban static check for ADR-0028 INV-0028-1, h2 (planned) cross_ref_code semantic_level field 도입, h3 (planned) validator self-strip stability hardening. | DEC-020 Q-045 resolution, ADR-0012 (INV-0012-3 backfill 시점) | TRACE-041 (validator coverage extension) | defined | landed | scripts/validate_invariants.ts (DocFrontmatter type 확장 + escapeRegex helper + checkCrossRefCode 신규 function + main check chain 등록); docs/adr/0012-non-archival-storage-neo4j-sqlite-r2.md (INV-0012-3 frontmatter cross_ref_code 4 entries 백필); tests/lint/cross_ref_code_test.ts (NEW — 13 tests: format detection 3 + file:exportName resolution 4 + file:line resolution 4 + repo-wide invariant 2); docs/current/CODE_MAP.md (validate_invariants.ts row 갱신); docs/current/TESTING.md (Cycle 14 entry prepend + test count 820 → 833); PR #(본 PR — pending merge SHA) | INFRA-1A.9.h1 / h2 / h3 |
| `INFRA-1A.9.h1-safe-fetch-raw-fetch-static-check` | P0-M2 hardening (**NOT gate-blocking — Opus PR #66~#78 review F4 close**) | INFRA | INFRA-1A | **Raw `fetch(` ban static check for ADR-0028 INV-0028-1** (Cycle 32, PR #90 aefab68) — `scripts/validate_invariants.ts` 에 `checkRawFetchBan()` 등록 + helper `findRawFetchCalls()` + exports `RAW_FETCH_BAN_DIRS` / `RAW_FETCH_BAN_ALLOWLIST` / `RAW_FETCH_BAN_PATTERN`. Detection = `(?<![.\p{ID_Continue}$])fetch\s*\(` Unicode-aware negative lookbehind (`.fetch(` / `$fetch(` / `prefetch(` / 한글 identifier suffix false-positive 차단). scope = `src/discovery/**` + `src/extraction/**`, allowlist = `src/discovery/fetch/safe-fetch.ts`. comment / string / regex-literal stripping = 기존 `stripCommentsAndStrings` 재사용. warning-level (INV-0002-1 contract). `tests/lint/raw_fetch_ban_test.ts` 신규 19 tests = pattern preceding-char exclusion 8 + comment/string defense 6 + repo-wide invariant 5 (repo-wide test 가 production-code 위반을 hard test failure 로 pin). ADR-0028 INV-0028-1 statement body 보강 — `cross_ref_code` self-reference (`scripts/validate_invariants.ts:checkRawFetchBan`) 은 의도적으로 추가하지 않음 (validator self-stripping 의 running backtick parity shift 로 false `export not found` 발생; 본 cycle 직접 실험 확인). 그 risk 의 hardening anchor 가 `INFRA-1A.9.h3-validator-self-strip-stability`. | INFRA-1A.9-validator-extension (validator 확장), ADR-0028 (INV-0028-1 statement), DEC-020 Q-045 cross_ref_code semantic scope clarification (PR #83) | (NOT P0-M2 gate-blocking — Opus PR #66~#78 review F4 followup) | defined | landed | scripts/validate_invariants.ts (checkRawFetchBan + findRawFetchCalls + RAW_FETCH_BAN_DIRS/ALLOWLIST/PATTERN exports); tests/lint/raw_fetch_ban_test.ts (NEW — 19 tests); docs/adr/0028-safe-fetch-adversarial-web-defenses.md (INV-0028-1 statement body extension); docs/context/current-state.md (anchor planned → landed); docs/current/TESTING.md (Cycle 32 header, 911 → 930 tests); PR #90 merged 2026-05-18 (aefab68) | INFRA-1A.9.h2 |
| `INFRA-1A.9.h2-cross-ref-semantic-level` | P0-M2 hardening (**NOT gate-blocking, P3 priority, defer-eligible** — Opus PR #66~#78 review F3 followup, GPT 합의 2026-05-18) | INFRA | INFRA-1A | **Stronger semantic level for `cross_ref_code` entries** (planned anchor, 운영자 promotion 시점에 활성화) — DEC-020 Q-045 wording correction (PR #83) 으로 `cross_ref_code` 가 enforcement proof 가 아니라 reachability heuristic (`file:exportName` = `export_exists` weak / `file:line` = `line_exists` 더 weak) 임이 명시됨. 본 slice 는 그 위에 **stronger semantic level**을 optional field 로 도입 — `cross_ref_code` entry 의 schema 를 `{path: string, semantic_level?: "export_exists" | "line_exists" | "callsite_exists" | "forbidden_call_absence" | "test_fixture_proved"}` 같은 구조로 확장 (또는 inline format `<path>#semantic=<level>` 등). validator 가 각 semantic_level 에 맞는 검증 수행 — `callsite_exists` = grep for `<name>(` call site / `forbidden_call_absence` = grep for ABSENCE of forbidden token / `test_fixture_proved` = test file 안에 invariant verification fixture 가 있는지 등. 본 slice 는 P3 priority — Opus review F3 wording correction 으로 즉시 risk 는 닫혔고, semantic level 도입은 stronger evidence 가 필요한 invariant 가 출현하면 활성화. 가장 가능성 높은 trigger = INV-0017-5 가 EXTR-1A.* production caller wiring 후 ledger persistence 검증을 cross_ref_code 로 표현해야 할 때. | INFRA-1A.9-validator-extension, INFRA-1A.9.h1-safe-fetch-raw-fetch-static-check, DEC-020 Q-045 cross_ref_code semantic scope clarification (PR #83) | (NOT P0-M2 gate-blocking — P3 defer-eligible, operator promotion 시점 결정) | proposed | planned | TBD (구현 시점: schema extension + validator check 분기 + 기존 entry migration / backwards-compat) | INFRA-1A.9.h3 |
| `INFRA-1A.9.h3-validator-self-strip-stability` | P0-M2 hardening (**NOT gate-blocking, P3 priority** — Cycle 32 PR #90 에서 발견된 fragility 의 follow-up anchor) | INFRA | INFRA-1A | **Validator `stripCommentsAndStrings` self-strip stability hardening** (planned anchor, 운영자 promotion 시점에 활성화) — Cycle 32 (PR #90) 에서 `scripts/validate_invariants.ts:checkRawFetchBan` 을 ADR-0028 INV-0028-1 `cross_ref_code` 에 self-reference 로 추가 시도 시, validator 의 자체 stripCommentsAndStrings pass 가 자신의 regex-literal pattern body 들과 상호작용하여 running backtick parity shift 가 발생, 새 `export function` declaration 이 stripped 된 source 에서 사라지고 `hasNamedDeclaration` 이 false `export not found` 를 emit 하는 fragility 가 확인됨. 본 slice 는 그 fragility 의 root cause 를 닫는다 — 가능한 옵션: (a) regex-literal stripping heuristic 을 token-aware tokenizer 로 교체 (TS AST 사용), (b) self-validation path 에 한해 `stripCommentsAndStrings` 의 multi-line eat 을 sentinel 로 차단, (c) cross_ref_code resolution 이 self-references (scripts/validate_invariants.ts 자신) 인 entry 를 자체 module 의 export map 으로 별도 lookup, (d) hasNamedDeclaration 이 stripped + raw 양쪽에서 match 되면 OK 처리 (relaxed contract). 옵션 선택은 운영자 결정. **본 slice 가 land 하기 전에는** validator 의 self-cross_ref 는 ADR statement body prose 로만 표현 (Cycle 32 ADR-0028 INV-0028-1 의 caveat 참조). | INFRA-1A.9-validator-extension, INFRA-1A.9.h1-safe-fetch-raw-fetch-static-check (Cycle 32 가 fragility 발견) | (NOT P0-M2 gate-blocking — P3 defer-eligible) | proposed | planned | TBD (구현 시점: 옵션 선택 + tokenizer 교체 또는 sentinel-based skip + regression tests for validator self-validation) | (INFRA-1A.9 series complete) |
| `INFRA-1A.9.h5-vault-jsonl-policy-validator` | P0-M2 hardening (**NOT gate-blocking — operator D4 2026-05-18, config-level invariants for ADR-0012**) | INFRA | INFRA-1A | **Pure policy validator for ADR-0012 INV-0012-5 (Markdown vault content kinds + scenario citation + MDX support) + INV-0012-6 (JSONL not canonical store)** (Cycle 35, PR #94, operator D4 2026-05-18 + 1 codex round hardening) — `scripts/check-vault-jsonl-policy.ts` 신규 (no runtime wiring): `findVaultFiles` (walks `VAULT_FILE_EXTENSIONS = [".md", ".mdx"]` over vault/ + docs/vault/; populates frontmatter + body per file) + `assertVaultContentKinds` (every vault file frontmatter `type` ∈ {documenthub / dossier / scenario / thesis / contentdraft / publication / promotedclaim / **editorialintent** [ADR-0025 added per codex P2]}, separator + case insensitive normalize) + `assertPromotedClaimsAreCited` (codex P2 — every `promoted_claim` file's claim_id (frontmatter `claim_id` field or filename basename fallback) must appear in either some scenario's frontmatter `cited_claims[]` array OR as a whole-word match in some scenario's body markdown text; INV-0012-5 "scenario에 인용된" enforcement) + `findJsonlFiles` (repo-wide *.jsonl scan skipping node_modules/.git/build dirs) + `assertJsonlIsNotCanonical` (every JSONL must be under one of ALLOWED_JSONL_PATH_PREFIXES = `.dev-cycle/` / `docs/audit-export/` / `docs/_generated/` / `tests/fixtures/` / `tests/lint/fixtures/` / `tests/policy/fixtures/`) + aggregate `checkVaultJsonlPolicy` + `import.meta.main` CLI guard (`bun run scripts/check-vault-jsonl-policy.ts [repoRoot]`). All assertions report ALL violations in a single error message (not fail-fast on first). **Scope boundary (operator D4 explicit)**: pure validator, NOT runtime wiring. INV-0012-5 의 "candidate claim 자동 markdown 생성 금지" 의 execution-level enforcement (writer 부재 보장) 은 EXTR-1A.* phase deferred — 본 PR 은 vault content static scan + scenario-citation static check + JSONL allowlist static check 만. 도메인이 ADR-0023 (D3, LLM routing) 와 다르므로 별도 PR 로 분리 (operator D4 명시). ADR-0012 INV backfill cumulative = INV-0012-1 (PR #88) + INV-0012-2 (PR #88) + INV-0012-3 (PR #72) + INV-0012-4 (PR #88) + INV-0012-5 (this PR — 2 targets: assertVaultContentKinds + assertPromotedClaimsAreCited) + INV-0012-6 (this PR) + INV-0012-7 (PR #86) = 7/7 invariants cross_ref_code 보유 (ADR-0012 backfill 완료). | DEC-020 Q-045 cross_ref_code system, ADR-0012 (INV-0012-5/6 frontmatter cross_ref_code), ADR-0022 (vault MDX emission), ADR-0025 (EditorialIntent kind), repo filesystem (vault/ + docs/vault/ + *.jsonl distribution) | (NOT P0-M2 gate-blocking — config-level enforcement only) | defined | landed | scripts/check-vault-jsonl-policy.ts (NEW — VAULT_ROOTS + VAULT_FILE_EXTENSIONS + PERMITTED_VAULT_KINDS (8 incl. editorialintent) + ALLOWED_JSONL_PATH_PREFIXES + VaultJsonlPolicyError + findVaultFiles + assertVaultContentKinds + assertPromotedClaimsAreCited + findJsonlFiles + assertJsonlIsNotCanonical + checkVaultJsonlPolicy + CLI entry guard); tests/policy/vault_jsonl_policy_test.ts (NEW — 49 tests post-codex-round-1: 4 live + 12 vault content kinds incl. EditorialIntent + MDX export coverage + 9 assertPromotedClaimsAreCited unit + 12 JSONL allowlist + 12 filesystem round-trip via tmpdir incl. MDX scan + orphan-claim throw + happy-path round-trip); docs/adr/0012-non-archival-storage-neo4j-sqlite-r2.md (INV-0012-5 frontmatter 2 cross_ref entries [assertVaultContentKinds + assertPromotedClaimsAreCited] + statement appendix note explaining EditorialIntent inclusion / INV-0012-6 frontmatter 1 cross_ref entry); docs/current/CODE_MAP.md (validator script + test row 등록 post-round-1 갱신); docs/current/TESTING.md (Cycle 35 baseline header, 1037 → 1086 tests post-codex-round-1); PR #94 (pending merge SHA) | (ADR-0012 INV backfill complete — 7/7) |
| `INFRA-1A.9.h4-llm-routing-config-validator` | P0-M2 hardening (**NOT gate-blocking — operator D3 2026-05-18, config-level invariants for ADR-0023**) | INFRA | INFRA-1A | **Pure config validator for `data/llm_routing.yaml` — ADR-0023 INV-0023-2 + INV-0023-3 + INV-0023-5** (Cycle 34, PR #93, operator D3 2026-05-18) — `scripts/check-llm-routing-config.ts` 신규 (no runtime LLM wiring): `loadLlmRoutingConfig` js-yaml parser + 3 assertion exports + aggregate `checkLlmRoutingConfig` + `LlmRoutingConfigError` + `import.meta.main` CLI guard. **INV-0023-2 enforcement (`assertTier0VendorRoles`)**: tier_0.openai.role='default' + openai.effort='extended_thinking' + anthropic.role='cross_vendor_review_only' + anthropic.effort ∈ {high, xhigh} + google.role='disabled' (or omitted). Codex PR #93 round 1 P2 — effort fields 도 검증 추가 (statement 의 "with extended thinking" + "with high effort" lock). **INV-0023-3 enforcement (`assertTiersCapabilityCanonical`)**: 모든 tier 가 non-empty `capability` 보유 + 8 forbidden tier-canonical KEY (price/cost/cheap/budget/cost_tier/price_tier/price_class/cost_class 대소문자 무관) 거부 + capability VALUE 도 4 forbidden token (price/cost/cheap/budget) snake_case 단어 경계 거부. Codex PR #93 round 1 P2 — value scan 추가 (live YAML 의 `tier_3.capability` 가 `low_cost_high_volume_and_search_grounding` 이었음 → `high_volume_throughput_and_search_grounding` 으로 갱신). vendor 서브블록 (tier.openai 등) 은 exempt — operational cost metadata 가능. **INV-0023-5 enforcement (`assertGoogleScopeIsTier3Only`)**: tier_3 외 모든 tier (미래 tier_4 / tier_experimental 포함) 에 대해 google.role !== 'disabled' 이면 거부 (Codex PR #93 round 1 P2 — 이전 tier_0/1/2 만 검사 → 모든 비-tier_3 tier scan); tier_3.google.role ∈ ALLOWED_TIER3_GOOGLE_ROLES = {search_grounding_or_cost_effective_fallback, search_grounding, cost_effective_fallback} 만 허용 (default/cross_vendor_review_only 거부 — INV-0023-5 main generator 금지 명시 매핑, Codex PR #93 round 1 P2). **Scope boundary (operator D3 명시)**: pure config validator, NOT runtime wiring. LLM 호출 0, routing logic 변경 없음. INV-0023-4 (cross_vendor_review_ledger writer) / INV-0023-6 (Data Science Module wiring) / INV-0023-8 (quote substring verifier) 은 EXTR-1A.* phase defer 유지 (production caller 0, Cycle 18 contract). ADR-0023 INV backfill cumulative = INV-0023-1 (PR #73) + INV-0023-2 (this PR) + INV-0023-3 (this PR) + INV-0023-5 (this PR) + INV-0023-7 (PR #87) = 5/8 invariants cross_ref_code 보유. | DEC-020 Q-045 cross_ref_code system, ADR-0023 (INV-0023-2/3/5 frontmatter cross_ref_code), `data/llm_routing.yaml` (operational catalog) | (NOT P0-M2 gate-blocking — config-level enforcement only) | defined | landed | scripts/check-llm-routing-config.ts (NEW — LlmRoutingConfig type + loadLlmRoutingConfig + assertTier0VendorRoles + assertTiersCapabilityCanonical + assertGoogleScopeIsTier3Only + checkLlmRoutingConfig + TIER0_OPENAI_REQUIRED_EFFORT + TIER0_ANTHROPIC_ALLOWED_EFFORTS + FORBIDDEN_TIER_PRICE_AXIS_KEYS + FORBIDDEN_CAPABILITY_VALUE_TOKENS + ALLOWED_TIER3_GOOGLE_ROLES + NON_GOOGLE_TIERS + GOOGLE_REQUIRED_TIER + LlmRoutingConfigError + CLI entry guard); tests/policy/llm_routing_config_test.ts (NEW — 62 tests after codex round 1 fix: 5 live integration + 15 assertTier0VendorRoles incl. 7 effort enforcement + 21 assertTiersCapabilityCanonical incl. 10 value scan + 21 assertGoogleScopeIsTier3Only incl. unknown-tier rejection + tier_3 role allowlist); data/llm_routing.yaml (tier_3.capability 갱신 — "low_cost" 제거); docs/adr/0023-llm-routing-v2-gpt-default-multi-vendor-minimal-cross-review.md (INV-0023-2/3/5 frontmatter cross_ref_code 3 entries); docs/current/CODE_MAP.md (validator script + test row 등록); docs/current/TESTING.md (Cycle 34 baseline header, 970 → 1032 tests post-codex-round-1); PR #93 (pending merge SHA) | (INFRA-1A.9 ADR-0023 config validator complete; 잔존 INV-0023-4/-6/-8 는 EXTR-1A.* phase defer) |
| `DOC-1A.1-testing-md-baseline-observability-hygiene` | P0-M2 hardening (**NOT gate-blocking, P3 hygiene** — Opus PR #66~#78 review F11 followup) | DOC | DOC-1A | **TESTING.md baseline observability hygiene** (planned anchor, 운영자 promotion 시점에 활성화) — Opus PR #66~#78 review F11 의 본질: `docs/current/TESTING.md` top paragraph 가 매우 길어 (현재 한 paragraph 안에 current baseline / thin-doc edits chain / Cycle 1~32 history / local test claim / GitHub CI evidence 가 모두 섞여 있음) operator 가 실제 현재 baseline 을 구분하기 어려움. baseline observability risk. 본 slice 의 scope: (1) `docs/current/TESTING.md` top 은 **current baseline only** — last verified SHA + date + test count + 1~2 sentence summary 만 (한 paragraph 짧게). (2) Cycle history 는 별도 file `docs/context/TESTING_HISTORY.md` (또는 structured table) 로 분리. (3) AGENTS.md "thin-doc Last verified against code" sync rule 갱신 — engineering slice PR 의 obligation 이 short baseline + history 별도 file append 로 분리됨. (4) test count 의 `source` (`local bun test` / `CI` / `external`) + `run environment` + `commit SHA` 를 structured 로 명시 (현재는 free-form prose). **본 slice 가 land 하지 않는 한** 현재의 long paragraph header 가 유지된다 — P3 hygiene 이므로 gate accept 와 무관. **GPT 의견 합의 (2026-05-18)**: 길이 자체보다는 baseline / history / claim source 가 한 paragraph 에 섞이는 observability risk 가 핵심. P3 hygiene 으로 downgrade. | INFRA-1A.9-validator-extension (cross_ref_code system 안정화 후 history 분리 작업 가능), AGENTS.md "thin-doc sync timing" rule (조정 필요) | (NOT P0-M2 gate-blocking — P3 hygiene only) | proposed | planned | TBD (구현 시점: TESTING.md baseline-only header 재작성 + TESTING_HISTORY.md 신설 + AGENTS.md rule 갱신 + structured test source metadata) | (independent of P0-M2 gate) |
| `INFRA-1A.x-shared-snapshot-id-constants` | P0-M2 hardening (**NOT gate-blocking refactor**) | INFRA | INFRA-1A | **Shared snapshot ID + snapshot-stage R2 prefix constants module** (Cycle 12, PR #66 Cycle 10 Finding 6 anchor consolidation) — pre-refactor 4 site magic-string duplication 종결: (1) `src/storage/audit/policy-decisions.ts` `SNAP_ID_SHAPE` + `assertValidSnapId` writer-boundary, (2) `src/ops/r2-invariant-scanner.ts` `SNAP_ID_SHAPE` + `SNAP_ID_RATIONALE_PREFIX` + `SNAPSHOT_R2_KEY_PREFIX` + 2 exported reader-boundary helpers (`parseSnapIdFromRationale` / `validSnapIdOrNull`) + internal `buildExpectedR2Key`, (3) `src/discovery/worker/snapshot-fingerprint.ts` 2 inline `permitted_artifact/derived/snapshot/${snapId}` templates (lines 541 dedup back-fill + 722 new-path), (4) `src/storage/r2/policy.ts` `PERMITTED_PREFIXES[5]` literal. Shared module `src/domain/snapshot-id.ts` 신설 — `SNAPSHOT_ID_REGEX` + `RATIONALE_SNAP_ID_PREFIX_REGEX` + `SNAPSHOT_R2_KEY_PREFIX` constants + `snapshotR2Key()` + `validSnapIdOrNull()` + `assertValidSnapId(value, context?)` + `parseSnapIdFromRationale()` helpers (zero imports — domain layer purity preserved). 4 consumer site import 으로 전환: scanner.ts 의 2 reader-boundary helper 는 re-export 로 backward-compat 유지 (downstream `tests/unit/r2_invariant_scanner_test.ts` + `src/ops/run-r2-invariants.ts` import path 불변); policy-decisions.ts 의 `assertValidSnapId(value)` 는 thin wrapper 로 `recordR2UploadDecision` context prefix 전달 (legacy single-arg signature + 기존 `/invalid snap_id shape/` error message + `recordR2UploadDecision: ` byte-compatible). **NOT gate-blocking** — PR #66 Finding 6 anchor 표현 "shared constants module 의 anchor 만 register (NOT gate-blocking, planned only)" 를 land 단계로 이행. drift surface (PR #66 Cycle 10 Finding 1 의 `parseSnapIdFromRationale` `(?=;|$)` delimiter strictness 가 정확히 이 class 의 silent failure 였음 — 한 site 만 fix 한 채 다른 site 가 stale regex 유지 시 silent corruption) 의 single source of truth 확정. Opus adversarial review NO ACTIONABLE FINDINGS (behavior drift / layering / 잔존 magic-string / re-export pattern / stale comment / test coverage / import path 7 obligation pass). +18 unit tests (snapshot_id_test.ts) = 798 → 816 tests. **Informational defer**: writer-side `formatRationale()` 의 `\`snap_id=${input.snapId}\`` literal (5번째 잠재 consolidation 후보, 본 slice scope 외 — terminator structure 차이로 별도 helper 추출은 후속 cycle 가능). | INFRA-1B.3.h7-gate-evidence-hardening (PR #66 Finding 6 anchor 원천), INFRA-1B.3.h6-policy-decisions-snap-id-schema-hardening (writer-boundary `assertValidSnapId`), INFRA-1B.3.h5-policy-decisions-snap-id-column-v9 (reader-boundary `validSnapIdOrNull`), OPS-1B.h4-r2-audit-column-rationale-drift-axis (Cycle 9 Axis 6 의 column ↔ rationale dual-write contract) | (NOT P0-M2 gate-blocking — drift-surface 종결 refactor; P0-M2 gate evidence 와 별도) | defined | landed | src/domain/snapshot-id.ts (NEW — single source of truth); src/storage/r2/policy.ts (PERMITTED_PREFIXES[5] literal → SNAPSHOT_R2_KEY_PREFIX import); src/storage/audit/policy-decisions.ts (local SNAP_ID_SHAPE + 본문 docstring 축소 + assertValidSnapId → thin wrapper passing "recordR2UploadDecision" context); src/ops/r2-invariant-scanner.ts (3 local const + 2 internal validator 제거 + buildExpectedR2Key → snapshotR2Key alias + reader-boundary 2 helper re-export from domain module); src/discovery/worker/snapshot-fingerprint.ts (2 inline template → snapshotR2Key()); tests/unit/snapshot_id_test.ts (NEW — 18 tests: shape regex / R2 prefix / snapshotR2Key / validSnapIdOrNull nullable / assertValidSnapId context-prefix 4 variants / parseSnapIdFromRationale Cycle 10 delimiter strictness 6 cases / RATIONALE_SNAP_ID_PREFIX_REGEX capture); PR #(본 PR — pending merge SHA) | (drift surface 종결, downstream slice 영향 없음) |
| `INFRA-1B.1.h4-category-enum-validation` | P0-M2 hardening (**planned follow-up anchor — NOT gate-blocking, blocked/sequence-dependent on h2**) | INFRA | INFRA-1B | **AC-031 source-side enum reject hardening anchor** (codex P1 finding on PR #55 round 1; slice ID 는 `INFRA-1B.1.h3-seed-sources-argv-allowlist` (PR #56) landed 후 prefix 충돌 회피 위해 `h3` → `h4` 로 변경). **Gate accept semantics (post-#58 GPT review 정정)**: 본 anchor 는 PR #59 로 등록되었으나 **P0-M2-hardening 의 16-slice engineering sequence 를 reopen 하지 않음** + **P0-M2 gate accept 의무 evidence 에서 제외** (operator 가 명시적으로 AC-031 을 gate evidence 에 promote 시점에만 변경). 본 slice 는 h2 (`data/categories.yaml` owner) sequencing 후에만 ready 가 되며 그 전까지는 blocked planned anchor. — 구현 scope: `src/storage/source-registry/seed.ts` 의 `validateSource` 가 현재 `meta_category` / `access_method` 를 non-empty string 으로만 검증 + policy enum 4 종만 enum-check. AC-031 source-side 요구사항 미충족 (`docs/06_ACCEPTANCE_TESTS.md:52`): (a) **source-side `meta_category` 필드** (`SeedSource` 의 실 컬럼명 — Dossier-side `topic` 측은 본 slice 영역 아님, AGG-1A.1 별도) = 4 메타 enum (`policy` / `economy` / `society` / `pop_culture`) 외 값 reject, (b) `subtopic_tags[]` = 기존 8 enum (`macro_finance` / `geopolitics_security` / `health_biosecurity` / `energy_commodities` / `trade_supply_chain` / `climate_environment` / `technology_cyber_ai` / `digital_assets`) + tag 5개 (`social_stability_information` / `demographics_migration` / `food_water_security` / `governance_institutions` / `critical_infrastructure`) 외 값 reject. seed parse-time 에 invalid 값 발견 시 `SeedValidationError` fail-fast. **Scope boundary vs `INFRA-1B.1.h2-source-profile` (AI-P1-4)**: h2 = Source profile canonical store 의 broader scope (`data/categories.yaml` 신설 + collectability / access_method / meta_category / subtopic_tags 4-축 persist 의무) 가 source of truth artifact 의 owner. h4 = h2 의 `data/categories.yaml` 을 import 해 `validateSource` 안에서 enum-reject wiring 만 추가하는 thin layer. h2 가 먼저 land 하면 h4 의 enum-reject 가 그 안에 흡수되어 redundancy 가능 — 운영자가 h2 / h4 sequencing 시점에 재평가 (h4 drop or h2 expand). AGG-1A.1 dossier `topic` 측 검증은 별도. **본 slice 신설 이유**: PR #55 round 0 에서 AC-031 을 INFRA-1B.1 Gate 에 추가했으나 codex P1 review (PR #55 round 1) 가 `validateSource` 코드 미구현을 지적 → false coverage 로 revert. AC-031 이 다시 orphan 상태가 되어 별도 hardening slice anchor 가 필요함을 commit history 에 trace 로 남김 | INFRA-1B.1, INFRA-1B.1.h2-source-profile (categories.yaml owner — **blocked-on-h2**), DEC-004 (4 메타 카테고리 lock) | AC-031 (**NOT P0-M2 gate-blocking — planned follow-up only**) | defined | planned (blocked-on-h2) | TBD (구현 시점: `validateSource` 본문 확장 + `data/categories.yaml` import + enum-reject unit tests + TEST-031 anchor `tests/lint/category_enum_test.ts` 실 위치) | INFRA-1B.2.x |
| `INFRA-1B.2.x` | P1-M2-hardening | INFRA | INFRA-1B | **Discovery streaming phase** (PR #25 retro F-13, Q-039) — `pollEligibleSources`를 chunked allSettled 또는 async-iterator로 전환해 source 수 / body size 증가에도 메모리 bounded 유지 | INFRA-1B.2, Q-039 resolution | AC-001 | defined | planned | TBD | DEPLOY-1A.1 |
| `DEPLOY-1A.1` | P1-MVP-prep | OPS | DEPLOY-1A | **v0→production schema migration framework** (PR #26 codex review, Q-040) — 마지막 wipe-and-reseed 시점 stamp + 그 이후 모든 변경은 ALTER만; backfill scripts(slug→canonical, legacy queue rows, crawl_state remap); migration runner rollback 지원; schema drift CI check. **P0-M6(MVP gate) accept + P1-M2/M3 hardening 슬라이스 안정화 이후, production deploy 직전 진입** (post-M6 hardening 결과까지 ALTER-only contract 적용 가능 시점) | INFRA-1B.2.x, INFRA-1B.3.x, INFRA-1B.6.x (모든 schema-touching hardening slice 안정화 후), Q-040 resolution | AC-032 | defined | planned | TBD | DEPLOY-1A.2 |
| `DEPLOY-1A.2` | P1-MVP-prep | OPS | DEPLOY-1A | **Timestamp 형식 CHECK constraints** (PR #25 retro F-17, Q-041) — `updated_at`, `discovered_at`, `accessed_at`, `expires_at` 등 모든 ISO timestamp 컬럼에 CHECK GLOB regex 추가. future writer drift 차단 | DEPLOY-1A.1, Q-041 resolution | AC-032 | defined | planned | TBD | MVP gate |
| `EXTR-1A.0` | P0-M3 | EXTR | EXTR-1A | **Prompt Injection 방어 기반 (ADR-0029 선행 구현, EXTR-1A.1 선행 조건)**: src/extraction/sanitize/html-to-text.ts (script/style/iframe 태그 완전 제거 후 plain text); src/extraction/prompt/untrusted-wrapper.ts (sentinel 래핑 + 토큰 상한 INV-0029-3); src/extraction/policy/llm-policy-gate.ts (external_llm_policy 게이트, prohibited/manual_review_required 차단). 방어별 단위 테스트 포함 | INFRA-1B.4 | AC-009 | defined | planned | TBD | EXTR-1A.1 |
| `EXTR-1A.1` | P0-M3 | EXTR | EXTR-1A | Extractor router (article/dataset/report 분기) + **extractor interface contract (NFR-007 / AC-021) — 신규 source type 추가 시 dry-run 1건 추가 가능** | EXTR-1A.0 | AC-009, AC-021 | defined | planned | TBD | EXTR-1A.2 |
| `EXTR-1A.2` | P0-M3 | EXTR | EXTR-1A | Article extractor — Tier 2 default = GPT-5 mini (OpenAI), 한국어 long-context 시 Sonnet 4.6 standard override (ADR-0023 + DEC-010) + run ledger (vendor/tier/domain_override_reason 필드) | EXTR-1A.1, OPS-1A.1 | AC-007, AC-010, SPIKE-003 | defined | planned | TBD | EXTR-1A.3 |
| `EXTR-1A.3` | P0-M3 | EXTR | EXTR-1A | Tier 1 escalate (GPT-5.5 Pro standard) + multi-vendor prompt caching layering (OpenAI 5분 cache / Anthropic 5분 / batch 1시간 sync) + batch API (vendor 추상화 layer) | EXTR-1A.2 | AC-010, AC-019 | defined | planned | TBD | EXTR-1A.4 |
| `EXTR-1A.4` | P0-M3 | EXTR | EXTR-1A | Review queue + auto-confirm threshold(SPIKE-002 결과 반영) | EXTR-1A.3, SPIKE-002 | AC-010, AC-015 | defined | planned | TBD | EXTR-1A.5 |
| `EXTR-1A.5` | P0-M3 | EXTR | EXTR-1A | **Data Science Module (ADR-0024)** — Polars + DuckDB + statsmodels + scipy stack lock + dataset_vintage 입력 → derived metric 산출 → `derived_metric_ledger` (SQLite) row + reproducibility 3-tuple (dataset_vintage_id + spec_sha256 + library_version_lock_sha256). + Report extractor (Tier 2 GPT-5 mini with structure prompt + section page locator) | EXTR-1A.1, INFRA-1A.2 (derived_metric_ledger migration) | AC-009 | defined | planned | TBD | EXTR-1A.6 |
| `EXTR-1A.6` | P0-M3 | EXTR | EXTR-1A | **Cross-vendor reviewer infrastructure** (ADR-0023 INV-0023-4) — preflight cite check overclaim (GPT-5 nano → Haiku 4.5) + scenario validate adversarial (GPT-5.5 Pro xthink → Opus 4.7 xhigh) + high-stakes thesis (operator flag) 의 cross-vendor pair wiring + `cross_vendor_review_ledger` (SQLite) row + cross_vendor_review_coverage KPI ≥ 0.95 측정 | EXTR-1A.5, INFRA-1A.2 (cross_vendor_review_ledger migration) | AC-013, AC-019 | defined | planned | TBD | OPS-1A.2 |
| `OPS-1A.1` | P0-M3 | OPS | OPS-1A | Run ledger 테이블 + cost 집계 | INFRA-1A.2 | AC-019 | defined | landed | src/ops/run-ledger.ts (startRun/completeRun/failRun/getDailyCostUsd/getDailyCostBreakdown) + migrations/sqlite/v4_run_ledger_completed_at_idx.sql + tests/unit/run_ledger_test.ts (29 tests, TEST-019); PR #23 merged 2026-05-12 (23de14c). cost throttling worker는 OPS-1A.2 슬라이스에서 추가 | OPS-1A.2 |
| `OPS-1A.2` | P0-M3 | OPS | OPS-1A | 일별 cost 상한 throttling worker | OPS-1A.1 | AC-019 | defined | planned | TBD | OPS-1A.3 |
| `OPS-1A.3` | P0-M3 | OPS | OPS-1A | Metrics framework v0 — metrics_run hook (publication preflight, scenario_validate, build_evidence_pack 부산물) + CLI `pipeline metrics report` markdown/CSV | INFRA-1A.2, OPS-1A.1 | AC-029 | defined | planned | TBD | OPS-1A.4 |
| `OPS-1A.4` | P0-M3 | OPS | OPS-1A | Policy learning Pattern 1 (source policy refinement) + policy_learning_events / source_policy_rules + raw_cache_items TTL worker | INFRA-1B.5 | AC-030 | defined | planned | TBD | INFRA-1B.7 |
| `INFRA-1B.7` | P0-M4 | INFRA | INFRA-1B | Search/Query 인터페이스 — Q-050 resolution 으로 **7a/7b/7c/7d/7e 5 슬라이스** split (7e = ModelGateway + shared schema, GPT T-26/T-31). P0-M6 안에는 INFRA-1B.7a 만 흡수 | INFRA-1B.4 | AC-002 | defined | planned (split) | TBD | INFRA-1B.7a |
| `INFRA-1B.7a` | P0-M4 | INFRA | INFRA-1B | **Internal Evidence Search API** (Q-050 split from INFRA-1B.7) — `src/search/internal-search.ts` — searchClaims / searchChunks / searchSnapshotsByUrl / expandClaimNeighborhood / buildEvidencePack (REQ-023 4-section). Neo4j FTS + Cypher traversal 만. single-shot, agent loop X. **P0-M6 안에 흡수** | INFRA-1B.4, Q-050 resolution | AC-002, AC-028 | defined | planned | TBD | AGG-1A.1 |
| `INFRA-1B.7b` | P1+ | INFRA | INFRA-1B | **Research Orchestration Schema** (Q-050 + Q-051 통합 migration v8) — exploration_round / round_search_run / round_candidate_url / round_synthesis / research_turn / operator_context **+ turn_event (GPT T-4 durable SSE log) + operation_ledger (GPT T-1 saga pattern) + quota_reservation (GPT T-7 atomic cap)** 9 신규 테이블 + research_session ALTER (title / **initial_intent** / mode / editorial_intent_id / total_cost_usd / max_rounds / per_session_cost_cap_usd / forked_from_session_id / **ready_for_thesis_compose (Q-051 Q-I)** / **fork_kind (Q-051 Q-J)**) + raw_cache_items.round_id + run_ledger.round_id / search_run_id / turn_id FK + Snapshot Neo4j property (raw_body_hash / canonical_text_hash / source_round_id). **document_fetch_state (Q-049)** 는 INFRA-1B.10 slice 안에서 별도 v7 migration 으로 분리 (P0-M2 hardening 흡수 위해 P1+ 의존 회피) | INFRA-1B.7a, Q-050/051 resolution | (schema) | defined | planned | TBD | INFRA-1B.7c |
| `INFRA-1B.7e` | P1+ | INFRA | INFRA-1B | **ModelGateway + shared schema** (GPT T-26/T-31) — `src/shared/llm/model-gateway.ts` 신규 (data/llm_routing.yaml 로드 + vendor/tier/model 결정 + run_ledger 자동 기록 + streaming event normalize) + `src/shared/schema/` 디렉토리 (Zod schemas — Astro Content Collection + Bun API 양쪽 import, no bun:sqlite / neo4j 의존) + `src/shared/research/round-context-pack.ts` (RoundContextPack pure type) | INFRA-1B.7b | AC-019, AC-028 | defined | planned | TBD | INFRA-1B.7c |
| `INFRA-1B.7c` | P1+ | INFRA | INFRA-1B | **Search Orchestrator** (Q-050) — `src/search/orchestrator.ts` — InternalEvidenceSearch (a/c) + ExternalAISearch (b) + KnownSourceRefresh + RoundSynthesizer (d) 4 layer 통합. RoundContextPack 생성 / 소비. **Provider interface 정의 (orchestrator skeleton-first)** — DISCOVERY-EXT.1 의 구체 implementation 전 단계에서도 mock provider 로 작동 가능 (interface 만 lock) | INFRA-1B.7b, INFRA-1B.7e (ModelGateway + RoundContextPack pure type 의존) | AC-028 | defined | planned | TBD | INFRA-1B.7d |
| `INFRA-1B.7d` | P1+ | INFRA | INFRA-1B | **Round Synthesizer** (Q-050) — `src/research/synthesis/round-synthesizer.ts` — b+c 결과 reconciliation → Dossier/Scenario/NextRoundPlan 생성. hypothesis_delta + branch tracking | INFRA-1B.7c | AC-028, AC-044 | defined | planned | TBD | AGG-1A.6 |
| `INFRA-1B.8` | P1+ | INFRA | INFRA-1B | **Revisit Policy** (Q-049) — `src/discovery/revisit/policy.ts` — TTL + event-driven hybrid. content type 기반 default. publication preflight / stale_trigger / manual_refresh / multi_round_force_revalidate event trigger | INFRA-1B.9 (canonical hash 도입 후 revisit 의사결정), ADR-0010 reflow (Q-049 #4) | AC-016, AC-020 | defined | planned | TBD | DEPLOY-1A.1 |
| `INFRA-1B.9` | P1+ | INFRA | INFRA-1B | **Canonical Text Hash** (Q-049 / DEC-021) — Mozilla Readability + linkedom + normalize + sha256 = canonical_text_hash. **Migration sequence (DEC-021 #3 구현 의무)**: (1) 기존 Snapshot.content_hash → raw_body_hash backfill 먼저 → (2) DROP `snapshot_content_hash_unique` (migrations/neo4j/v1_schema.cypher line 57 의 실제 constraint 이름) + CREATE `snapshot_raw_body_hash_unique` (v1 schema 의 `snapshot_` prefix 컨벤션 유지) → (3) canonical_text_hash nullable property + `snapshot_canonical_text_hash_idx` non-unique INDEX 추가 → (4) 신규 Snapshot Readability 적용 → (5) 기존 Snapshot canonical_text_hash 별도 backfill batch (host concurrency cap + daily candidate cap 와 분리) + document_fetch_state ALTER (canonical_text_hash 컬럼 추가). 변경 감지 1차 = canonical diff (ADR-0010 INV-0010-4 lock), 2차 = raw diff (source_changed_minor alert, v1+) | INFRA-1B.10 (document_fetch_state schema), DEC-021 (Q-049 resolution) | AC-020 | defined | planned | TBD | INFRA-1B.8 |
| `INFRA-1B.10` | P0-M2 hardening | INFRA | INFRA-1B | **Article-Level Conditional Fetch + document_fetch_state v7 migration** (Q-049 — GPT 권고 scope 확대) — **migrations/sqlite/v7_document_fetch_state.sql** 신규 (table + indexes, ALTER-only contract) + If-None-Match / If-Modified-Since 전송 + 304 시 Snapshot 미생성 + state 만 갱신 + queue enqueue 전 fetch_state 검사. **P0-M2 게이트 검증 안에 흡수** (단순 + 즉시 비용 절감). canonical_text_hash 컬럼은 INFRA-1B.9 에서 ALTER 추가 (P1+) | INFRA-1B.3 (snapshot-fingerprint), ADR-0028 | AC-003, AC-020 | defined | planned | TBD | INFRA-1B.9 |
| `DISCOVERY-EXT.1` | P1+ | INFRA | INFRA-1B | **External AI Search Provider** (Q-050) — `src/discovery/external/ai-search-provider.ts` interface + geminiSearchGrounding / openaiSearchTool / anthropicWebSearch / manualCandidateInput 4 implementation. CandidateUrl 표준 type emit. AI search result → safeFetch → snapshot → claim ingestion 강제 (Q-050 (5a)) | INFRA-1B.7c, 신규 ADR (Q-050 #7) | AC-001, AC-022 | defined | planned | TBD | AGG-1A.6 |
| `RESEARCH-1A.1` | P1+ | INFRA | RESEARCH-1A | **HTTP API + ResearchTurn schema + Auth + Stack scaffold** (Q-051 round 2 / **DEC-022 stack lock + DEC-023 routing default**) — `src/api/research-ask.ts` (Hono routes) + research_turn 테이블 + operator_context 테이블 + **DEC-022 stack scaffold**: `astro.config.ops.ts` (Hetzner build target) + `@astrojs/react` integration + `src/shared/ui/` shadcn/ui CLI init + `src/ops/lib/query/` TanStack Query v5 provider + SSE binding hook (`useEventStream` + `setQueryData` 패턴) + Radix primitives 도입 + **CLI flags**: `--session <id>` / `--round <id>` (context selection — route enum 은 `continue_round`) + `--new-session` (route=`new_session`) / `--new-round` (route=`new_round`) / `--branch-from <id>` (route=`branch_round`) / `--fork-session --from-session <id>` (route=`fork_session`) / `--answer-only` (route=`answer_only`). 7 flag = 6 route enum value. **DEC-023 #1 lock**: no-context default = HTTP 400 / CLI exit 1 (error message: "No active session/round. Choose --new-session / --session / --round"). Web UI `/ops/ask` 진입 시 active context NULL 이면 "새 세션으로 시작" 버튼 default highlight 명시 (RESEARCH-1A.2 안 처리). **DEC-023 #2 lock**: classifier 활성은 RESEARCH-1A.2 로 deferred — 본 1A.1 안에서는 explicit route selector dropdown 만, classifier 호출 없음. **Cloudflare Tunnel + Access wiring (v1+, Q-051 phasing lock)** + **ADR-0031 (Research App /ops Stack) 발급 완료 (status: accepted, DEC-022 anchor 해소)** — INV-0031-1~7 lock + §3.1 package.json caret 범위 + §3.2 `astro.config.ops.ts` shape + §3.3 `client:*` directive policy 페이지별 표 + §3.4 `src/shared/ui` layout + §3.5 `src/ops/lib/query` 구조 + §3.6 Lighthouse mobile bundle budget acceptance gate. 본 슬라이스 accept 조건의 일부 = INV-0031-7 bundle budget gate **(a) `client:visible` only** — JS transfer ≤ 100KB gzip / LCP ≤ 2.5s / TBT ≤ 200ms / CLS ≤ 0.1 (Slow 4G mobile). `client:load` 170KB gate 는 `/ops/scenarios/:sid` 도입 슬라이스 RESEARCH-1A.3 acceptance 로 phasing. CLI 는 P0-M6 critical path (PUB-1A.5 publish/edit), P1+ 부터 web /ops 가 primary, CLI 는 자동화/cron secondary | INFRA-1B.7b (schema), INFRA-1B.7e (shared schema Zod), AGG-1A.6 (multi-round backend), DEC-022 (stack lock), DEC-023 (routing default lock), **ADR-0031 (/ops stack lock + bundle budget gate)** | AC-025 (확장) | defined | planned | TBD | RESEARCH-1A.2 |
| `RESEARCH-1A.2` | P1+ | INFRA | RESEARCH-1A | **Web UI core pages (mobile-first) + AI intent classifier (advisory)** (Q-051 / DEC-022 / DEC-023) — `/ops` 홈 + `/ops/ask` global ask + `/ops/sessions` list + `/ops/sessions/:id` round timeline + `/ops/turns/:tid` + active context badge + Route dropdown (canonical enum: `continue_round` / `new_round` / `branch_round` / `fork_session` / `new_session` / `answer_only` — 6 route value, schema CHECK constraint 와 일치) + Tailwind responsive + **DEC-022 stack**: Astro shell + React 18 island (`client:visible` directive 우선, scenario graph 만 `client:load`) + shadcn/ui Sheet/Drawer for 모바일 + TanStack Query 의 useQuery/useMutation + SSE EventSource 로 turn progress streaming. **DEC-023 #2/#3 lock**: AI intent classifier 본 슬라이스에서 활성 — advisory only (auto-route 없음), Tier 3 LLM (ADR-0023). chip UI 표시 + 사용자가 dropdown/chip-tap 으로 명시 선택 의무. ENV `KWM_AUTO_ROUTE_CONFIDENCE_MIN` reserved-inactive (후속 DEC 까지 enforcement 없음). classifier prompt 는 ADR-0029 INV-0029-1 untrusted-content wrapper 의무. **DEC-023 #1 lock 보강**: `/ops/ask` no-context 진입 시 "새 세션으로 시작" 버튼 default highlight (one-tap explicit new_session). Topology B `/ops/*` private prefix lock. **ADR-0031 §3.3 `client:*` directive policy 페이지별 표 enforce** (INV-0031-2) — 본 슬라이스 신규 페이지 추가 시 §3.3 표 갱신 의무. **본 슬라이스 안 voice input 미포함** — voice button (MediaRecorder + `client:only="react"`, INV-0031-6) 은 RESEARCH-1A.5 (Whisper API) 슬라이스 scope. UI-spec docs/design/ui-spec-research-app.md 참조 | RESEARCH-1A.1, **ADR-0031 (directive policy enforce)** | AC-025 (확장) | defined | planned | TBD | RESEARCH-1A.3 |
| `RESEARCH-1A.3` | P1+ | INFRA | RESEARCH-1A | **Web UI advanced (desktop-first)** (Q-051 / DEC-022) — `/ops/claims/:cid` evidence panel + `/ops/scenarios/:sid` graph view (vega-lite / mermaid, Q-G 모바일 zoom/pan 도 가능) + `/ops/dossiers/:did` 2-pane editor + `/ops/publications/:pid` preflight inspector + `/ops/drafts/:did/edit` (DEC-022 React island + Radix Dialog/Popover, shadcn/ui Form/Textarea). Topology B `/ops/*` private prefix 의무. **`/ops/scenarios/:sid` 가 본 슬라이스에서 도입되는 유일 `client:load` 페이지 (INV-0031-2 exception)** → 본 슬라이스 accept 조건의 일부 = INV-0031-7 bundle budget gate **(b) `client:load` 170KB gzip per route** (Slow 4G mobile, lighthouse CLI). RESEARCH-1A.1 의 `client:visible` 100KB gate 와 함께 모든 directive 별 gate full coverage | RESEARCH-1A.2, **ADR-0031 (`client:load` budget gate)** | AC-018, AC-035 | defined | planned | TBD | RESEARCH-1A.4 |
| `RESEARCH-1A.4` | P1+ | INFRA | RESEARCH-1A | **PWA + offline draft** (Q-051 / DEC-022 Critique 5) — manifest.json + service worker (Workbox + Astro adapter) + IndexedDB pending turns + online sync + **TanStack Query optimistic update 와 IndexedDB queue sync** (DEC-022). 모바일 idea capture 우선 | RESEARCH-1A.2 | (UX) | defined | planned | TBD | RESEARCH-1A.5 |
| `RESEARCH-1A.5` | P1+ | INFRA | RESEARCH-1A | **Voice input (Whisper API)** (Q-051) — `/api/transcribe` + 모바일 voice 버튼 + ADR-0023 routing 안에 Whisper 추가 (Tier 3). cost ≈ $0.006/분 | RESEARCH-1A.2 | (UX) | defined | planned | TBD | MVP gate |
| `AGG-1A.6` | P1+ | AGG | AGG-1A | **Multi-round Research Session backend** (Q-050 / **DEC-023 #4 fork inheritance**) — `src/research/session/` — ResearchSession + ExplorationRound + parent_round_id branching + RoundContextPack propagation + ScenarioRevision lineage bridge + termination conditions enforcement. EditorialIntent.purpose enum (ADR-0025 갱신) + research_session.mode hybrid. **DEC-023 #4 fork inheritance lock**: `fork_kind = side_exploration` → **α (fresh intent only)** — `forked_from_session_id` + `fork_kind` + `title` 만 carry, `editorial_intent_id` / `mode` / initial_intent / Dossier 모두 fresh. `fork_kind = derivative` → **β (soft inherit)** — `mode` + `editorial_intent_id` parent 와 동일 carry, initial_intent 는 새 question, Dossier 는 fresh. **γ deep inherit (`promoted_claim_ids[]` deep copy) disallowed by default** — Dossier evidence 는 RoundContextPack 에서 reference 로 조회. CLI / UI surface 는 RESEARCH-1A.* 슬라이스에서 본 backend logic 호출 (no circular dependency) | INFRA-1B.7d, AGG-1A.5 EditorialIntent, DEC-023 (fork inheritance lock) | AC-014, AC-017 | defined | planned | TBD | EXTR-1A.7 |
| `EXTR-1A.7` | P1+ | EXTR | EXTR-1A | **AI Agent Loop** (Q-050) — multi-step tool-calling (internal-search.ts / fetch-and-fingerprint.ts / round_synthesis.ts). 신규 ADR (가칭 — Search Orchestrator + Agent Loop) 의 recursion depth cap (default 5) / per-loop cost cap ($0.50) / tool whitelist / prompt caching 강제 / audit log (run_ledger.search_run_id FK ↔ round_search_run.search_run_id ↔ research_turn.turn_id chain) 의무 lock | EXTR-1A.6, INFRA-1B.7d (Round Synthesizer 의존 — round_synthesis.ts), AGG-1A.6 (multi-round session context), 신규 ADR (Q-050 #7) | AC-019, AC-029 | defined | planned | TBD | MVP gate |
| `AGG-1A.1` | P0-M4 | AGG | AGG-1A | Dossier 합성 (promoted claim 정책 + counterclaim pool + source_perspective 분포 + **`:EVIDENCE_FOR` evidence_role 6 enum schema lock + operator_lock assignment 강제 (AC-043 / INV-0027-5, DEC-020 Q-043 resolution)** + **Dossier minimum coverage 자동 검증 (supporting ≥3 / opposing ≥2 / monitoring ≥3, AC-044 (a) / INV-0027-4 — 미달 시 Dossier reject → manual review queue)**). **AC-044 cover 범위 = (a) minimum coverage + (b) `:EVIDENCE_FOR` enum + operator_lock 만**. **AC-044 (c) EvidencePack v0 4-section 생성 (REQ-023 + ADR-0027 INV-0027-3) 은 본 slice 영역 아님 — PUB-1A.2 (P0-M6) 에서 cover** | EXTR-1A.4, INFRA-1B.7a (Q-050 split — P0 consumers 는 7a 만 의존, 7b/7c/7d 는 P1+) | AC-004, AC-027, AC-043, AC-044 (a)+(b) only | defined | planned | TBD | AGG-1A.2 |
| `AGG-1A.2` | P0-M5 | AGG | AGG-1A | Scenario composer + scenario_revisions ledger (append only) + impact_targets / impact_direction_by_target / transmission_channels v0 | AGG-1A.1, INFRA-1A.7 | AC-014, AC-017, AC-026 | defined | planned | TBD | AGG-1A.3 |
| `AGG-1A.3` | P0-M5 | AGG | AGG-1A | Scenario Validator 5종 검사 + counterclaim polarity-symmetric direction tag | AGG-1A.2 | AC-012 | defined | planned | TBD | AGG-1A.4 |
| `AGG-1A.4` | P0-M6 | AGG | AGG-1A | Thesis Composer — Scenario revision + **EditorialIntent reference (AGG-1A.5)** → Thesis with stance + market_stance(optional) align (ADR-0025 INV-0025-6). default = GPT-5.5 Pro standard (Tier 1), high-stakes flag 시 Tier 0 cross-vendor review | AGG-1A.3, AGG-1A.5, INFRA-1A.7 | AC-026 | defined | planned | TBD | OPS-1B.1 |
| `AGG-1A.5` | P0-M6 | AGG | AGG-1A | **EditorialIntent Composer** (신규, ADR-0025) — Scenario revision → EditorialIntent (purpose / audience / tone / alignment_criteria / exclusion_criteria / bidirectional_weight_intent) LLM 자동 propose (Tier 1 GPT-5.5 Pro standard) + **운영자 명시 lock 의무** (`decided_by_operator = true`, INV-0025-4). CLI `pipeline intent compose / show / lock` + Neo4j EditorialIntent 노드 + `:HAS_INTENT` / `:USES_INTENT` relationship + vault `editorial_intents/<eit_id>.md` + Astro Zod schema editorial_intent_id 확장 (ADR-0022 INV-0022-3) | AGG-1A.3, INFRA-1A.2 (EditorialIntent migration) | AC-026 | defined | planned | TBD | AGG-1A.4 |
| `OPS-1B.1` | P0-M5 | OPS | OPS-1B | Stale worker (time / snapshot_diff / counterclaim 트리거 3종) | INFRA-1A.4, EXTR-1A.4 | AC-016 | defined | planned | TBD | PUB-1A.1 |
| `PUB-1A.1` | P0-M6 | PUB | PUB-1A | ContentDraft composer — **v0 blog_long only** (DEC-005). 4-format schema-level lock 유지(ADR-0025 supersedes ADR-0011), 나머지 3 format composer는 v1+ phasing (Q-032). 인용 ledger + Thesis 재사용 + **EditorialIntent `:USES_INTENT` reference 의무** (ADR-0025 INV-0025-2). ContentDraft 산출물은 `vault/publications/blog_long/<slug>.mdx` 로 emit + frontmatter `editorial_intent_id` 필드 (Astro Zod schema 확장, ADR-0022 INV-0022-3) | AGG-1A.4, AGG-1A.5 | AC-018 | defined | planned | TBD | PUB-1A.2 |
| `PUB-1A.2` | P0-M6 | PUB | PUB-1A | Cite Check 5+1 (stale / retracted / horizon / unit / overclaim + unresolved HIGH/CRITICAL access_intervention + v1+ one-sided thesis warning) + EvidencePack v0 4-section (**AC-044 (c) — evidence_role grouping 기준 4-section, ADR-0027 INV-0027-3**). 일부는 Astro Zod schema build-time enforce (ADR-0022 INV-0022-3) | PUB-1A.1, OPS-1B.1, INFRA-1B.5 | AC-013, AC-028, AC-044 (c) only | defined | planned | TBD | PUB-1A.3 |
| `PUB-1A.3` | P0-M6 | PUB | PUB-1A | Publication ledger + cascade alert (Q-003 결정 반영) + vault sync trigger — **DEC-006로 git push 단일화** (별도 `pipeline vault-sync` CLI v0 제거, v1+ Q-033 외부 플랫폼 auto cross-post 시 재도입). v0 manual correction approve (DEC-005 + ADR-0018) | PUB-1A.2, Q-003 resolution | AC-013 | defined | planned | TBD | PUB-1A.4 |
| `PUB-1A.4` | P0-M6 | PUB | PUB-1A | **자체 사이트 Astro skeleton** — Astro 5.0 + Content Collection(`glob('vault/publications/**/*.{md,mdx}')`) + Zod schema mirror (**status / cite_refs[] / correction_ledger[] / format + editorial_intent_id (ADR-0025) + editorial_quality_rubric_passed (DEC-012)**) + `<Cite/>` / `<RetractionBanner/>` / `<CorrectionLedger/>` 컴포넌트 + pagefind + vega-lite/mermaid + `@astrojs/rss` 4 format feed + **Cloudflare Pages 배포** (git push trigger) (ADR-0022). 6 field 모두 dead-link / invalid / false / missing 시 build fail (AC-035) | PUB-1A.3 | AC-018, AC-035 | defined | planned | TBD | PUB-1A.5 |
| `RESEARCH-1A.API0` | P0-M6 | INFRA | RESEARCH-1A | **Hetzner Bun + Hono API skeleton (P0-M6 read-only)** — `src/api/server.ts` Bun + Hono entry + systemd unit + `/api/healthz` + `/api/readyz` + Tailscale Serve binding + thin read-only endpoints (`GET /api/sessions` / `GET /api/sessions/:id` / `GET /api/publications`). SQLite + Neo4j read-only access. Doppler env loading. CF Pages 와 분리된 build target (`astro.config.ops.ts`) | INFRA-1A.2 (SQLite schema), INFRA-1B.4 (chunker, Neo4j read 의존) | (skeleton) | defined | planned | TBD | RESEARCH-1A.0 |
| `RESEARCH-1A.0` | P0-M6 | INFRA | RESEARCH-1A | **Read-only `/ops` mobile minimum (Q-051 Q-C/Q-D 결정)** — `/ops` 홈 + `/ops/sessions` list + `/ops/sessions/:id` 읽기 (**P0 schema 안의 컬럼만**: session_id / status / scenario_id link / thesis_id link / created_at — **`title` / `initial_intent` / `editorial_intent_id` / round timeline 미포함**, 이들 컬럼은 INFRA-1B.7b 의 v8 migration P1+ 에서 추가) + `/posts/:slug` public 검토. RESEARCH-1A.API0 의 read-only endpoint 사용. **v0 Auth = Tailscale-only** (Q-051 lock, 폰 + 데스크탑 Tailscale client). CF Tunnel/Access 는 v1+. ask / edit / publish trigger 는 CLI (critical path). RESEARCH-1A.1+ (full ask/round/orchestration / round timeline) 는 P1+ phasing | RESEARCH-1A.API0 (Hetzner Bun API skeleton) | (ops UX — Q-051 AC-WEB-007/008 일부) | defined | planned | TBD | PUB-1A.5 |
| `PUB-1A.5` | P0-M6 | PUB | PUB-1A | **첫 publication (blog_long 1건)** — **카테고리 = 경제 + 주제 = "한국 부동산 시장/정책의 현주소, 리스크" (DEC-009) + sub-topic = "한국 부동산 폭락 시나리오: 누적 risk × 잘못된 금리정책 × 강력한 부동산 대책 결합" (DEC-011 lock, Q-036 resolved)** × **1 EditorialIntent 명시 작성/lock** (ADR-0025, audience + tone + bidirectional_weight_intent 운영자 선택) × 1 Thesis × 1 blog_long ContentDraft → vault/publications/blog_long/ commit → git push → Cloudflare Pages build → 자체 사이트 publish. Substack / YouTube / X manual cross-post (cite footnote는 자체 사이트 URL anchor — AC-034 lint). **PUB-1A.5 accept 시 운영자 Editorial Quality Rubric (AC-036~042, DEC-012) manual verify + Dossier evidence_role minimum coverage (AC-044, ADR-0027) 의무**. **첫 publication 발행 흐름 = CLI critical path + /ops read-only mobile 보조 (Q-051 Q-C/Q-D 결정)** — RESEARCH-1A.0 의 read-only inspection 으로 publication status / cite anchor 폰 검토. **v0 turn-key MVP gate** | PUB-1A.4, AGG-1A.4, AGG-1A.5, DEC-009, DEC-011, RESEARCH-1A.0 | AC-013, AC-018, AC-026, AC-028, AC-034, AC-035, **AC-036, AC-037, AC-038, AC-039, AC-040, AC-041, AC-042, AC-044** | defined | planned | TBD | MVP gate accepted |

## Gates / Acceptance

- Gate definitions live in `06_ACCEPTANCE_TESTS.md`.
- Automated checks are listed in `docs/current/TESTING.md` once they exist.
- CI/CD guidance lives in `docs/11_CI_CD.md`; workflow examples live under
  `.github/workflows/*.yml.example`.
- A slice can be `landed` before its gate is `passing`.
- A milestone is `accepted` only when its required gates are passing or explicitly waived.

## Traceability

- Completed slices should have a row in `09_TRACEABILITY_MATRIX.md`.
- Link slices to the relevant Q / DEC / ADR, REQ / NFR, AC / TEST, and milestone.
- Do not use trace rows as a backlog. They are connection records for important paths.

## Dependencies

- 외부 팀 / 시스템:
  - OpenAI API (GPT-5 nano/mini/standard/extended-thinking + prompt caching + batch + response_format json_schema) — default vendor (ADR-0023, DEC-010)
  - Anthropic API (Claude Haiku 4.5 / Sonnet 4.6 / Opus 4.7 + prompt caching + batch + strict tool-use) — cross-vendor review + domain override (ADR-0023, DEC-010)
  - Google AI Studio (Gemini 2.5 Flash + Search grounding) — 탐색 보조 + Tier 3 비용효율 우위 시만 (메인/리뷰 X, ADR-0023 INV-0023-5)
  - Cloudflare R2 (S3 compatible)
  - Cloudflare Pages (자체 사이트 호스팅 — git push trigger, ADR-0022)
  - v1+ Substack / YouTube Data / X API (auto cross-post — Q-033)
  - v1+ TTS provider API (외주 또는 self-host — Q-031)
- 라이브러리 / 벤더:
  - SQLite + FTS5 (system)
  - `openai` SDK (OpenAI 공식, default LLM vendor — ADR-0023)
  - `@anthropic-ai/sdk` (Anthropic 공식, cross-vendor review + domain override)
  - `@google/genai` 또는 `google-genai` (Gemini, 탐색 보조 — 선택)
  - Python 3.12+ + Polars + DuckDB + statsmodels + scipy + numpy (Data Science Module, ADR-0024) — EXTR-1A.5 / EXTR-1A.6 진입 직전 lock
  - HTTP/parsing libs는 EXTR-1A.5 단계에서 결정 (PDF parser, dataset parser)
  - Astro 5.0 + `@astrojs/mdx` + `@astrojs/rss` + Zod (자체 사이트, ADR-0022)
  - pagefind (client-side full-text)
  - vega-lite + mermaid (chart / diagram)

## Risks (open)

- SPIKE-001: **Neo4j Community + native FTS** NFR-001 (검색 < 1초 p95) —
  INFRA-1A.2 진입 직전 결과 필요 (대상 SQLite+FTS5에서 Neo4j로 갱신)
- SPIKE-002: auto-confirm threshold 0.85 fp율 — EXTR-1A.4 진입 직전 결과 필요
- SPIKE-003: prompt caching cache hit rate ≥ 70% — EXTR-1A.3 진입 직전 결과 필요
- Q-001: scenario horizon enum — AGG-1A.3 진입 전 lock
- Q-002: Dossier `stale_after` default — OPS-1B.1 진입 전 lock
- Q-003: Publication 정정 ledger 트리거 — PUB-1A.3 진입 전 lock
- ~~Q-004~~: **resolved (INFRA-1A.2)** — k-world-monitor repo는 SQLite(research.db)만 보유; vault-wide index는 second-brain vault jsonl 책임. promoted artifact export 시 변환 (INFRA-1B+).
- ~~Q-020~~: **resolved (INFRA-1A.2)** — 1인 internal use 범위 lock. (a) embed-배포 / (b) Cypher procedure fork / (c) open data dump 동봉 셋 다 미해당 현재. 해당 시 별도 ADR 의무.
- ~~Q-021~~: **resolved (INFRA-1A.6, 2026-05-12)** — 72 source 가
  `data/sources_seed.yaml` 로 commit (PR #12, 67a5f6e). 분포 risk 19% /
  opportunity 29% / neutral 42% / mixed 10% (AC-027 통과, 안전 마진 4%).
  size cap 폐기 (DEC-009 reflow). RSS endpoint 실 검증은 INFRA-1B.1 ~ 1B.2
  슬라이스에서 별도 완료
- ~~Q-022~~: **resolved by DEC-004** (v0 4 메타 카테고리: 정책 / 경제 / 사회
  / 대중문화. 기존 8 enum + tag 5개는 subtopic_tags[] 로 강등 보존)
- ~~Q-024~~: **resolved (INFRA-1A.2)** — v0: APOC standard + Cypher 5.x core. v1+: GDS Community 알고리즘. Enterprise-only/extended는 별도 ADR 권한 부여 시만.
- Q-025: 이 repo (= second-brain vault 기준 "외부 repo") 부트스트랩 cadence
  — INFRA-1A.1 완료 후 재평가
- ~~Q-026~~: **resolved by DEC-006** (vault sync trigger = git push 단일화,
  ADR-0022 자체 사이트 stack 후 단순화)
- ~~Q-027~~: **resolved by DEC-007** (retention / R2 lifecycle 3 expire
  rule + 의미적 GC batch 3개 + soft-delete tombstone 14d grace + RETENTION_
  PROTECTED_KINDS 상수 + raw_cache 24h~7d ceiling)
- ~~Q-028~~: **resolved by DEC-008 → re-resolved by DEC-010** (LLM routing
  v2 multi-vendor — OpenAI GPT default + Anthropic dual-vendor + Google
  exploration-only + minimal cross-vendor review at preflight + Data
  Science Module (ADR-0024) for dataset + cost ceiling soft $5/hard $7.5/
  weekly $25 + Tier 0 일일 cap 5회 + backfill bucket + KPI 6개. DEC-008
  Anthropic-only 라우팅 supersede)
- Q-029: ImpactAssessment v0 embedded vs v1 노드 — AGG-1A.2 진입 전 lock
- Q-030: counterclaim multi-relation v1 도입 우선순위 — v1 진입 시점에 lock
- Q-031: TTS v1 timing + provider — v1 PUB-1B 트랙 진입 시점에 lock (DEC-005
  v0 TTS deferred 연장)
- Q-032: ContentDraft 4-format auto-generate phasing (newsletter →
  youtube_long → shorts 권고) — v1 PUB-1B 트랙 진입 시점에 lock (DEC-005
  v0 blog_long only 연장)
- Q-033: 외부 플랫폼 auto cross-post timing (Substack → YouTube → X 권고)
  — v1 PUB-1B 트랙 진입 + Q-032 진입 시점에 lock (DEC-005 v0 manual 연장)
- Q-034: Auto retraction trigger 정책 v1+ (live → corrected 일부 자동,
  retracted 는 v2까지 manual) — v1 OPS-1B 트랙 진입 시점에 lock (DEC-005
  v0 manual approve 연장)
- ~~Q-037 ~ Q-041~~: **resolved by DEC-019** (apoc.lock / worker_id CAS /
  chunked streaming / pre-deploy migration framework / millis-bearing
  timestamp). Implement slices: INFRA-1B.2.x / 1B.3.x / 1B.6.x (P1-M2/M3
  hardening) + DEPLOY-1A.1/1A.2 (P1-MVP-prep)
- ~~Q-042 ~ Q-048~~: **resolved by DEC-020** (NFR-003 5-hop trace path
  reflow / evidence_role / NFR-008 R2 audit code enforcement / invariant
  validator coverage extension / quota module / Doppler secret store / CI
  required check + branch protection). Implement slices:
  - AGG-1A.1 (evidence_role 자동 검증) — planned (slice 표 등록).
  - **INFRA-1B.3.x-audit (Q-044 R2 audit, TRACE-040 anchor) — landed (PR #39,
    2026-05-14)**. `src/storage/audit/policy-decisions.ts` + `INTENDED_ACTION`
    enum + `R2_UPLOAD_DECISION` 4 lifecycle values + snapshot-fingerprint r2Put
    2 call site 전후 INSERT + 16 audit unit tests + 8 audit-hook integration
    tests. **이어서 INFRA-1B.3.h1-policy-fix (R2 cross-source archive_policy
    guard, AI-P0-1 legal-safety P0) — landed (PR #41, 2026-05-15)** —
    `allLinkedSourcesAllowR2SnapshotUpload` rename + archive_policy 검사
    추가 + 6 regression tests. 잔여 hardening = OPS-1B.h1-runtime-invariant-scanner
    (planned, AI-P1-6) + INFRA-1B.3.h3-audit-hardening (v8 trigger + upload_attempt_id,
    planned, AI-P1-7).
  - OPS-1A.2 (quota module, slice 표 등록) — planned.
  - INFRA-1A.9-validator-extension (TRACE-041 anchor, AI-P2-5) — planned.
  - CI admin task (운영자 admin step, code 변경 없음) — DEC-024 D1 partial
    supersede 적용 (운영자 admin task 미실시, break-glass 절차 명시).
- ~~Q-049~~: **resolved by DEC-021** (revisit policy = TTL + event-driven
  hybrid; snapshot_diff = canonical_text_hash diff primary, raw_body_hash
  fallback; ADR-0010 INV-0010-4 lock). Implement slice: INFRA-1B.8/1B.9/
  1B.10 (P0-M2 hardening + P1+)
- Q-050: AI 검색 + repo 통합 architecture (proposed body landed, 7 운영자
  결정 항목 pending — Q-050 frontmatter status:open 유지). Implement slices:
  INFRA-1B.7a~e + AGG-1A.6 + DISCOVERY-EXT.1 + EXTR-1A.7 (대부분 P1+;
  INFRA-1B.7a 만 P0-M6 흡수)
- ~~Q-051~~: **resolved by DEC-022 + DEC-023** (UI stack lock = Astro shell
  + React island + shadcn/Radix + Tailwind + TanStack + SSE; Round 1 routing
  default 5 항목 lock). Implement slices: RESEARCH-1A.0/API0 (P0-M6) +
  RESEARCH-1A.1~5 (P1+). ADR-0031 placeholder landed, RESEARCH-1A.1 시작
  직전 본문 확장

## Capacity / Timeline

- 인원: 1명 (운영자 + LLM 에이전트 보조)
- 주당 가용 시간: TBD
- 예상 완료: P0-M1 ~ P0-M6 milestones은 시간 박싱 없이 진행. 첫 milestone
  (P0-M1) lock 이후 capacity 재평가. Q-025에서 cadence 가이드 (week 1-9
  reference) 결정.
